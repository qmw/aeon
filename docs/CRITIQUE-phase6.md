# Phase 6 critic findings

## critique 1 — 34/100
REJECT. Gate FAIL on the exact regions I was told to measure: near/far HF ramp 0.77 (needs >= 1.6), water MID/HF 0.77 (needs >= 0.9), crushedPct 0.42 (ceiling 0.30). Extending the probe to clean regions makes it worse — desert-mid HF_rms 30.04 with MID/HF 0.84, deep water HF_rms 0.56, far-jungle and near-jungle both sat 0.24 (floor 0.28). Six of the listed automatic fails are present in one frame.

Side by side, unlabeled, against a real Civ VI screenshot: the player picks Civ VI, and it is not close — it takes under a second.

The single tell: THE GRID DIES ON VEGETATION. In Civ VI you can always see the hex you are about to click, on every biome, because the grid is a designed stroke composited over the lit ground. Here the hex lattice is legible only on desert (crop shots/terrain-p6r1.png 900,430,260,180 — clean, continuous, follows slope) and is completely absent over grass, jungle and forest — I zoomed 60,620,300,200 at 3.5x and there is not one hex edge in that entire green field, measured edge contrast ~2-4/255 against the ~18/255 you need. Roughly 45% of your playable land has no board on it. The moment a player cannot see the tile they are clicking, this stops being a 4X map and becomes a landscape painting. Civ VI never loses the board. That is the tell.

The runner-up tell, if you want a material one: every cliff face in the top-left carries the same down-slope corduroy comb (crop 0,80,200,160 at 5x) — a single planar projection stretched along steepest descent. Civ VI's rock has bedding; yours has hair.

Nothing here is a taste disagreement with your own ART-BIBLE. I judged execution against your locked direction and the direction is not being executed. What IS good and must be kept: the HUD (top bar, notification rail, unit panel, minimap, end turn) is clean, well-spaced, zero clipping or overlap — I checked 1470,0,130,60 at 7x and the TECH/T badge is properly inset. The coastline foam read and the city keeps are the only things in the world layer holding up.

**Problems**
- ILLEGIBLE HEX GRID (auto-fail, non-negotiable #1). Grid is visible on desert only. Over grass/jungle/forest — crop 60,620,300,200 — zero hex edges survive the foliage speckle; perpendicular edge contrast measures ~2-4/255 vs the ~18/255 minimum. Even on desert the stroke is unstable: 1px hairline on some edges, 3-4px doubled and ghosty on others, so the AA is a raw screen-space derivative with no clamp, not analytic.
- DETAIL GROWS WITH DISTANCE (auto-fail). far-cliff HF_rms 16.32 vs near-field HF_rms 12.56 → ramp 0.77 against a 1.6 requirement. The micro octave is being evaluated at constant screen frequency with no footprint-driven fade — under a software rasteriser you have no hardware mips and you clearly are not computing the texel footprint yourself.
- PER-PIXEL CONFETTI STANDING IN FOR MATERIAL (auto-fail, non-negotiable #2). desert-mid HF_rms 30.04 against a 22 ceiling with MID/HF 0.84 below the 0.9 floor — crop 1050,690,250,170 is a wall of 2-3px cream pebble speckle at uniform screen density that reads as gravel dithering, not sand. The jungle is the same disease in green: 1-3px lime chips sprayed over near-black, no canopy structure, no tree clumping, no readable forest mass.
- UNTEXTURED FLAT-MATTE SURFACES (auto-fail). Deep ocean HF_rms 0.56 — a dead vinyl sheet. The pale top-left slabs (#918795 / #B0A6B6, v 0.63-0.88) carry essentially no high-frequency energy at all: flat lavender planes with a Gouraud gradient and nothing else.
- PROPS WITHOUT CONTACT SHADOW (auto-fail, non-negotiable #3). The plank shed at ~(1075,730), the timber derrick at ~(970,570), and every tree pinned to the cliff faces at ~(95,175) and ~(150,140) have no AO decal and no contact darkening — they intersect the ground plane and float. The cliff trees additionally do not orient to slope and are the same on-screen size at every depth.
- TOY / PROGRAMMER-ART UNIT SILHOUETTE (auto-fail). The Warrior portrait is a Playmobil bean: smooth featureless head, bucket helmet, flat blue torso, no shoulder line, nothing breaking the bounding box. On the map it is worse — the unit at ~(975,680) is an olive blob completely occluded by its own fixed-screen-size strength badge. A player cannot name the unit type from shape, which kills non-negotiable #4.
- STREAK ARTIFACTS AND ORIGAMI CLIFFS (auto-fail). Every cliff face carries parallel down-slope comb hatching from a single planar projection stretched along steepest descent, with hard corduroy banding at the base of crop 0,80,200,160. The geometry underneath is flat-shaded triangle fans with knife-edge silhouettes and visible per-triangle value discontinuities — no bedding, no strata, no talus, no erosion. Cliff sat measures 0.436 against the mountain ceiling of 0.08-0.18: these are terracotta shards, not rock.
- PALETTE NON-COMPLIANCE, CRUSHED AND OFF-HUE. crushedPct 0.42 vs 0.30 ceiling. Near grass #312D27 at v50 0.169 against a 0.28 floor and sat 0.189 against a 0.30 floor — mud. Jungle #474D3B is olive-khaki (R≈G) where the bible says #35592B. Deep ocean #52597A vs #123A63: 30% too light, 60% too desaturated, hue drifted 213°→226°. The shore band at x≈1130 reads #ADA7B3, hue ~280° violet, against lit sand at hue 31° — that is a 250° swing on the same surface, exactly the navy-shadow-on-tan-sand sin the bible forbids. Sand shadow #C5B3B5 vs lit #B7946F is a 38° hue swing with chroma collapsing 0.393→0.138, which means the ambient term is flat grey, not albedo-weighted with ground bounce.

**Fixes**
- GRID — pull it out of albedo into its own decal pass composited after lighting. Analytic width from hex barycentric distance d: w_px = clamp(0.055_world / fwidth(d), 1.15, 2.4); alpha = 1 - smoothstep(0, w_px, d/fwidth(d)). Composite value-only with a floor so it survives dark jungle AND bright sand: L_out = L_in * (1 - 0.42*a), plus a 1px inner lift of +0.06*a_inner. Acceptance: measured perpendicular dLuma >= 18/255 on every biome, and stroke width variance <= 0.4px across the frame. Fade only over open water, over depth > 3m, alpha *= 1 - smoothstep(3, 9, depth).
- FOOTPRINT-DRIVEN OCTAVE FADE — you have no hardware mips under a software rasteriser, so compute the footprint per pixel: fp = depth * tan(fovY/2) * 2 / screenH / max(cos(slope), 0.25). Weight the micro octave by 1 - smoothstep(0.6, 1.4, fp / 1.5) so it is fully gone once one pixel covers >1.4m. This one change fixes both the desert HF 30.04 overshoot and the inverted ramp.
- GROUND MATERIAL — rebuild as exactly three world-space octaves with locked wavelengths: macro 24m, meso 6m, micro 1.5m, amplitude ratio 1.0 / 0.45 / 0.18. No fourth layer, no screen-space term anywhere. Acceptance on the same material: near HF_rms 17-20, mid 13-15, far 8-11, ramp >= 1.8 (headroom over the 1.6 gate), MID/HF 1.0-1.2 everywhere.
- AERIAL PERSPECTIVE — currently far-jungle sat 0.240 vs near-jungle 0.245, i.e. literally zero desaturation with distance. Add t = saturate((depth - 40) / 120); blend toward #A9B6C4 at 0.22*t and scale chroma by (1 - 0.45*t). Acceptance: far sat <= 0.72 * near sat on the same biome, far mean luma > near mean luma, always.
- CLIFF GEOMETRY — tessellate the cliff band and displace with stratified bedding: horizontal planes every 1.8m world at 0.35m amplitude, plus 0.12m vertical fracture noise, so the silhouette gains steps instead of knife edges. Add a talus skirt at the base, 2.5m run, blending to the ground material over 1.2m.
- CLIFF MATERIAL — replace the single planar projection with triplanar: Y weight pow(|n.y|, 4), lateral weights pow(|n.xz|, 4), normalized. That kills the down-slope comb outright. Then desaturate: cliff-lit currently sat 0.436, cut chroma by 65% to land in the mountain band 0.08-0.18 while holding value 0.32-0.62.
- AMBIENT — replace the flat grey lift with albedo-weighted ambient plus bounce: ambient = skyColor * 0.35 * albedo + (albedo * sunColor * 0.25) * 0.20. Acceptance: shadow hue within 8° of lit hue on the same surface (sand is at 38° now, the shore band at 250°), and shadow sat >= 0.75 * lit sat (sand is at 0.35 now).
- CRUSH — raise the ambient floor by +0.055 and lengthen the tonemap toe; nothing lit should land below luma 0.20. Re-target near grass from v50 0.169 to 0.34-0.40 and sat 0.189 to 0.30-0.42. Acceptance: crushedPct <= 0.25, blownPct <= 0.03, and no sand v95 hitting 1.0 (three of my desert samples clipped to pure white).
- PROP GROUNDING — one AO decal per prop, no exceptions: radial gradient projected on the terrain, radius 0.65 * footprint radius, opacity 0.55 at center falling as pow(1-r, 1.8), tinted as a MULTIPLY of the receiving albedo not a black blob, plus a contact darkening lobe offset 0.30m along the sun's ground vector. Then orient cliff vegetation to the surface normal and scale it by world size, not screen size.
- WATER AND UNITS — replace the screen-space worley with 4 Gerstner waves at 18m/7m/3m/1.2m, amplitudes 0.22/0.09/0.035/0.012, directions spread ±35° about one prevailing swell, and fade the two shortest by the same footprint term. Target deep-water HF_rms 9-13 (it is 0.56) and recolour deep ocean from #52597A to #123A63. For units: widen shoulders to >= 1.8x head width, break the bounding box with a spear or shield, and move the strength badge to a (-14,-16)px offset capped at 22px diameter so it stops eating the unit it labels.

## critique 2 — 34/100
REJECT. Gate FAIL (3 failures), and five separate automatic fails fire on top of it.

MEASURED, cited:
`node tools/metrics.mjs shots/grid-p6r1.png 60,90,240,160:far-cliff 380,380,240,160:mid 700,700,240,160:near-field 1150,200,240,160:water` →
far-cliff HF 15.54 / MID 19.96 / MID_over_HF 1.28, sat 0.363, mean 97.5
mid HF 18.36 / MID 17.47 / 0.95, sat 0.308, mean 86.6
near-field HF 12.22 / MID 12.97 / 1.06, sat 0.340, mean 83.4
water HF 14.28 / MID 10.97 / **0.77** (floor 0.9), mean 90.8
crushedPct **0.42** (cap 0.30), blownPct 0.01, nearFarHFRamp **0.79** (floor 1.6). gate FAIL.
Second, independent region set confirms it is not sampling luck: 935,600,60,45:disc-flat MID/HF 0.79; 480,690,120,90:nearground HF 24.31 (>22, confetti) and sat 0.482 (>0.46); ramp 1.37.

GRID VISIBILITY. Not absent — unreliable, which for a 4X is the same product failure. At 3-4x it is a legible thin dark seam on flat mid-ground (x60-420,y400-640) and around the tree cluster at 150,430. It is gone in the near field: over the 280x260 patch at (420,620) — roughly the tiles a player actually clicks — and gone around the selected unit. `src/render/grid.js:477` is why: `fade = 1 - sstep(2.4, 5.2, relief)`, times 0.78 for mountain/snow (:479), times 0.85 for water (:483), then in the shader times the distance ramp, times the grazing ramp `smoothstep(0.07,0.30,toCam.y/dist)` (:85), times `att` 0.80-1.0 (:164), times `cur` 0.84-1.0 (:194), times `mix(1.20,0.86,lit)` (:270). Six attenuators in series. Even at 0.85 apiece the product is 0.38, and on any tile with relief > 3 the first term alone is already ~0.5. The shader header claims "a hex the player cannot see is a hex they cannot click" and then ships exactly that.

AERIAL PERSPECTIVE DIRECTION. The bible says farther is LIGHTER, COOLER, LESS saturated, never darker. Grass-only, material-controlled, by y-band: FAR (y70-170) L=108.1 sat 0.255 B-R -5.6; mid (440-540) L=103.9 sat 0.443; mid-near (560-660) L=116.0 sat 0.497; NEAR (810-898) L=68.0 sat 0.505. Cooler ✓, less saturated ✓, but the value lift is absent — far grass is 7% DARKER than the mid-near band. Ocean is worse and is inverted at the source: vertical strip x1100-1200 runs L=76.1 at y70 → 151.9 at y550. Scrim-corrected that is still 108 far vs 164 near, ratio 0.68 — the sea gets darker as it recedes. Far forest (430,150,140,110) has drifted to hue 158.5° at sat 0.194 against a #3C5E31 spec of hue ~100° sat 0.32-0.44: the haze is pushing distance toward cyan, the exact bug post.js:431 claims it fixed.

CRUSHED SHADOWS. The world is clean — world luma p0.1 = 14.3, p1 = 23.7, zero pixels under 4. The 0.42% is UI chrome: 3991 of 6044 crushed pixels sit inside the panels, and the world's remainder is almost entirely row y=53, a full-width 1524-pixel #000 separator hairline, plus rows 74/129/184/240 (303 px each, notification card rules) and columns 22/393/1292/1577 (97-409 px each, panel borders). A gate failure bought with pure-black UI strokes, not with lighting.

VIGNETTE HOTSPOT. post.js:575 asserts "NO RADIAL VIGNETTE... a hotspot wearing a lighting model". True of post — and then `src/ui/hud.css:87-88` puts one back in CSS: `.scrim.t` 150px of rgba(5,7,10,.88) and `.scrim.b` 250px of rgba(5,7,10,.76) laid straight over the board. Two matched-material tan strips 700px apart show the identical profile: x1040-1260 L=107.0 at y790 → 47.2 at y890; x420-560 L=103.3 at y780 → 46.4 at y890. Same material, same lighting, 56% of the exposure gone, twice. The 70px-blurred luminance field peaks at 149 around (1130,450) and falls to 57-77 along the top and 47-70 along the bottom — a 3:1 centre-to-edge ratio, heavier than most film emulations. It also manufactures the ramp failure: HF_rms is absolute, so halving the near-field luminance halves its measured HF. Matched tan gives 1040,770,200,100:near-tan HF 14.92 against 300,470,200,100:far-tan HF 21.23, ramp 0.70; back out the scrim and the true material ramp is ~1.17 — still short of 1.6, so the mip is genuinely broken too, but the scrim is the dominant term.

PALETTE. Mountain (70,95,220,150) sat 0.369 hue 10.9°, second sample (150,180) sat 0.354 hue 4.0°, against #7A7368 / sat 0.08-0.18. Two to four-and-a-half times the allowed chroma and 25-30° too red, on the largest single mass in the frame. Shadow hue drift, top vs bottom quintile of each region: grass 28.4°, desert 23.6°, mountain 31.8°, near tan 11.4° — the cap is 10°, and grass shadow saturation collapses from 0.371 lit to 0.070, so shaded grass is grey mud. The old navy-shadow bug has been overcorrected into a warm-red one.

AUTOMATIC FAILS: (1) per-pixel noise for material — water MID/HF 0.77, and at 3x the sea from (1250,330) is a uniform field of hard-edged ~4px shards at constant screen size from shore to horizon with no swell, no crests, no octave hierarchy, terminating in a flat matte navy panel; (2) detail does not shrink with distance — 0.79 headline, 0.70 matched-material, ~1.17 scrim-corrected; (3) toy silhouette — the selected unit at (975,680) is a 26px black number disc over an unreadable 10px khaki smear, and the HUD portrait is a featureless helmet oval on a flat blue torso; (4) muddy/garish colour — grass shadow at sat 0.070 and a sat 0.37 red-orange massif; (5) streak artifacts — parallel diagonal hatching down every steep cliff face in (60,90)-(300,250), textbook UV stretch on foreshortened geometry. Plus props on bright cream octagonal pads (196,505)-(233,550) that are brighter than the ground — a placement decal where a contact shadow should be — and Calyx's buildings as untextured flat-matte solids with a facet-banded dome.

WHAT IS GOOD, and it is not nothing: the HUD is genuinely shippable — clean type, real hierarchy, zero clipping or overlap anywhere I could find. The composition is directed rather than found. The coastline foam reads. World shadow toe is properly lifted with zero crushed pixels. Somebody competent framed this shot.

UNLABELED, SIDE BY SIDE WITH A REAL CIV VI SCREENSHOT: the player picks Civ VI, immediately, and not on a close call.

THE SINGLE TELL: the selected unit. In Civ VI your eye lands on the unit you are about to move and gets everything at once — you can name it as a Warrior from its silhouette across the room, and the hex it stands on is outlined unmistakably. Here the eye lands on the same spot and finds a black number token floating over a smear, on a tile with no visible boundary, in the murkiest and softest part of the frame — because that part of the frame is under 0.76 alpha of black and carries less high-frequency detail than mountains forty units further away. The image gets sharper and brighter the further it is from you. Nothing else in the shot gives it away that fast.

**Problems**
- CSS HUD scrims wash the board: hud.css:87-88, .scrim.t 150px rgba(5,7,10,.88) and .scrim.b 250px rgba(5,7,10,.76). Two matched tan strips 700px apart both lose 56% of exposure (L 107->47 at x1040-1260, L 103->46 at x420-560). It manufactures the ramp failure and inverts aerial perspective in the delivered image, while post.js:575 asserts there is no vignette.
- Detail does not shrink with distance. nearFarHFRamp 0.79 against a 1.6 floor; 0.70 on matched tan material (near-tan HF 14.92 vs far-tan 21.23 at near-identical mean luminance); ~1.17 even after backing the scrim out. The farther patch carries 42% MORE pixel detail than the nearer one.
- Ocean is per-pixel confetti, not material. water MID/HF 0.77 against a 0.9 floor. At 3x from (1250,330) it is a uniform field of hard-edged ~4px polygonal shards at constant screen size from shore to horizon, with no swell, no chop-on-swell, no long crests, ending at a hard boundary into a flat matte navy panel.
- The selected unit is unreadable. At (975,680) it is a 26px black number disc over a ~10px khaki smear; a player cannot name the unit type from shape, which is bible non-negotiable #4. The HUD portrait repeats it: featureless helmet oval, flat blue blocked torso, no material.
- Shadow hue drift 24-32 degrees against a 10-degree cap (lit vs shadow quintiles): grass 28.4, desert 23.6, mountain 31.8, near tan 11.4. Grass shadow saturation collapses from 0.371 lit to 0.070, so shaded grass reads as grey mud. The old navy-shadow bug has been overcorrected into a warm-red one.
- Mountains are 2.0-4.6x over the allowed chroma. (70,95,220,150) sat 0.369 hue 10.9deg; (150,180,140,90) sat 0.354 hue 4.0deg; spec #7A7368 is sat 0.08-0.18 at hue ~34deg. The largest mass in the frame is a saturated red-orange, and it is also the most saturated thing in the shot, which is backwards for the farthest thing in the shot.
- Hex grid fails exactly where it matters. Legible on flat mid-ground, gone in the near field over the whole (420,620)-(700,880) patch and gone around the selected unit. grid.js multiplies six independent attenuators in series (relief fade :477, rough 0.78 :479, water 0.85 :483, distance and grazing ramps :85, att :164, cur :194, lit :270); at 0.85 apiece the product is 0.38 before the terrain noise at HF_rms 12-24 swallows what is left.
- Grounding and surface finish: boulders at (196,505)-(233,550) sit on hard-edged cream octagonal pads BRIGHTER than the grass around them, a placement decal where a contact shadow belongs; Calyx's buildings are untextured flat-matte tan solids with a facet-banded dome; every steep cliff face in (60,90)-(300,250) carries parallel diagonal UV-stretch hatching.

**Fixes**
- Cut the scrims. hud.css:87-88 -> .scrim.t { height: 70px; background: linear-gradient(180deg, rgba(5,7,10,.42), transparent); } and .scrim.b { height: 120px; background: linear-gradient(0deg, rgba(5,7,10,.34), transparent); } so each dies at its own panel edge instead of running 150px into the mountains and 250px into the near field. Expected: near-tan HF 14.92 -> ~24, headline ramp 0.79 -> ~1.17, and the far-vs-mid value inversion on land disappears without touching a shader.
- Lift every UI stroke off pure black: #000 -> #14100C (luma 17). Targets are row y=53 (1524 px, full-width separator), rows 74/129/184/240 (303 px each, notification card rules), and columns 22/393/1292/1577 (97-409 px each, panel borders). crushedPct 0.42 -> ~0.14, under the 0.30 cap, with zero change to lighting. 66% of the current crush is UI chrome; the world is already clean at p1 = 23.7.
- Recentre the mip window. post.js:709 uFootA 1.25 / uFootB 2.50 against uCamY. At the hero notch (input.js zoom 0.300 -> dist 28.5, pitch 55deg, camY 23.4) flat ground has n.v = 0.82, so d/nv runs ~24 at the bottom of frame to ~85 at the top against a 29.2-58.4 window: mip is pinned at 0 across the whole near third and clamps to 1.0 past mid-frame. Set uFootA 0.90, uFootB 3.20 (window 21-75) so mip runs ~0.1 near to ~0.9 far and the mix(1.50,-1.00,mip) detail grade at :855 finally has range to express. Raise uBlur 0.20 -> 0.34 so the far low-pass actually removes the pixel band it exists for.
- Give the sea a wave hierarchy. Two lower octaves under the existing detail: swell at 14 world-unit wavelength, amplitude 0.10; chop at 4 units, amplitude 0.035; both scrolled anisotropically along one wind vector at 3:1 aspect so crests are long instead of isotropic shards. Mip the detail normal by world-space footprint and hard-cut any contribution under 2.5 px of footprint. Target water HF_rms 9-12 and MID/HF 1.05-1.15, from 14.28 / 0.77 today.
- Desaturate the massif to spec. Multiply rock albedo chroma by 0.55 and rotate hue +22deg (measured 4.0-10.9deg, spec #7A7368 sits near 34deg). Hold value: measured 0.432-0.492 is already inside the 0.32-0.62 band, so this is a pure chroma correction. Target sat 0.14-0.17 from 0.354-0.369.
- Fix the ambient tint so shadows keep their hue. Weight ambient by albedo instead of tinting it independently: ambient = skyColor * mix(vec3(1.0), albedo, 0.75) * ao, and clamp the sky-tint chroma at 0.12. Acceptance: lit-to-shadow hue shift <= 8deg on every biome (today 28.4 grass / 23.6 desert / 31.8 mountain) and shadow saturation >= 0.55x lit saturation (today grass is 0.070 against 0.371, a ratio of 0.19).
- Stop the grid attenuator stack from multiplying. In grid.js take the MIN of the six fade terms rather than their product, and widen the relief window at :477 from sstep(2.4, 5.2, relief) to sstep(4.0, 7.5, relief) so only a genuine cliff loses the lattice. Drop the rough 0.78 multiplier at :479 entirely — mountains are where a player most needs to count tiles.
- Make the seam beat the terrain it sits on, measurably. Terrain HF_rms is 12-24 in display units; a stroke is only legible at a post-tonemap delta of about 2x local HF. Sample the 8px-blurred luminance already available in the detail pass and drive the seam multiplier so the delta lands at max(26, 2.0 * localHF) display levels. On grass at HF 17.2 that is a 34-level drop; today the seam composites to roughly 20 levels on flat ground and about 8 on any tile with relief > 3.
- Put a readable unit under the badge. Shrink the count disc to 0.55 scale (26px -> 14px) and move it to the model's lower-right. Give the Warrior a real silhouette at 42px tall at this zoom — three-figure group, shield disc plus spear diagonal — held to >= 40 luma of contrast against the ground beneath it. Add the contact shadow it currently lacks: an elliptical AO decal 1.4x the base width, 0.55 opacity at centre, multiplied not composited.
- Ground the props and unstretch the cliffs. Delete the cream octagonal pads under the boulders at (196,505)-(233,550) and replace each with a multiplied AO decal at 1.25x base radius, 0.42 centre opacity, smoothstep to zero over the outer 35%, so the ground texture runs through it instead of being covered by a plate brighter than the grass. For the diagonal hatching on every steep face in (60,90)-(300,250), triplanar-blend the rock material with a blend exponent of 6 at a world scale of 1/2.5 units and clamp the up-projection weight to zero past 55deg of slope.

## critique 3 — 31/100
Rejected. Gate is FAIL on three of your own bible's targets (water MID/HF 0.76 < 0.9; crushedPct 0.42 > 0.30; near/far HF ramp 0.77 < 1.6), and the shot trips five of the listed automatic fails on top of that.

Unlabeled, side by side with a real Civ VI screenshot, the player picks Civ VI, and it is not close. The single tell is the units. In Civ VI a player names every unit on the board from silhouette alone at gameplay zoom. In AEON I cannot find the units without the HUD telling me one is selected. I measured the selected Warrior at (862,600,44,46): mean luma 113.4. The ground immediately beside it at (930,600,44,46): mean luma 112.5. That is 0.9 L of separation — the unit is photometrically invisible against its own tile. At 12x it resolves as a navy/rust/grey smear with roughly nine competing shapes crammed into 44 px, reading as battlefield debris, not a soldier. The second unit at the foot of Solmere reads identically, so this is the unit pipeline, not one bad instance. And at (975,681) there is a world-space "1" stack badge sitting on bare grass with no unit under it at all — the badge outlived its mesh.

The portrait is the second knife. The Warrior portrait is vector programmer-art: one ellipse helmet, one linear airbrush gradient, a pure-black visor slit, a rounded-rect torso, zero material break between metal, cloth and leather. Then the top bar shows three leader portraits at (1100-1250, 10-45) that are the SAME head asset in three background tints — identical silhouette, identical shading, identical helmet. Three civilizations, one face. Civ VI ships fully-rendered characters with distinct headdress silhouettes readable at 30 px.

What is genuinely working, and I will say so: the coastline foam and shore transition, the HUD layout and typography, and city size variety. Calyx (pop 1) is a hut cluster with a rotunda, Solmere (5) is mud-brick terraces, Aurelia (7) is a walled keep with red tile — three legibly different settlement tiers. That is the one focus area of this pass that is actually shipped. Keep it.

Everything else in the focus list fails. Grounding: the sheep props at (587-620, 572-592) are untextured flat-matte white capsules sitting on brown dirt with no contact decal, and the fence around them is orange line-art. Material: at 8x, the desert at (240,560,160,110) is a uniform Nyquist-scale pebble dither — sd 48.96, min 15, max 245 inside one 160x110 window — with not one hex stroke visible anywhere in it, which is roughly a tile and a half of open ground. Your non-negotiable #1 is broken outright; the only "hex edges" in the frame are terrain-type value seams and slope facets. And the far cliff at (60,90) carries HF_rms 15.93 against the near field's 12.26 — the most distant surface on screen is the noisiest, which is a missing mip bias, not a style. Finally the XP chip at x≈396,y≈793 clips the "P" against its own border, and the veterancy "1" badge is clipped by the portrait frame's bottom-right corner.

The map sells at thumbnail scale. It collapses the moment anyone looks at a unit — which in a 4X is every single turn.

**Problems**
- Unit silhouette is photometrically invisible: selected Warrior at (862,600,44,46) means L=113.4 against adjacent ground at (930,600,44,46) L=112.5 — 0.9 L of separation, and at 12x it is an unnameable navy/rust smear with ~9 competing shapes inside 44 px. Second unit at Solmere reads the same, so it is systemic.
- Portrait is vector programmer-art — one ellipse helmet, one linear airbrush gradient, pure-black visor slit, no metal/cloth/leather material break — and the three top-bar leader portraits at (1100-1250,10-45) are the identical head asset in three background tints.
- Hex grid absent on open terrain: an 8x zoom of (240,560,160,110), roughly 1.5 tiles wide, contains zero grid stroke. Only terrain-type seams and slope facets read as tile edges. Non-negotiable #1 broken.
- Per-pixel noise standing in for material: desert (200,500,180,110) sd 48.96 with min 15 and max 245 at pixel scale — a full-range dither, not world-space material. Grass (470,440,150,110) sd 37.9 same story.
- Detail does not shrink with distance: far-cliff (60,90) HF_rms 15.93 vs near-field (700,700) HF_rms 12.26, ramp 0.77 against a required 1.6. The rock stroke layer is sampled at constant screen frequency with no mip bias, so the farthest surface is the busiest.
- Gate FAIL on the bible's own targets: water (1150,200) MID/HF 0.76 < 0.9 (noise dominates structure, no directional swell) and crushedPct 0.42 > 0.30 limit.
- Props are ungrounded: sheep at (587-620,572-592) are flat-matte untextured white capsules with no contact decal, and the tree and crate props beside them have no ground occlusion either.
- UI is clipped in three places: XP chip clips its trailing 'P' against the chip border at x≈396,y≈793; the veterancy '1' badge is clipped by the portrait frame's bottom-right corner; and an orphan world-space '1' stack badge renders over empty grass at (975,681) with no unit beneath it.

**Fixes**
- Force unit-vs-ground value separation to >= 25 L (currently 0.9). Bake a hard 3-value ramp into every unit: head/helmet L 165-180, torso L 105-120, legs/base L 55-70. Add a unit-only rim pass at 0.6 intensity, 5600K, from the sun azimuth, with a 1.5 px world-space wrap. That is the exact trick Civ VI uses to keep units off the terrain.
- Cut unit silhouette complexity to 3 features max at gameplay zoom. The unit projects to ~44x46 px; cull any sub-feature under 6 px projected size and move it to the portrait. Then scale the model to 0.55-0.65 x hex inradius in height (yours reads ~0.35) and drop tile prop density near units so the figure owns its tile.
- Ground every unit with a two-part decal: a soft AO ellipse at radius 0.55 x hex inradius, alpha 0.55 at centre falling to 0 at the rim, multiply blend, plus a hard contact core 1.5 px wide at alpha 0.8. Tint it terrain_albedo x 0.55 — never neutral grey, never blue.
- Instance the same contact decal on every prop: footprint ellipse x 1.3, alpha 0.45, 2 px penumbra, colour terrain_albedo x 0.55. One instanced quad per prop covers the sheep at (587-620,572-592), the crates, and the trees.
- Repaint the unit portrait with 4 tonal steps and a real spec lobe: bronze base #8A7248, crown highlight #D9C48E at 20-25% coverage, shadow #3E3423, cool bounce #4A5568 at 8% on the shade side. Kill the pure-black visor — use #1C1A16 with a 1 px warm reflected edge. Go 3/4 view, and give metal a tight 6-10 px highlight while cloth stays 0 spec.
- Author 3 distinct leader heads, not 3 tints of one. Differentiate at the silhouette level (helmet dome vs hair mass vs headdress profile) so they separate at the 30x30 px they actually render at in the top bar.
- Fix the material frequency: move the pebble layer into a tiling albedo at 2 m world period with a full mip chain, cap negative mip bias at 0.5, and clamp its contrast to +/-18 L (currently +/-115 L). Replace pixel dither with 3 discrete macro-shapes per 4 m2, each >= 6 px on screen. Then raise MID by adding 30-60 px value shapes (drainage, ledges, grass patches) rather than just cutting HF — the bible wants MID/HF 0.9-1.3, not low HF.
- Restore the distance ramp with mip bias = clamp(log2(dist/dist_ref), 0, 4) and fade the detail-normal weight to 0 across 40-90 world units. Apply aerial perspective BEFORE the detail layer so haze actually kills far contrast. Target far-cliff HF_rms <= 9 with near-field at 15-18, giving a ramp of ~1.7-2.0 against the required 1.6.
- Draw the hex grid you promised: one stroke per edge, world-space width 0.06 m (~2.2 px at this zoom), analytic AA, conforming to slope, drawn as a darken-by-12-L multiply over land and fading to 0 over open water past the shelf. It must survive the (240,560) desert crop, where right now there is nothing.
- Lift the toe and unclip the UI: raise the tonemap black point from 0 to L 0.055 or add 0.12 albedo-weighted ground bounce so no land region floors below L 22 (currently 13-19) and crushedPct drops under 0.30. Give stat chips min-width = measured text + 12 px padding per side to stop the XP clip at x≈396, inset the veterancy badge 3 px inside the portrait frame, and bind world stack-badge visibility to the unit instance's visible flag to kill the orphan at (975,681).

## critique 4 — 34/100
REJECT. Gate FAIL, and the water pass fails on its own terms before taste enters the room.

First, the thing nobody on this team has noticed: **your water gate box is measuring the UI.** The prescribed probe `1150,200,240,160:water` overlaps the notification rail (rail occupies x>=1265, y 70-330). It returns HF_rms 13.96 — inside the 7-15 target — because it is measuring the bevels and 11px type of the COMBAT REPORT and DISPATCH panels. Move the box onto actual sea and the number collapses: `1000,110,220,140` -> HF_rms **1.01**, MID/HF **3.76**. `940,250,200,130` -> HF_rms **5.70**. `1080,380,220,140` (the shelf, the busiest water in frame) -> HF_rms **6.70**, still under the floor. Banded down the open ocean: HF_rms **0.52** at y=60, **0.39** at y=150, **1.79** at y=240, **6.32** at y=430, **8.41** at y=540. Every honest water probe fails. Roughly a fifth of the frame is an untextured flat-matte gradient — automatic fail, on the nose. At 6x, `1010,60,180,120` is literally a smooth vertical ramp with nothing on it. Not stylised. Empty.

Second: **there is not one specular glint in your ocean.** Zero. Open-ocean pixels above p50+60 sit at 1.11%, and inspecting the crop, that fraction is coastline bleed, not glitter. You have a 5600K key at golden hour raking across a water plane and the plane returns pure Lambert. Water's entire visual identity is the sun path. You shipped a sea with the sun turned off.

Third: **the waves are not waves.** At 6x on `1040,300,180,120` the shelf is a single-octave quilted cell pattern — one wavelength, one direction, constant amplitude, edge to edge, cutting the shoreline at an arbitrary angle instead of refracting parallel to it. It reads as hammered vinyl or bubble wrap. Adjacent to it, a hexagonal brightness step is plainly visible in open sea around x1100-1180 / y330-400: **the water is shaded per tile.** You have a quilt on the ocean. That is grid bleed-through in its worst form, because it is not a stroke you can fade — it is the lighting itself.

Fourth: **rivers are decals, and the grid is drawn straight across them.** At 8x on `550,450,140,100`, the hex edge continues over the water surface at approximately (600,505) and (655,545) at full land weight. The ribbon has no incision, no bank, no bed, no flow, constant width, a hard alpha edge on the upslope side and a soft one on the downslope side. The terrain runs continuously underneath it. Civ VI puts rivers on hex edges with modelled banks; you painted a blue stripe over a hill and let the tile border cut it in half.

Fifth: **caustics do not exist.** The mint stipple at `1120,520` is a two-tone camouflage blob at constant scale. There is no seabed visible anywhere. Water at 0.5m and water at 4m are the same pixels. And the shore has a depth-sort bug — at (955-1030, 315-335) the teal water plane blends over the *top* of the cliff; brown rock reads through the water above the waterline.

Palette has drifted violet across the board. Deep ocean samples **#505676** (hue 229, sat 0.32) against a bible spec of #123A63 (hue 211, sat 0.82). Coast **#6187aa** (hue 209, sat 0.43) against #2E7C93 (hue 193, sat 0.80). Estuary **#464463** (hue 244) — that is the navy-shadow-on-warm-key failure your own bible bans, applied to a whole bay. Foam samples **#b7a9b2**, hue **318** — magenta foam, under a warm key. And aerial perspective is inverted on water specifically: mean luma **71.8** at y=60 (far) vs **166.3** at y=540 (near), so the far sea is 94 levels *darker*, and sat 0.322 far vs 0.241 near, so it is *more* saturated with distance. Both halves of the rule, backwards. You drove value from depth and never layered fog on top.

Global gate: crushedPct **0.42** (limit 0.30 — driven by the far water's black end, min luma 42.2 open sea, 17.5 in the river), near/far HF ramp **0.75** (needs >=1.6; far-cliff 16.43 vs near-field 12.35 — detail *grows* with distance). The mid-distance jungle at 8x is 1-2px confetti that has not been mipped. Three separate automatic fails before I even score the water.

Credit where due, because you should not re-roll these: the HUD is genuinely shippable — top bar, notification rail, unit panel, END TURN, minimap, no clipping, no overlap anywhere in frame. City banners read. The coastline silhouette at 1:1 has a real cartographic shape. The land material at mid-distance has honest world-space grain. That is the bible's "already good" list and it is holding.

**Unlabeled, side by side with a real Civ VI screenshot, which does a player pick?** Civ VI. Instantly, and not by a small margin — this is not a close call that turns on style preference.

**The single tell:** the ocean has no sun on it. Cover everything but the top-right quadrant of both images and Civ VI still shows a moving swell with a specular path and inked, animated water; AEON shows a flat lavender gradient measuring HF_rms 0.39. A player will not say "the specular is missing" — they will say the sea looks like construction paper. No shipped 4X has ever had an ocean with zero glint, so the eye reads it as a placeholder before the conscious mind finds a reason. Everything else on this list is fixable polish. That one reads as unfinished.

**Problems**
- The water gate box `1150,200,240,160` overlaps the notification rail (x>=1265) — it has been measuring UI bevels and type, not sea. Honest probes: HF_rms 1.01 / 5.70 / 6.70. Every water PASS in prior passes was fraudulent.
- Deep ocean is an untextured flat-matte gradient: HF_rms 0.52 (y=60), 0.39 (y=150), 1.79 (y=240). ~20% of the frame carries no material at all. Automatic fail.
- Zero specular anywhere in the water. Open-ocean pixels above p50+60 = 1.11%, and that fraction is coastline bleed. A golden-hour sea returning pure Lambert.
- Water is shaded PER HEX TILE — a hexagonal brightness step is visible in open sea at x1100-1180, y330-400. A quilt on the ocean; grid bleed-through you cannot fade away because it is the lighting.
- Rivers are flat painted decals with the hex grid stroke running across them at full land weight (approx. 600,505 and 655,545). No incision, no banks, no bed, no flow, constant width, asymmetric alpha edge.
- Aerial perspective inverted on water: far mean luma 71.8 vs near 166.3 (far is 94 levels DARKER), far sat 0.322 vs near 0.241 (far is MORE saturated). Both halves of the bible rule, backwards.
- Palette drift 16-20 degrees violet at roughly half spec saturation: ocean #505676 (h229 s0.32) vs #123A63 (h211 s0.82); coast #6187aa (h209 s0.43) vs #2E7C93 (h193 s0.80); estuary #464463 (h244); foam #b7a9b2 at hue 318 — magenta foam under a 5600K key.
- No caustics and no seabed: 0.5m and 4m water are identical pixels. Plus a shore depth-sort error at (955-1030, 315-335) where the water plane blends over the cliff top and rock reads through above the waterline.

**Fixes**
- Re-baseline the gate first. Retire `1150,200,240,160:water` — it sits on the notification rail. Canonical water probes become `1000,110,220,140:water-deep` and `1080,380,220,140:water-shelf`, plus a distance triplet at y=60/240/540. Re-run every historical water claim against these before touching a shader.
- Ocean surface: sum 3 Gerstner bands — wavelengths 22.0 / 9.0 / 3.5 m, amplitudes 0.42 / 0.16 / 0.05 m, steepness Q 0.55 / 0.35 / 0.20, headings spread +/-35 deg about a single swell direction (118 deg reads well against this coast), phase speed sqrt(g*L/2pi). Tessellate the water plane to a 1.0 m edge clamp inside 80 m. Target HF_rms 9-12 at y=430.
- Detail normals: two world-space-UV scrolling layers at 3.0 m and 0.75 m tiling, scroll 0.045 and 0.11 m/s on divergent headings, derivative-based LOD (no manual bias). Weight the 0.75 m layer to 0 past 70 m but hold the 3.0 m layer to keep far ocean at HF_rms 7-9 instead of 0.39 — flat far water is as wrong as fizzing far water.
- Specular: GGX, roughness 0.055 at crest lerping to 0.14 in trough (drive off Gerstner Jacobian), F0 = 0.020, sun angular diameter 4-6 deg so you get an elongated glitter path rather than a dot. Clamp the specular term to 3.5x local diffuse luma so it survives the tonemap. Target 0.8-1.5% of open-ocean pixels above p50+70.
- Fix aerial perspective on water with the same fog the land uses: lerp toward horizon colour 0.55-0.65 at the top of frame, and hard-clamp far-ocean luma to >= 0.85x near-ocean luma. Target far-ocean mean 175-195 with sat 0.18-0.24. This alone drags crushedPct from 0.42 to under 0.30, since the crushing lives in the far sea's black end (open-sea min luma 42.2).
- Kill the violet drift at the source — stop tinting water albedo and let absorption do it. Per-metre extinction sigma_R 0.42, sigma_G 0.075, sigma_B 0.022, applied over the depth field. That lands hue naturally in the 193-211 band the bible specifies instead of the current uniform violet ambient wash. Re-check that the sky IBL is not multiplying a violet term into the water's ambient.
- Foam: clamp albedo to 0.92 and light it with the terrain's key/fill so it stops coming out magenta (currently #b7a9b2, hue 318). Place it from two sources, not a painted band — (a) shoreline distance field, full at 0-1.2 m from the waterline falling to 0 at 4.0 m; (b) Gerstner Jacobian, foam where J < 0.35. Add a 0.6 s decay trail so it advects instead of floating at an angle across open water like it does now.
- Shallow water: render the seabed albedo, refract the view ray at IOR 1.333 before sampling it, then modulate by a caustic term pow(saturate(1 - abs(div(N))), 3.5) projected on the bed at 1.8 m cell scale, two layers scrolling 0.03 and 0.05 m/s, faded to zero past 4.5 m depth. The player must be able to see sand through the shelf; right now 0.5 m and 4 m are indistinguishable.
- Rivers: (a) carve the channel — lower the river spline's terrain 0.8-1.4 m with a 2.5 m bank falloff so the water sits in geometry and the banks self-shadow; (b) mask the hex grid stroke to zero inside the river distance field (< 1.5 m from centreline, 0.5 m feather), reusing the open-water fade path; (c) add flow — advect the detail normals along the spline tangent at 0.25-0.4 m/s and brighten riffles where the bed gradient exceeds 0.08.
- Merge all water tiles into one continuous mesh with continuous UVs, and move the depth/colour term to a smoothed depth field texture rather than a per-tile constant — that removes the hexagonal shading quilt at x1100-1180. Then fix the shore: clamp the soft-intersection fade to 0.35 m and re-enable depth test on the water pass to stop the plane blending over the cliff top at (955-1030, 315-335). Separately, write velocity for the animated water surface into the velocity buffer so TAA reprojects it — the two diagonal comet trails at (860,610)-(940,680) are smear, not wake.

## critique 5 — 38/100
REJECT. Gate is FAIL on the tool's own numbers (water MID/HF 0.72 < 0.9), and the frame trips five separate automatic fails on top of that. Metrics as run: far-cliff HF 12.1 / MID-HF 1.14 / sat 0.298; mid 19.52 / 0.96 / 0.329; near-field 21.66 / 0.92 / 0.380; water 13.25 / 0.72 / 0.418; nearFarHFRamp 1.79; crushed 0.17; blown 0.01. So the ramp, the crush and the land sat bands are fine — and everything the ramp cannot see is broken. Land MID/HF sits at 0.92-0.96, the floor of the band, which is the metric politely saying the material is HF noise with just enough low-frequency shading smeared over it to sneak past.

Focus pass, unit craft, item by item.

SILHOUETTE — automatic fail. The selected Warrior measures 8 x 13 px inside a 28 px selection ring on a ~130 px hex. That is 10% of tile width. At 10x it is a grey mannequin smear: no weapon, no shield, no helmet crest, no team colour on the model itself. The only reason a player knows it is a Warrior is the text panel. Non-negotiable #4 says "a player names the unit type from shape alone at gameplay zoom" — nobody names this. The tragedy is src/render/units.js is 4060 lines that synthesise lamellar scale armour with 7 scales across the cuirass (line 457) and a domed shield carcass (line 650); none of it survives to the framebuffer because the thing is drawn 13 px tall.

MATERIAL — automatic fail, twice. (a) The canopy is per-pixel confetti: 1-2 px lime specks running to #B4D42A over crushed near-black gaps, near-forest measuring hue 74 / sat 0.44 against the bible's forest #3C5E31 (hue 100). It reads as lichen sprayed on a heightfield, not trees. (b) Simultaneously, the built surfaces are untextured flat matte — Aurelia's keep tower faces, the brown cone hut at (985,625), the distant water polygon at (250,140) are single-value gradients with one step per face. The build has managed both failure modes at once: noise where it wants material, and nothing where it needs it.

GROUNDING — automatic fail. The 5-6 px lime trees on the far cliff at (172,124), (245,158), (272,155) float with no AO decal. The pale rocks on the plains have none. The selected unit has none — the dark disc under it is the selection ring's own vignette. units.js:234 bakes "a ground-contact gradient into vertex colour"; it is clearly not reaching scattered props.

STREAK — automatic fail. The pale wedge at (890-990, 655-720) is a flat #A0C1D6 triangle with a razor un-antialiased diagonal edge, a blown near-white core and no shoreline blend. It is a river drawn as a decal and it looks like a rendering bug.

COLOUR — automatic fail. The farm at (860-950, 610-670) measures sat 0.569 / value 0.712 against the plains spec of sat 0.28-0.38 / value 0.35-0.58. The single loudest object in a golden-hour frame is a background tile improvement, and it reads as a neon barcode. Separately the shadow-hue rule is broken everywhere: lit sand hue 32.8, shadowed sand hue 351-6.8 — a 27-42 degree rotation into mauve, when the bible allows 10. Not navy on tan, but the same crime rotated.

AERIAL PERSPECTIVE — wrong direction. Band means: FAR value 0.460 / sat 0.258 / vector hue 6.0; NEAR value 0.399 / sat 0.377 / vector hue 39.6. Lighter and less saturated, correct. But hue 6 degrees is magenta-red — the depth cue tints WARMER with distance, the exact opposite of "COOLER". Distant forest at (420,180) has gone pink at hue 340.5. The fog colour is simply the wrong colour.

HEX GRID — borderline fail. Legible on open sand, gone inside canopy, dashed and broken across the far cliff. A luma scan along open plains at y=790 finds 20 dark dips of 45+ luma across 500 px: the ground noise generates more false edges than the lattice generates real ones. The shader work is honest (1.5 px core, exact-gradient width, one stroke per edge) and it is being defeated by the terrain it is drawn on.

PORTRAITS — fail. Three leader busts in the top bar are the SAME beige profile head, identical geometry and pose, distinguished only by a background swatch (maroon / olive / teal) and a colour bar. The Warrior portrait is a featureless egg helmet with a black visor slot, no chin, no hands, no weapon, and a hard vertical seam down the blue tabard.

CITY VARIETY — thin. Aurelia (pop 7) is genuinely decent massing: red tile roofs, tan keep, crenellations, wall ring, harbour. Solmera (pop 5) two tiles away is a tan lump of quads with no keep, no banner, no landmark. The population ramp changes fidelity, not vocabulary, so a pop-5 city reads as a rockslide rather than a small town.

WHAT IS ACTUALLY GOOD, and it is not nothing: the HUD ships. The unit panel, notification rail, top bar and end-turn block are cleanly kerned, correctly grouped, no clipping, no overlap, panels stop 20 px clear of the frame edge. The coastline foam ribbon and shallow-to-deep gradient read. Aurelia's keep would survive a screenshot crop. If the world were as well-made as the chrome this would be a different review.

THE ANSWER YOU ASKED FOR: unlabeled, side by side with a real Civ VI screenshot, the player picks Civ VI in under a second, and it is not close. The single tell is the units. Civ VI's contract is that you can name every unit on the board from its silhouette at default zoom — spearman, scout, warrior, settler, all readable as posed characters with weapons at roughly 40% of tile height. This build's only visible unit is a nine-pixel grey smudge inside a gold ring, and its three national leaders are one head with three background swatches. A player does not need to know what a MID/HF ratio is to feel that difference; they feel that nobody is home on the board.

**Problems**
- Selected Warrior renders 8x13 px inside a 28 px ring on a ~130 px hex — 10% of tile width, no weapon, no team colour, unnameable from shape. Non-negotiable #4 broken, and 4060 lines of armour-detail shader in units.js never reach the screen.
- Gate FAIL: water MID/HF 0.72 vs 0.9-1.3 required. The deep ocean carries a ~2 px diagonal cross-weave normal map aliasing below Nyquist — screen-space confetti standing in for wave structure, MID_rms only 9.52 against HF_rms 13.25.
- Forest and jungle canopy is per-pixel confetti: 1-2 px lime specks to #B4D42A over crushed black gaps, hue 74 / sat 0.44 vs the bible's #3C5E31 hue 100. No tree silhouettes at any scale. Non-negotiable #2 broken.
- Untextured flat matte on every built surface: Aurelia's keep tower faces, the cone hut at (985,625), the distant water polygon at (250,140) are single-value gradients with one step per face and zero albedo break-up.
- Props ungrounded: the 5-6 px trees at (172,124), (245,158), (272,155) and the plains rocks have no AO decal, and the selected unit's only dark ground is the selection ring's vignette. Non-negotiable #3 broken.
- Streak artifact at (890-990, 655-720): the river delta is a flat #A0C1D6 triangle with an unantialiased diagonal edge, a blown near-white core, and no shoreline blend or foam.
- Aerial perspective tints the wrong way — FAR band vector hue 6.0 vs NEAR 39.6, distant forest at hue 340.5. Value lift (0.460 vs 0.399) and desaturation (0.258 vs 0.377) are right; the fog colour is magenta instead of cool.
- Three leader portraits are the identical beige profile head separated only by a background swatch, and the Warrior portrait is a featureless egg helmet with no hands and no weapon.

**Fixes**
- units.js:2486 and :3589 — the foot-unit factor of 2.20 yields 13 px. Add a screen-space floor after usc is solved: s *= clamp(minPx / projectedPx, 1.0, 4.5) with minPx = 0.42 * projected hex flat-to-flat (≈55 px at this camera). Take the selection ring from 28 px to 0.72 * tile width (≈94 px) so the ring frames a figure instead of hiding one.
- Water: bias the detail normal mip +1.0 and fade it to zero over 40-90 m of view distance, then put the energy back at MID frequency with an 18-25 m swell shaded to 10-14 luma amplitude. Target MID_rms 13-16 and HF_rms 10-12, i.e. MID/HF ≈ 1.2, which clears the gate with margin rather than by 0.02.
- Canopy: replace the per-pixel speckle with instanced blob clusters of 4.5-6 m world diameter (14-20 px here) on 3 LODs, and drive value break-up from a mipped world-space texture at 2 m/texel band-limited so the finest surviving octave is >= 6 px on screen. Clamp canopy sat to 0.32-0.44, lit value <= 0.52, and delete the >0.55-sat lime highlight outright.
- Fog/inscatter: change the tint from its current warm-pink to hue 200-210, sat 0.10-0.14, value 0.72-0.78 (≈ #A9BECE), keeping the existing +0.06 value lift and the sat falloff. Far sand should land near hue 25 (cooler than the near 33), not the current 6.8.
- Shadow ambient: set it to lit hue -6 degrees, sat x0.75, value x0.55. On #DDAF79 sand that gives shadow ≈ #8A6949 at hue 27, inside the bible's 10-degree rule, replacing the current #6C5356 mauve that is rotating 27-42 degrees.
- Buildings are not sampling the material shader that units.js:271-281 already provides. Give any face wider than 3 m stone courses at 0.45 m pitch with +/-6 luma per-course variation and a 0.35 AO in the joints, and require at least two octaves of albedo break-up whose finest octave is >= 5 px on screen.
- Add a per-prop AO decal for scattered trees and rocks: radius 0.85 x footprint, 0.55 centre opacity smoothstepping to 0, offset 0.25 x prop height along the sun's ground projection. Same decal under every unit, independent of the selection ring.
- River: drop the flat fill and run it through the coast shader — blend into terrain over a 1.5 m shoreline band with the existing foam ribbon, and clamp its specular to max luma 232 so the core stops blowing to white.
- Farm improvement: retune to #A8A05E (sat 0.34, val 0.66) inside the plains band, widen the furrow pitch from ~4 px to ~11 px world-space so it mips, and fill the furrow gaps with a 0.25 AO instead of pure black.
- After the canopy fix lands, take the land-side grid stroke from a 1.5 px core to 2.0 px and make it a 0.58x multiply of local ground luma rather than a fixed dark colour, so it holds contrast on both #DDAF79 sand and #5A6E33 canopy instead of competing with 20 noise dips per 500 px.

## critique 6 — 31/100
REJECT. Gate FAIL (far-cliff MID/HF 1.53 > 1.3; water MID/HF 0.72 < 0.9), plus five independent automatic fails: illegible hex grid, per-pixel noise standing in for material, untextured flat-matte surfaces, props with no contact shadow, toy/programmer-art unit silhouettes, and garish colour. Measured: far-cliff mean 133.6 sat 0.384 HF 12.87 MID 19.72; mid 82.5 / 0.318 / 17.01 / 18.69; near-field 102 / 0.376 / 21.03 / 20.40; water 99.6 / 0.408 / 13.43 / 9.63; nearFarHFRamp 1.63; crushedPct 0.12; blownPct 0.00.

Unlabeled, side by side with a real Civ VI screenshot: the player picks Civ VI, and it is not close — it takes about a fifth of a second. THE SINGLE TELL IS THE HEX GRID. Civ VI reads as a board from across the room because every hex edge is an explicit constant-width stroke that survives forest, mountain and city. AEON has no drawn stroke at all. What you are calling a grid is the shading crease between adjacent flat-shaded hex plates, so it appears wherever two tiles happen to tilt differently and vanishes wherever they do not — you can watch it die mid-edge in the open grass at (430-670, 120-270). Numerically: in the far band the 95th-percentile grid dip is only 16.7 L against a local 19px box mean, while the terrain's own noise floor sits at a median dip of 10.1 (mid) and 17.9 (near). The grid signal is at or below the noise it has to sit on top of. AEON does not read as a board; it reads as a slightly out-of-focus satellite photo of moss. Runner-up tell, half a second later: the acid chartreuse — the brightest canopy pixel is rgb(224,250,106), HSV 71 deg / 0.58 / 0.98, against your own forest band of value 0.20-0.40 and sat 0.32-0.44. That is a hi-vis safety vest at golden hour.

On the specific focus items, and I will be fair where the build earns it. Crushed shadows are NOT a problem: crushedPct 0.12 against a 0.30 budget, blownPct 0.00 against 0.05, and only 2.42% of the near-field falls below L 18. There is also no hard corner vignette — I sampled the ocean at constant depth and the falloff I first read at x 1320-1480 was the notification rail, not the image. The real tonal defect is the opposite of a vignette: a broad shapeless bright lobe over the shallows at (1000-1300, 400-600) running L 157-167 against an ocean base of ~110, with zero glint anisotropy, that reads as bloom rather than sun glitter and pulls the eye clean off the city, which is supposed to be the subject of the frame. Aerial perspective is right on two axes and inverted on the third: far sand at (240,150) is L 162.6 sat 0.465 B-R -92, near sand at (500,800) is L 115.4 sat 0.509 B-R -75. Lighter, marginally less saturated, and 17 units WARMER with distance. Your bible says cooler. The haze is a tan wash, so distance yellows the map instead of pushing it back.

The rest is execution debt against a direction you already locked. 4.26% of pixels in the green field at (300-700, 380-620) sit more than 45 L below their own 7x7 mean — that is a dense black-pepper field of sub-pixel vegetation impostors, and TAA has already converged, so it is baked in, not a sampling artifact. MID_rms is 19.72 far and 20.40 near, a ratio of 1.03: mid-frequency blob scale does not shrink with distance at all, and your HF-only ramp of 1.63 clears its 1.6 floor by 2% while hiding that. Lit grass at (520,300) is hue 79 and its own shadow at (600,430) is hue 23 — a 56 degree swing against a 10 degree cap; sand is 23 to 8, still over. Nothing in the frame is grounded — not one tree in the canopy field at (490-620, 250-400), not the rocks and huts at (900-1030, 560-650). The selected warrior at (975,680) is a 14px grey smudge inside a gold ring, and its panel portrait is a box-and-cylinder mannequin. Calyx (pop 1) and Aurelia (pop 7) are the same brown blob. The HUD chrome itself is genuinely good — top bar, unit panel, notification rail and end-turn are clean, well-hierarchied and free of clipping, and 2720 BC is not clipped, I checked at 9x — but the in-world banners are being destroyed by the world render: "Aurelia" at (735,580) is ~8px cap height, resampled through TAA into mush with black speckle punching through the banner fill.

This is not a taste disagreement. Every number above is measured against constraints your own team wrote down and locked.

**Problems**
- Hex grid is not drawn — it is the shading crease between flat-shaded hex plates, so it vanishes wherever two adjacent tiles share a tilt. Far-band p95 edge dip 16.7 L versus a terrain noise floor of median 10.1 (mid) / 17.9 (near): the grid is at or below the noise it must sit on. Automatic fail, and it is non-negotiable #1 in your own bible.
- Water is pure per-pixel confetti: MID/HF 0.72 against a 0.9 floor, HF_rms 13.43 versus MID_rms 9.63. No swell, no wave crests, no anisotropy — a static 2px sparkle field with red/maroon chroma speckle mottling the blue at (1180-1440, 300-400). Gate failure and automatic fail.
- Far terrain is blurry blobs on flat facets: far-cliff MID/HF 1.53 against a 1.3 ceiling, HF only 12.87. The cliffs at (40-340, 70-270) are hard-edged untextured polygonal planes with a cloudy popcorn overlay smeared over them. Gate failure plus the untextured-flat-matte automatic fail.
- Detail does not shrink with distance. MID_rms is 19.72 far and 20.40 near — ratio 1.03. The HF ramp of 1.63 squeaks past its 1.6 floor while the mid-frequency blob scale is completely flat across depth. Screen-space detail wearing world-space clothes.
- Nothing is grounded. Zero contact shadow or AO decal under any tree in the canopy field at (490-620, 250-400), any rock or hut at (900-1030, 560-650), or the crate at (760,790). Trees and props sit on the terrain like stickers. Bible non-negotiable #3, automatic fail.
- Toy silhouettes. The selected warrior at (975,680) is a 14px grey smudge inside its selection ring — unnameable as a unit type. Its panel portrait is a cylinder head, box torso, stick arms and a rectangle shield. Calyx (pop 1) and Aurelia (pop 7) are indistinguishable brown clusters, so city size does not read from shape. Bible non-negotiable #4, automatic fail.
- Garish and muddy at once. Brightest canopy pixel rgb(224,250,106) = HSV 71/0.58/0.98 against a forest band of value 0.20-0.40, sat 0.32-0.44 — 2.4x over on value. Lit grass sat measures 0.50-0.55 against a 0.28-0.46 gate band. Meanwhile the shadow side goes violet and cyan: lit grass hue 79 to shadow hue 23 is a 56 degree swing against a 10 degree cap, and sand runs 23 to 8.
- Per-pixel pepper standing in for vegetation: 4.26% of pixels in (300-700, 380-620) sit more than 45 L below their own 7x7 mean. Sub-pixel scatter impostors aliasing into dirt specks, baked in post-TAA-convergence, and they punch straight through the Aurelia banner at (735,580).

**Fixes**
- Draw an actual grid stroke in the terrain shader instead of relying on plate creases. Signed distance d to the nearest hex edge in world XZ, w = fwidth(d), a = 1 - smoothstep(0, 1.5*w, d - halfWidth) with halfWidth 0.045 tiles (~2.2 screen px at this zoom). Composite as a LIGHTEN — lerp(base, base + 0.16, a) — because the terrain noise floor is already dark-biased and a dark line at that amplitude is invisible. Acceptance: measured edge dip >= 28 L against the local 19px box mean in all three depth bands (today far is 16.7).
- Weld the hex plates. Share vertices across adjacent hexes and smooth normals with a 60 degree angle threshold, so the tile boundary is carried by the drawn stroke and not by a geometric fold. Keep a hard crease only at real cliff edges, 0.15 tiles of bevel. This is what is producing both the fake grid and the far-cliff MID/HF of 1.53.
- Kill the pepper at the source, not with a blur. Give the vegetation scatter LODs a screen-size floor: cull any instance whose projected bound is under 3.0 px, and cross-fade the last 3-6 px into ground albedo over a 0.25 range instead of popping. Acceptance: pixels more than 45 L below their 7x7 mean drop from 4.26% to <= 0.5% over (300-700, 380-620).
- Give the water structure. Two mipped Gerstner/normal octaves at world wavelengths ~9 m and ~3 m, amplitudes 0.22 m and 0.06 m; cut the specular-noise term contrast by 60%; clamp normal-map mip bias to 0 (a negative bias is the standard cause of exactly this HF 13.43 / MID 9.63 signature). Acceptance: water MID_rms >= 14, HF_rms 9-12, MID/HF ~1.2.
- Put real material on the far rock. Triplanar detail layer at 1.2 m tiling, mip bias 0, normal strength 0.6, and halve the amplitude of the current cloudy overlay that is generating the blob energy. Acceptance: far-cliff MID/HF 1.05-1.20 with HF_rms staying in 12-22.
- Make blob scale depth-dependent, not just HF. Confirm the ground detail texture is sampled in world UV and not a fixed screen-space UV — MID_rms 19.72 far versus 20.40 near says something is screen-locked. Add MID_rms near/far >= 1.5 to the gate alongside the existing HF ramp so this cannot hide again.
- Fix ambient so shadows keep their surface hue. Replace the fixed ambient tint with albedo-weighted ambient: ambient = albedo * skyColor * 0.35 + albedo * groundBounce * 0.25, and cap the permitted cool shift at 6 degrees. Acceptance: probe pairs (520,300)/(600,430) and (240,150)/(300,215) both come back within 10 degrees of hue; today they are 56 and 15.
- Pull the greens into the locked palette. Set forest canopy albedo to #3C5E31 and clamp post-tonemap canopy value to 0.46 (brightest pixel is currently 0.98); drop grass saturation from the measured 0.50-0.55 to 0.34-0.40. That single change fixes the acid-lime read and brings the green regions inside the 0.28-0.46 saturation band.
- Ground every instance. One AO decal per tree, rock, hut, crate and unit: radius 0.55x the instance footprint, opacity 0.55 at centre falling to 0 with smoothstep, multiplied into ground albedo and drawn BEFORE the grid stroke so the stroke stays on top.
- Flip the aerial-perspective hue and re-anchor the labels. Change fog inscatter from the current warm tan to a cool ~#8FA6BE, driven by view depth rather than screen y — acceptance: far-minus-near B-R of +15 to +30, a sign flip of about 110 units from today's -92 far versus -75 near. Separately, render map banners in a post-TAA pass at native resolution, 12 px minimum cap height, 1 px dark outline, so 'Aurelia' at (735,580) stops being mush with pepper punching through it.

## critique 7 — 22/100
Gate FAIL, and the water pass fails on every one of its own five criteria. Reject.

First, your measurement is lying to you. The `water` box you told me to sample — 1150,200,240,160 — lands almost entirely on the notification rail. Its HF_rms 13.44 is the anti-aliased edges of the words "Korrath and Vellum are at war." You have been scoring your ocean on UI text. Sampling actual open water: water-open (1300,320) HF_rms 5.35 — below the 7 floor, dead flat. water-shallow (990,300) HF 6.21, MID/HF 1.43. water-farNE (880,180) HF 4.13, MID/HF 1.43. water-nearbay (1080,650) HF 21.89 — above the 15 ceiling, confetti. Eight gate failures, not two, and a near/far water ramp of 5.30 that is not a ramp, it is a cliff.

Wave structure vs noise: it is noise, and I can prove it without opening my eyes. Octave-residual RMS on the deep ocean patch (1330,330) runs 5.04 / 6.99 / 5.49 / 4.12 across four octaves — ratio 1.22. A flat spectrum. A Phillips or JONSWAP surface falls off hard; swell should dominate at roughly 0.35-0.50. Yours has as much energy at one pixel as at eight. Then I opened my eyes: at 10x the shallow shelf is salt-and-pepper, individual pixels swinging #e0f0ff to #2a3f6b with no coherence past 2px. That is the exact thing your own bible bans at line 15, verbatim. And the deep field ends in a hard straight diagonal against dead lavender — the stipple does not attenuate toward the horizon, it just stops. Detail does not shrink with distance; it gets clipped by a seam.

Streaks: ocean-deep dH/dV = 1.74, shelf 1.52. Horizontal gradient 74% stronger than vertical means the pattern is stretched along screen-Y. Under a 55-60 degree pitch the perspective compression should push this UNDER 1.0 — far ocean, the one patch that behaves, reads 0.89. Your land reads 1.07. So the harness is fine and the water shader is building UVs in screen space or off a plane projection with no pitch term. Those vertical 1px combs in the shallows are that bug, visible.

Specular: there is none. Brightest deep-ocean pixels are p99 [148,201,223] and max [186,211,215]. R-B = -29. Cyan. Under a 5600K key those highlights must be warm-white, +25 to +45. There is no glint path anywhere, no cone toward the sun azimuth (upper-right, per the city shadows), no Fresnel horizon brightening. What you have is an albedo tint with noise on it. And the base is wrong anyway: deep ocean [81,106,159] is hue 221, value 0.62 — bible ocean #123A63 is hue 211, value 0.39. You shipped periwinkle.

Shallow caustics: absent. The shelf is the same isotropic stipple as the deep water, just brighter. No depth gate, no projection, no scroll, no red-first extinction. Nothing distinguishes 0.5m from 5m except a lightness ramp.

Rivers: not channels. At 6x the Aurelia river is a chalky pale-blue and white smear laid across the hillside — no bank, no bed, no depth darkening at the margins, arbitrary width, brighter and less saturated than the terrain it crosses, and it terminates mid-slope without a mouth. Worse, terrain punches through it in hard-edged polygonal patches along its length. That is a coplanar decal z-fighting the heightfield. Automatic fail on its own.

Grid bleed-through: in the 10x shallow crop I cannot recover a single hex edge. The only 1px dark features present are the noise itself, so an edge and a trough are the same pixel. Illegible hex grid over water. Automatic fail.

And while I was in there: sandLIT [148,111,79] hue 28, sandSHADOW [102,92,136] hue 253 — a 135 degree hue error against a 10 degree limit, on the exact surface your bible names by hand ("never navy shadows on tan sand"). That headland facet is also flat-matte and untextured with a hard triangular crease, and the white rock at ~1265,845 sits on the sand with no contact shadow. Far-cliff octave RMS 12.87/13.63/17.51/16.43 — macro blobs carry MORE energy than fine grain, which is why MID/HF is 1.53: distance is being faked with a blur instead of a mip.

Answer to the question asked: unlabeled, side by side with a real Civ VI screenshot, the player picks Civ VI without hesitating. The single tell is the water. Civ VI's ocean is a calm, low-frequency, obviously hand-authored surface that a hex grid sits cleanly on top of. Yours is a field of blue television static that has eaten its own grid — one glance at the coast and you know nobody drew this, a noise function did.

**Problems**
- Ocean is value noise, not waves: octave-residual RMS 5.04/6.99/5.49/4.12 across four octaves = ratio 1.22, a flat white-noise spectrum where a wave spectrum must fall to 0.35-0.50. At 10x the shallow shelf is per-pixel salt-and-pepper (#e0f0ff to #2a3f6b, no coherence past 2px) — the literal case ART-BIBLE line 15 bans.
- Hex grid is illegible over water. In the 10x crop at 1160,400 no hex edge is recoverable; the only 1px dark features are the noise itself, so an edge and a wave trough are indistinguishable. AUTOMATIC FAIL (non-negotiable #1).
- Vertical streak artifacts across the whole water surface: dH/dV anisotropy 1.74 (deep) and 1.52 (shelf) where far water reads 0.89 and land reads 1.07. Wave UVs are stretched along screen-Y — built in screen space or from a plane projection missing the camera pitch. AUTOMATIC FAIL.
- Zero specular. Brightest deep-ocean pixels are p99 [148,201,223], max [186,211,215], R-B = -29 (cyan). A 5600K sun key demands +25 to +45 warm-white glints. No glint path, no cone toward the sun azimuth, no Fresnel horizon lift.
- Rivers are painted decals, not channels: at 6x the Aurelia river is a chalky white-blue smear with no bank, no bed, no edge darkening, arbitrary width, brighter than the terrain, terminating mid-slope with no mouth — and terrain punches through it in hard polygonal patches along its run (coplanar decal z-fighting). AUTOMATIC FAIL.
- Water detail does not shrink with distance, it gets clipped: near/far water HF ramp 5.30 (21.89 at 1080,650 vs 4.13 at 880,180), and the deep stipple ends at a hard straight diagonal seam against flat lavender instead of attenuating. AUTOMATIC FAIL.
- No shallow caustics at all — the shelf is the same isotropic stipple as deep water with the brightness turned up. No depth gate, no sun projection, no scroll, no wavelength-dependent extinction.
- Palette + grounding failures on the coast: sandLIT [148,111,79] hue 28 vs sandSHADOW [102,92,136] hue 253 = 135 degree error against a 10 degree limit, on the exact 'navy shadow on tan sand' case the bible names; that headland facet is flat-matte and untextured; the rock at ~1265,845 has no contact shadow. Deep ocean [81,106,159] (hue 221, value 0.62) vs bible #123A63 (hue 211, value 0.39).

**Fixes**
- Replace the water noise with 4 summed Gerstner waves in WORLD space: wavelengths 6.0R / 2.8R / 1.4R / 0.65R (R = hex radius), amplitudes 0.030R / 0.014R / 0.006R / 0.0022R, steepness Q 0.55 / 0.35 / 0.20 / 0.10, directions spread +/-25 degrees around one wind azimuth. Re-run tools/_wspec.mjs and drive octRatio (oct0/oct3) from 1.22 down into 0.35-0.50.
- Fix the vertical smear: build wave and detail UVs from world XZ only, never from screen or view space. Verify with tools/_wspec.mjs — ocean-deep aniso must land 0.85-1.15 (currently 1.74; your land already reads 1.07, so the harness is clean and the bug is in the water shader alone).
- Mip the detail normal properly: sample with textureGrad using world-space derivatives, tiling at 3.5 world units, and flatten the normal toward (0,1,0) by smoothstep(35, 110, viewDist). Target deep-water HF_rms 8-12 everywhere and a near/far water ramp of 1.6-2.2, replacing today's 5.35-vs-21.89 cliff. Delete whatever hard distance cutoff creates the straight diagonal seam at ~1370,400.
- Add a GGX sun lobe: roughness 0.055-0.085, sun colour 5600K (~[255,241,224]), F0 = 0.02 with Schlick. Success is measurable — brightest ocean pixels must flip from R-B = -29 to R-B = +25..+45 and form an 18-25 degree cone toward the sun azimuth (upper-right per the city shadows), with p99.9 luminance ~2.2x the water mean instead of today's flat 1.6x.
- Re-key the ocean colour: base #123A63 at normal incidence rising to sky at grazing, so near-camera deep water measures value 0.36-0.44 (currently 0.62, hue 221 vs target 211) and the horizon band is the lighter end. Right now the gradient is backwards — far ocean at y=130 reads lum 113 while mid ocean at y=480 reads 152.
- Build real caustics as a depth-gated term, not noise: intensity = saturate(1 - depth/2.5m) * sunVisibility, projected along the sun direction in world space, two voronoi layers at 1.2 and 0.7 world units scrolling +0.06 and -0.09 u/s, contrast 0.35, clamped to the top 0.6m of the column. Pair it with exponential extinction sigma_RGB = (0.42, 0.11, 0.06) per metre so shallow #2E7C93 dies to ocean #123A63 over 0-6m, red first.
- Turn rivers into geometry: carve a V into the heightfield, depth 0.10-0.14R, width 0.35R at the hex edge, water surface 0.02R below the bank lip, rendered with depth-write instead of a coplanar decal (that kills the z-fighting patches). Bed colour #1E4C5C, a 2-3px damp-gravel band at each bank, downstream flow UV, and route every river to the sea — the Aurelia river currently stops on a hillside.
- Draw the water hex grid as a separate analytic pass AFTER water shading, projected on the still-water plane and NOT displaced by the wave normal: world-space width 0.035R (~1.6px at this zoom), colour = local water colour darkened 22%, alpha 0.5 in the shallows falling to 0.18 beyond 2 tiles from shore. Verify by re-cropping 1160,400 at 10x — a continuous stroke must be recoverable by eye.
- Fix the ambient term causing the violet sand: shadow = albedo * (ambientSky * 0.35 + groundBounce * 0.65) with groundBounce albedo-weighted. sandSHADOW must move from [102,92,136] (hue 253) to about [96,74,55] (hue 26, value 0.30) against sandLIT hue 28 — inside the 10 degree limit. Same pass: give that flat headland facet the mipped sand grain the near beach has.
- Stop faking distance with blur on the far cliff: octave RMS there is 12.87/13.63/17.51/16.43, macro blobs carrying more energy than fine grain, which is what pins MID/HF at 1.53. Keep three mip octaves of detail alive out to 140 units and cut macro-blob contrast 35% instead of blurring the fine layer; target MID/HF 0.9-1.3 with HF_rms 12-22. Also rebalance saturation — far-cliff 0.384 is currently MORE saturated than near-field 0.376, inverting aerial perspective.

## critique 8 — 34/100
REJECT. Gate FAIL, six automatic fails, and the focus area of this pass — unit craft — is the weakest thing in the frame.

MEASURED (node tools/metrics.mjs shots/units-p6r3.png, the boxes you gave): gate FAIL. far-cliff MID/HF 1.53 (bound 1.3) — "blurry blobs, no material". water MID/HF 0.72 (bound 0.9). near-field HF_rms 21.39 / MID/HF 0.96 / sat 0.381 and mid HF_rms 17.12 / MID/HF 1.11 / sat 0.318 are the only two regions in spec. nearFarHFRamp 1.64 clears 1.6 by 4%. crushedPct 0.12, blownPct 0 — fine.

Your prescribed water box is contaminated: 1150,200,240,160 overlaps the notification rail (panel starts x≈1265), so its HF_rms 13.47 is UI glyph edges, not water. Re-probed on clean open water: 990,80,220,150 → HF_rms 1.04 (band 7-15), MID/HF 1.34. 1000,380,200,140 → HF_rms 8.51, MID/HF 1.42. The ocean is a dead vinyl sheet. You have been tuning water against the HUD.

Aerial perspective is half-implemented: far-cliff mean 137 vs near-field 101.7 — correctly lighter. But far sat 0.384 vs near sat 0.381. The bible says farther must be LESS saturated. It is +0.003. There is no desaturation term in the fog at all.

Palette execution, per-pixel samples: ocean at (1300,300) hsv(225°, 0.459, 0.601) against bible ocean #123A63 = hsv(209°, 0.82, 0.39) — 16° violet of target, 44% short on saturation, 54% too bright. Coast at (1090,300) hsv(213°, 0.509, 0.736) against #2E7C93 = hsv(193°, 0.61, 0.58) — 20° violet. Lit desert at (330,520) sat 0.53 against the 0.24-0.34 band — 56% over. Grass tile at (430,420) hsv(91°, 0.286, 0.297) — under the 0.30 sat floor and scraping the 0.28 value floor. So the sand is garish and the grass is muddy in the same key light, 2.6x apart in value. That split is the whole "muddy" read.

UNIT CRAFT — the actual focus, and a categorical failure. Zoom (930,645,100,80) at 12x: the tile occupant is an olive bell with a gold TORUS on top containing a black void and a 6px white smear. Zoom (845,655,110,80): an orange banana, a second torus, blue cylinders, an eye-shaped white blob. I cannot name a single unit type from shape. Nothing on this map reads as a person. At (990,675)-(1120,730) a pale rod runs ~150px diagonally at uniform 4px width, hard specular edge, no taper, terminating in mid-air over the river attached to no figure — a weapon socket at roughly 4x scale or a mesh parented to a culled bone. The loudest shape in every unit is the base ring, and it communicates nothing.

The portrait (36,698,74,96 at 8x) is a low-poly mannequin: flat-matte blue tunic with zero cloth breakup, flat brown skin, a red squashed-cylinder disc for a hat, box shoulder pads, a beard rendered as one dark blob, eyes as a single smear. The spear tip is clipped by the frame's left edge and the bust sits 60% right of centre. The three leader heads in the top bar (1080,0) are the SAME mesh in the SAME pose three times — only the background tint and the bar colour change.

Cities: three cities, three unrelated asset languages, and none of them scale-reads. Calyx (pop 1) is a stack of concentric lathe-turned cylinders — a wedding cake, not a settlement. Aurelia (pop 7) is a keep whose big wall face measures HF_rms 15.21 against its own roof at 32.28: the walls carry half the material energy of the roof and read as a gradient. You tell pop 1 from pop 7 only by reading the number badge.

Grid, non-negotiable #1: crop (120,140,200,140) at 7x shows the edge stroke on the cliff breaking into a 1px dotted stipple, and its polarity inverts — cream-bright on the shadow side, dark on the lit side. That is not one analytically-AA'd world-space stroke, it is an aliasing artifact. Same box holds 183 near-black pixels (RGB sum < 70) across a fully sunlit desert cliff — T-junction cracks between hex tiles, e.g. (222,98) and (234,103), showing through to void.

Vegetation is 1-2px lime confetti with no tree instances, no canopy silhouettes, no contact shadows — bible rule 2 and rule 3, broken in the same square inch. The lime tree at (1140-1190,660-710) beside Solmere floats.

Credit where due: the top bar, unit panel and notification rail are genuinely well-crafted — crisp type, good hierarchy, no clipping. The coastline foam reads. The near-field and mid regions hit their material bands. That is a competent UI artist and one working terrain shader on an otherwise unshipped world.

THE QUESTION — unlabeled, side by side with a real Civ VI screenshot, which does a player pick, and what is the single tell?

Civ VI, in under a second, and it is not close. The single tell: put a finger on any occupied tile. In Civ VI a tile holds a figure — you see a head, a weapon, a shield, a hard elliptical shadow welding it to the ground, and you can name it "warrior" without reading a label. In AEON the same tile holds a tan doughnut with a floating spear beside it. A player does not need to know what mipmapping or analytic AA is to register that one game has people on the board and the other has pottery.

**Problems**
- Unit silhouettes are non-existent. Zooms at (930,645,100,80) and (845,655,110,80) show a torus-on-a-bell, an orange banana arc, and unnameable blue/tan jumbles. No figure on the map reads as a person. Automatic fail: toy/programmer-art unit silhouettes.
- Gate FAIL. far-cliff MID/HF 1.53 vs bound 1.3 (blurry blobs, no material); the rock is one octave of fixed-frequency value noise that reads as bubble wrap, and the same lobe size appears at both near and far depth inside a single surface.
- Water is broken twice over. Clean UI-free probes: HF_rms 1.04 at (990,80) against a 7-15 band — the open ocean is a featureless gradient. Meanwhile shallow water carries visible flat-shaded triangle creases and cyan per-pixel noise. Your gate never caught it because the prescribed water box 1150,200,240,160 overlaps the notification rail at x>=1265 and was measuring HUD glyph edges.
- Hex grid fails non-negotiable #1 on any slope. Crop (120,140,200,140) at 7x: the stroke dissolves into a 1px dotted stipple and inverts polarity — cream on the shadow side, dark on the lit side. Not analytic AA, not world-space constant width.
- Untextured flat-matte surfaces throughout. Aurelia's keep wall measures HF_rms 15.21 against its own roof at 32.28; the portrait tunic, skin and the three leader heads carry zero surface breakup. Automatic fail.
- Vegetation is per-pixel confetti standing in for material — 1-2px lime specks with no instanced canopies, no tree silhouettes, and no contact shadows (the tree at 1140-1190,660-710 and the shed at 735-760,775-800 both float). Automatic fails on both counts.
- Streak artifact and geometry cracks. A ~150px uniform-width pale rod at (990,675)-(1120,730) floats unattached over the river. 183 near-black pixels (RGB sum < 70) sit inside a fully sunlit cliff box at (60,95,260,195) — T-junction gaps to void at e.g. (222,98), (234,103).
- Colour: lit desert sat 0.53 against a 0.24-0.34 band while grass sits at sat 0.286 / value 0.297 — garish sand and muddy grass, 2.6x apart in value under one key. Ocean is 16 degrees violet of #123A63 and 54% too bright. Aerial perspective has no desaturation term at all: far sat 0.384 vs near 0.381.

**Fixes**
- Grid: replace the current stroke with an analytic edge in the terrain shader. d = distance to nearest hex edge in world XZ (never UV), w = fwidth(d), alpha = 1 - smoothstep(0, 1.5*w, d - 0.055). Clamp the resulting screen width to [1.1, 2.2] px so it cannot sub-pixel on slopes, and composite as a 0.62 multiply over albedo — never additive, which is what produces the inverted cream dots at (128,150)-(160,250).
- Rock: swap the single noise octave for 3 triplanar octaves at world scales 4.0 m / 1.1 m / 0.3 m, amplitudes 1.0 / 0.45 / 0.20, plus a bedding mask sin(worldY * 3.2) at 0.15 contrast. Target far-cliff MID/HF 1.53 -> 1.05-1.15 by lifting HF_rms 13.03 -> 15-18 without raising MID_rms above 20.
- Deep water: two scrolling normal layers, world tiles 6.0 m and 1.7 m, scroll 0.030 and 0.011 m/s, normal strength 0.35 and 0.18, spec roughness 0.08 with sun intensity clamped to keep blownPct at 0. Target HF_rms 1.04 -> 9-13. Shallow water: subdivide the plane to <=0.5 m edges or go flat-plane-plus-normal-map to kill the flat-shaded triangle creases at (760-940, 730-830).
- Water colour: ocean is hsv(225, 0.459, 0.601) and must be hsv(209, 0.82, 0.39) per #123A63 — hue -16, sat +0.36, value -0.21. Coast is hsv(213, 0.509, 0.736) and must be hsv(193, 0.61, 0.58) per #2E7C93. The uniform violet bias across both says the sky-ambient term is unclamped; tint the ambient to the coast hue before it lands on water.
- Vegetation: kill the pixel specks. Instance 4-7 canopy cards per hex, 18-28 px tall at this zoom, 3 silhouette variants x 2 scales, each with a 0.55-alpha elliptical AO decal at 1.35x footprint. Clamp canopy albedo to sat 0.44 / value 0.40 (bible forest #3C5E31) — the current specks peak above value 0.78, which is what makes the mass fizz.
- Terrain values: raise grass to value 0.38-0.44 / sat 0.32 (currently 0.297 / 0.286) and pull lit desert saturation from 0.53 to 0.30-0.33. Closing the 2.6x value gap between sand and grass fixes the muddy-vs-garish split without touching the lighting rig.
- Aerial perspective: add a saturation term to the fog, sat *= mix(1.0, 0.72, fogFactor), so far regions land at 0.27-0.30 while near-field holds 0.38. Leave the existing value lift alone — 137 far vs 101.7 near is correct.
- Units: enforce a silhouette budget per unit — one dominant vertical (spear/standard) at 1.6x body height, a distinct head shape, and a 3-value read (dark boots / mid body / light head+weapon) with >=0.35 luminance separation between the bands. Delete the torus base ring, currently the loudest shape and semantically empty; replace with a flat 2px civ-colour arc at 0.7 alpha under the feet. Then trace the unattached rod at (990,675)-(1120,730): 150px at uniform width, no owner — a ~4x weapon socket scale or a mesh parented to a culled bone.
- Contact shadows on every prop, no exceptions: projected AO decal at 1.2x footprint radius, 0.45 core opacity falling to 0 over 0.4 m, tinted to the ground albedo hue rather than to black. Start with the floating tree at (1140-1190,660-710) and the shed at (735-760,775-800).
- Portraits and cities: (a) portrait needs a 2 mm-scale fabric normal at 0.6 strength on the tunic, 0.25-roughness anisotropic metal on the spear, a camera-left rim at 15% key, and a re-frame — the spear tip is clipped at the left edge and the bust sits 60% right of centre, so centre it and dolly back 12%. (b) The three leader heads at (1080,0) are one mesh three times — build 3 distinct head silhouettes in 3 different 3/4 rotations. (c) Tie city footprint to population: pop 1-3 = 3-5 huts in a palisade, no tower; pop 4-6 = add a stone keep at 1.6x hut height; pop 7+ = curtain wall plus second tower — sharing one roof pitch and one wall material so Calyx, Aurelia and Solmere read as one civilisation instead of three games.

## critique 9 — 31/100
REJECT. Gate FAIL on the harness boxes (far-cliff MID/HF 1.53 > 1.3; water MID/HF 0.71 < 0.9), and six independent automatic fails on top of it. My own probes make it worse, not better: grass-mid HF_rms 24.56 (>22), water-near HF_rms 23.71 and water-mid 20.78 (>15), sand-near MID/HF 0.72, cliff-far MID/HF 1.57, grass-far HF_rms 3.17 (<12), plains sat 0.237 (<0.28).

Read those numbers together and they describe one defect, not seven. Every near region is BELOW 0.9 MID/HF and every far region is ABOVE 1.3. There is no mid-frequency band anywhere in this frame. Near ground is single-pixel confetti; far ground is a blur with the confetti smeared out of it. That is the exact signature of one high-frequency detail texture sampled without world-space derivatives, with a distance blur bolted on to hide the aliasing. The bible's non-negotiable #2 says detail lives in the world, not on the monitor, and this build has it on the monitor. Water proves it outright: HF 20.78 at y=500 versus 23.71 at y=800 is a 1.14x ramp over 300 scanlines. The chevron crosshatch is the same pixel size near and far. That is a fixed screen-space normal map. Then it cliffs to 5.12 in the distance — a mip-bias cutoff, not a LOD.

Non-negotiable #1 is broken and it is the one that ends the argument. I cannot trace a single complete hexagon in the left half of this frame. The stroke is a multiply-darken, so over the lime grass at 380,150 it vanishes entirely, under the city it vanishes, over the tan at 780,780 it is a 1px stair-stepped pure-black jaggy with no analytic AA, and over dark grass top-right it is a clean 2px line. Four different behaviours in one screenshot. Meanwhile the water shows per-tile value steps at 820,300 — so the tiles are visible where the grid should fade and invisible where it must not. That is exactly backwards.

The cliffs are the worst asset. At 3.5x the top-left massif is flat-shaded polygon salad: large uniform quads with literally zero texture inside them abutting a bubble-wrap noise heightfield, with black hairline cracks along the base of every pale slab (coincident-face gaps or z-fight). No strata, no bedding dip, no cleavage, no scree, no snowline, so there is no scale cue at all — those slabs could be 3m or 300m. Palette: mountain is locked at #7A7368, sat 0.08-0.18, val 0.32-0.62; measured cliff-lit is #c6a67c, sat 0.373, val 0.776. Twice the chroma ceiling. The rock is sharing the desert albedo. Shadow discipline is broken too: cliff lit hue 34 to shadow hue 19 is a 15-degree rotation against a 10-degree cap, and the sand shadows at 850,320 come out lavender — the bible bans navy-on-tan and this is the same error rotated to magenta. Aerial perspective scores 1 of 3: lighter yes (val 0.634 far vs 0.427 near), but the far massif is hue 29 warm orange against near hue 68, so it is the WARMEST thing in frame, and saturation is 0.316 far vs 0.325 near — flat, no ramp.

Nothing is grounded. Not one prop, tree, boulder or building in this frame has an AO decal or a contact shadow. The tree pancakes at 700,200 float on the grass. The keep at 780,660 — the hero asset, the focal point — meets the ground with a hard silhouette and no occlusion gradient, and its front wall is a flat untextured matte quad with a darker rectangle inset on it. That is programmer art at the centre of your composition. Add TAA comet-tails off the small props at 830,830, and a clipped XP stat cell plus a gold corner bracket drawn on top of the BUILD button face in the unit panel, and the Warrior portrait — cylinder head, floating red saucer hat, mitten hands, a bare white triangle for a spear — and the automatic-fail list runs to eight.

Credit where it is owed, because the bible says keep it: the HUD chrome is genuinely well made, the city keep silhouette and its red tile roofs read at gameplay zoom, the coastline foam reads, the rivers read. That is why this scores 31 and not 12. It is not incompetent. It is one honest material system away from being a real game, and it does not have one.

Unlabeled, side by side with a real Civ VI screenshot: the player picks Civ VI in under a second, and it is not close. The single tell is that nothing in your world has an edge. Civ VI terminates every element in a deliberate hard edge — tile boundary, canopy, cliff ridge, coastline — and that is what makes it read as an illustration you can play. Every organic element in this frame dissolves into a blurred mottle with pixel confetti sprinkled on top, so the image has no drawn structure at any scale. The one edge the game cannot afford to lose, the hex grid, is the one that is gone.

Fix the material system and the grid and I will look again. Do not touch the direction — the direction is fine, the execution is not there.

(Probe I added for your shadow-hue and palette-compliance checks: /home/piotr/looping_opus_5_test/tools/_hueprobe.mjs — same arg format as metrics.mjs, prints hex/hue/sat/val per box. You need it in CI; the 10-degree shadow rule is currently unverified and broken on every surface I sampled.)

**Problems**
- Hex grid illegible — automatic fail on non-negotiable #1. Four different behaviours in one frame: absent over lime grass at 380,150 and under the city, 1px stair-stepped pure black with no analytic AA over sand at 780,780, clean 2px over dark grass top-right. It is a multiply-darken, so it dies on dark terrain and aliases hard on light. Meanwhile per-tile value steps ARE visible in the water at 820,300, where the grid is supposed to fade — exactly backwards.
- No mid-frequency material anywhere. Every near region measures MID/HF below 0.9 (sand-near 0.72, water-near 0.87, near-field 0.96) and every far region above 1.3 (far-cliff 1.53, cliff-far-blob 1.57). Near is confetti, far is mush, nothing in between. Grass-mid HF_rms 24.56 against a 22 ceiling; grass-far HF_rms 3.17 against a 12 floor.
- Water detail is screen-space, not world-space — automatic fail on non-negotiable #2. HF_rms 20.78 at y=500 vs 23.71 at y=800 is a 1.14x ramp over 300 scanlines; the chevron crosshatch is the same pixel size near and far. Then it cliffs to 5.12 in the distance, which is a mip-bias cutoff, not a LOD. Plus hard diagonal polygon seams across the surface from per-vertex wave normals.
- Cliff geometry is untextured flat-shaded facets — automatic fail. At 3.5x the top-left massif is large uniform quads with zero texture inside them, abutting a bubble-wrap noise heightfield, with black hairline cracks along the base of every pale slab (coincident-face gaps or z-fighting). No strata, no bedding dip, no scree, no snowline — so no scale cue at all.
- Nothing is grounded — automatic fail on non-negotiable #3. Zero AO decals and zero contact shadows on any prop, tree, boulder, building or unit in the frame. Tree pancakes float at 700,200; the keep at 780,660 meets the ground with a hard silhouette and no occlusion gradient.
- Palette non-compliance on rock and water. Cliff-lit measures #c6a67c sat 0.373 val 0.776 against mountain spec #7A7368 sat 0.08-0.18 val 0.32-0.62 — twice the chroma ceiling; the rock is sharing the desert albedo. Ocean measures #6a95b4 val 0.708 against spec #123A63 val 0.39 — 1.8x too bright, no depth absorption. Plains sat 0.237 is below the 0.28 floor: muddy.
- Shadow hue and aerial perspective both inverted. Cliff lit hue 34 to shadow hue 19 is a 15-degree rotation against a 10-degree cap, and sand shadows at 850,320 render lavender — the banned navy-on-tan error rotated to magenta. Aerial perspective is lighter with distance but WARMER (far hue 29 vs near 68, the far massif is the warmest thing in frame) and flat in saturation (0.316 far vs 0.325 near).
- Flat-matte hero asset, toy unit silhouette, UI clipping, TAA streaks. The keep's front wall is an untextured matte quad with a darker rectangle inset. The Warrior portrait is cylinder head / floating red saucer hat / mitten hands / bare white triangle spear. The XP stat cell's right border is clipped and the gold corner bracket is drawn on top of the BUILD button face. Directional comet-tails off the small props at 830,830.

**Fixes**
- Hex grid: replace the multiply-darken with an analytic SDF edge. w = fwidth(d); a = 1.0 - smoothstep(0.0, 1.5*w, d), stroke width fixed at 0.055 tiles in WORLD space so it holds at every zoom. Make the colour value-relative, not absolute: darken the inner core by 0.22 of the underlying luminance and lighten the outer 40 percent of the stroke by 0.10, so the edge survives both #3C5E31 forest and #C6A874 sand. Fade alpha to zero over water starting at the second tile from shore. Verify by tracing three complete hexagons across a grass/sand/forest boundary at 1:1.
- Ground material: kill the screen-space octave. Sample albedo and normal in world XZ via explicit textureGrad with world-space derivatives — the 1.14x water ramp over 300 scanlines is the signature of derivative-free sampling. Three fixed world scales: macro 32m (never fades), meso 4m, micro 0.5m fading out between 25m and 45m camera distance. Target per land region HF_rms 14-18, MID_rms 16-21, MID/HF 1.00-1.20.
- Add the missing mid band. Author a meso albedo layer at 2-6m world period — soil patches, grass clumping, rock outcrop — as plus/minus 0.06 value and plus/minus 0.04 saturation applied BEFORE lighting. That lifts MID_rms by roughly 6 and pulls MID/HF into band from both directions at once.
- Distance ramp: delete the distance blur, let mip selection do the work. grass-far HF_rms 3.17 against a 12 floor means far terrain is being blurred, not filtered; the bias reads like +1.5 to +2.0. Clamp mip bias to 0.0. Target near/far ramp 1.7-2.0 with far HF still at or above 12.
- Cliffs: triplanar the faces with Y-projection weight pow(abs(N.y), 4.0) so vertical rock gets the strata projection instead of the ground projection. Add bedding — a 1D strata function along world Y, 1.2m period, plus/minus 0.05 albedo value, rotated by a per-massif dip of 8-15 degrees. Direction is the one thing bubble-wrap noise cannot fake. Weld vertices within 0.01m or inset faces 0.5 percent before extrusion to kill the black hairline cracks.
- Rock palette: multiply rock albedo chroma by 0.45 toward #7A7368 and clamp peak value to 0.58. Target sat 0.08-0.18, val 0.32-0.62. It is currently sharing the desert albedo at sat 0.373 val 0.776.
- Shadow hue: the ambient term is a constant cool colour added to warm albedo. Replace with albedo-weighted ambient — ambient = skyColor * 0.35 * albedo + groundBounce * 0.65 * albedo, ground bounce tinted to the local albedo hue at 0.25 strength. Hard-cap lit-to-shadow hue delta at 8 degrees and assert it in CI on sand, grass and rock.
- Aerial perspective: retint haze to roughly #A8BCC8 (hue 200) applied as exp(-d/D) with D = 140m, and add an explicit saturation lerp toward 0.55x at max distance. Target far sat 0.22-0.26 against near 0.34-0.40, and far hue must land cooler than near, not 39 degrees warmer.
- Water: replace the single fixed normal map with three Gerstner layers at world wavelengths 12m / 3.5m / 0.8m, amplitudes 0.35 / 0.12 / 0.03, directions 40 degrees apart, with the 0.8m layer mipped out past 30m. Compute normals per-pixel from the wave function, not per-vertex, to kill the diagonal seams. Add Beer-Lambert depth absorption with extinction (0.35, 0.09, 0.045) per metre so shallow lands on #2E7C93 and deep converges on #123A63 by 8m. Target water HF_rms 9-13, MID/HF 1.0-1.2.
- Grounding, then UI and props: every prop, tree, building and unit gets TWO elements, not one — a radial AO decal at 1.4x footprint radius, 0.55 centre opacity falling off with smoothstep(0.35, 1.0, r), multiplied into ground albedo BEFORE lighting so it survives the shadow pass, plus a separate directional contact shadow at the sun angle. Texture the keep wall with the same triplanar plus a 0.9m x 0.45m ashlar course, 0.02m recessed joint, plus/minus 0.04 per-block value jitter. Recompute the stat row to 4 cells at (panelW - 2*pad - 3*gap)/4 and inset the corner-bracket layer to pad+2px outside the button row bounds. Clamp TAA history to a 3x3 YCoCg neighbourhood AABB and reject above 2 px/frame velocity.

## critique 10 — 34/100
REJECT. Gate FAIL (water HF_rms 15.04 > 15, water MID/HF 0.64 < 0.9, blownPct 0.06 > 0.05) plus five separate automatic fails, and three of them break the art bible's own non-negotiables. Judged as execution against the locked direction, not taste: the bible asks for golden hour with warm key and a cool-but-not-navy shadow, and the build ships a violet ambient that puts lit rock at hue 1.8 deg and its own shadow at hue 290 deg — a 72 deg split against a 10 deg budget. That is the bible's explicit "never navy shadows on tan sand" clause, violated in the purple flavour, globally.

What is genuinely good, and I will say it once: the HUD is shippable. Top bar, notification rail, unit panel, END TURN, minimap frame — spacing, gold-on-charcoal hierarchy, corner brackets, no clipping, no overlap. The coastline foam reads. The city keeps have real silhouette. None of that is the problem.

The terrain is. Measured: far-cliff HF_rms 12.98 / MID/HF 1.10, mid 14.96 / 1.28, near-field 21.80 / 1.18, water 15.04 / 0.64, nearFarHFRamp 1.68. The land numbers pass and they are lying to you. Two probes say why. First, 1px chroma-over-luma is 0.42 on far cliff, 0.47 on near sand, 0.58 on far ocean, 0.48 on near ocean — against 0.19 on your own UI panel as a reference surface. A lit surface varies pixel-to-pixel in luminance, not in colour. A near-constant chroma-noise fraction at every depth and in every biome is a screen-space grain pass riding the whole frame, and it is what the HF numbers are actually scoring. Second, aerial perspective is inverted on matched material: sand at (900,430) is value 0.494 hue 326.6 deg, sand at (760,760) is value 0.554 hue 21.1 deg. The far sand is DARKER than the near sand and it is magenta. The bible says farther is lighter, cooler, less saturated, never darker. Your fog is a magenta multiply. The 1.68 ramp only passed because the far probe sits on bright rock and the near probe sits on grass — different materials, so the ramp measured albedo, not distance.

The hex grid is gone across the entire mountain massif. I contrast-boosted the top-left 420x280 region by 2.2x and there is not one hex edge anywhere on the rock. Mid-field the perpendicular delta is 34/255 on mean-126 sand, roughly 13% local contrast; far-field on rock it is zero. This is a turn-based strategy game and there is a whole biome the player cannot click accurately. Bible non-negotiable #1.

Cliff geometry is an undisguised hex prism. Flat trapezoidal sidewall, hard crease, flat cap, repeat — you can count tiles by their walls. The ground texture is projected top-down and smears into horizontal stripes down every vertical face, which is the missing-triplanar tell at a hundred paces. No strata, no dip, no scree, no talus: the walls meet the ground on a perfect mathematical line. The rock material itself is one blotch mask of cream patches that clip to 1.00 (that is your blownPct) with orange speckle in a completely unrelated hue family scattered through it, and the shadowed faces carry no material at all — flat matte lavender.

Ground material is one octave of Worley cells doing all the work: pale rings with dark halos, reading as camouflage or lichen, not soil. To its credit it is honestly world-space and it does grow toward camera. It is also the only thing there — no macro breakup, no near-field grain.

Palette compliance, measured against the bible line by line: mountain lit sat 0.287 against a 0.08-0.18 ceiling and hue 1.8 deg against a spec'd ~30 deg, so the rock is pink. Grass #777740, hue 59.8 deg, sat 0.455 — that is yellow, 22 deg off #5C7A3A and over the 0.42 ceiling; highlighter, not grass. Forest #647548 at value 0.462 against a 0.40 ceiling. Ocean #5b6387 at hue 232, sat 0.368, value 0.551 against #123A63 — periwinkle, half the saturation, 40% too bright. Coast hue 203 against a spec'd 194. And plains #b38f6b hue 27.7 sat 0.403 versus desert #b3916c hue 27.8 sat 0.402: those two biomes are now the same colour to three decimal places. The bible spec'd plains at hue ~55. A player cannot tell plains from desert, which is a gameplay read failure rather than a look note.

Water is the single worst surface. MID/HF 0.64 means noise is beating structure two to one — no swell, no crests, no directional chop, no foam with shape, just a dense cornflake crumple near shore and RGB dither at the horizon. The rivers are worse: a flat cyan alpha ribbon laid over the terrain with the grass texture visible straight through it and a hard aliased edge where it crosses land.

Grounding is inconsistent rather than absent, which is harder to fix and worse to look at. Contact delta on the rock prop at (1262,842) is +38 luma; its identical sibling at (1226,880) is +5. Same prop, same tile, one grounded and one pasted on.

And the city nameplate "Calyx" at (600,283) is an unreadable smear — a world-space quad resampled then TAA-blurred. "Azrodo" at (770,588) the same. Nameplates are UI; they do not go through the world pipeline.

Unlabeled and side by side with a real Civ VI screenshot, the player picks Civ VI, immediately, and it is not close. The single tell is shadow colour. Civ VI keeps every shadow in the same hue family as the surface it falls on, so the map reads as one place under one sun. This build's shadows swing 72 degrees into violet while the lit faces stay warm tan, so every tile looks like it has a purple gel over half of it and the frame splits into two unrelated hue families — acid chartreuse and periwinkle — that no time of day produces. Fix the ambient before you touch anything else on this list; it is one term, and it is costing you the whole image.

**Problems**
- Violet ambient breaks the palette globally: mountain lit hue 1.8 deg vs its own shadow hue 290 deg = 72 deg split against the bible's 10 deg budget. Ocean lands at hue 232 sat 0.368 val 0.551 vs spec #123A63; coast hue 203 vs 194. This is the bible's 'never navy shadows on tan sand' clause violated in purple, and it is the single side-by-side tell against Civ VI.
- AUTOMATIC FAIL - illegible hex grid. Zero hex edges anywhere on the mountain massif even at 2.2x contrast boost on the top-left 420x280 region. Mid-field perpendicular delta is 34/255 on mean-126 sand (~13% local contrast); far-field on rock it is 0. Bible non-negotiable #1, and a whole biome the player cannot click.
- AUTOMATIC FAIL - per-pixel noise standing in for material. 1px chroma/luma is 0.42 far cliff, 0.47 near sand, 0.58 far ocean, 0.48 near ocean, against 0.19 on your own UI panel. A near-constant chroma-noise fraction at every depth in every biome is a screen-space grain pass, and it is what the passing land HF numbers are actually scoring.
- AUTOMATIC FAIL - detail does not shrink with distance / aerial perspective inverted. Matched material: sand (900,430) value 0.494 hue 326.6 deg vs sand (760,760) value 0.554 hue 21.1 deg. Far is DARKER and magenta. Bible requires farther = lighter, cooler, less saturated, never darker. The 1.68 ramp passed only because the far probe sat on rock and the near probe on grass.
- AUTOMATIC FAIL - water is noise, not material. Gate: HF_rms 15.04 > 15 and MID/HF 0.64 < 0.9, so noise beats structure 2:1. No swell, no crests, no directional chop, no shaped foam - cornflake crumple inshore, RGB dither at the horizon. Rivers are a flat cyan alpha ribbon with the grass texture visible through it and a hard aliased edge on land.
- Cliffs are naked hex prisms with stretched UVs. Flat trapezoidal sidewall, hard crease, flat cap, tile after tile; the top-down projection smears into horizontal stripes down every vertical face (missing triplanar). No strata, no dip, no talus, no scree - walls meet ground on a perfect line. Shadowed faces are untextured flat matte, and the cream caps clip to 1.00 (blownPct 0.06 > 0.05).
- Biome colour has collapsed and gone garish. Plains #b38f6b hue 27.7 sat 0.403 vs desert #b3916c hue 27.8 sat 0.402 - identical, so the player cannot tell them apart (a gameplay read failure). Grass #777740 hue 59.8 sat 0.455 is yellow, 22 deg off #5C7A3A and over the 0.42 ceiling. Forest value 0.462 over its 0.40 ceiling. Mountain sat 0.287 vs a 0.08-0.18 ceiling.
- Grounding inconsistent and nameplates unreadable. Contact delta +38 luma on the rock prop at (1262,842) but +5 on its identical sibling at (1226,880) - same prop class, same tile, one grounded and one pasted on. 'Calyx' at (600,283) and 'Azrodo' at (770,588) are TAA-smeared world-space quads; both illegible.

**Fixes**
- Fix the ambient first - it is one term and it costs you the image. Make it albedo-weighted as the bible already specifies: ambient = skyIrradiance * mix(vec3(1.0), albedo, 0.65), and rotate the sky/ambient SH from its current ~hue 250 to hue 215 at half its present saturation. Acceptance: per material, shadow hue within 10 deg of lit hue - mountain shadow must land hue 355-12 deg (currently 290) with sat 0.08-0.18 (currently 0.242).
- Delete the screen-space grain pass. If you need dither, make it luma-only at <= 1.5/255 applied pre-tonemap, never chroma. Acceptance: 1px chroma/luma <= 0.22 on every terrain probe at every depth, measured against the 0.19 you already hit on the UI panel.
- Rebuild water from structure, not hash. Three Gerstner octaves at world wavelengths 22 m / 7 m / 2.4 m, amplitudes 0.90 / 0.28 / 0.07 m, sampled from a mipped normal map at LOD bias 0 with aniso 8; fade the 2.4 m octave out over 40-110 m so the horizon is swell only. Remove the per-pixel hash entirely. Target: water HF_rms 9-12, MID/HF 1.05-1.20. Depth-blend the rivers (soft-particle style, 0.6 m fade) so they stop reading as alpha decals over grass.
- Replace the multiply fog with additive in-scatter: colour = colour * exp(-sigma*d) + inscatter * (1 - exp(-sigma*d)), sigma ~ 0.0055 /m, inscatter = horizon sky at hue 210, sat 0.14, value 0.72. Kill the magenta tint. Acceptance on matched material: far sand value >= near sand value + 0.08, far sat <= near sat - 0.06, far hue within 15 deg of near hue (currently value -0.06 and hue 305 deg apart).
- Give the hex stroke a guaranteed absolute contrast instead of a multiply against albedo. Enforce a minimum linear-luma delta of 28-40/255 perpendicular to the edge with a hard floor, so it survives dark violet rock and bright acid grass alike; world-space width with a 1.25 px screen-space floor so it never sub-pixels at the horizon; analytic fwidth coverage AA, not a texture. Acceptance: measurable 28-40/255 delta on every biome including slopes >= 30 deg.
- Triplanar the cliff material at blend sharpness 4.0 so vertical faces stop wearing stretched top-down UVs, then add strata: world-Y banded albedo and normal at 1.1 m spacing, +/-0.25 m per-band jitter, 12 deg dip. Break the base with 6-10 talus boulders per cliff edge inside a 2.5 m skirt plus a scree ramp, so the wall never meets the ground on a straight line. Drop rock albedo to 0.42-0.48 to clear blownPct and to get mountain value back inside 0.32-0.62.
- Stop letting one Worley octave carry the ground. Cut the existing cell layer's contrast by ~40% (it currently owns the entire MID band), then add a macro breakup at 18 m wavelength driving albedo VALUE only by +/-0.06 with no hue shift, and a near-field detail octave at 0.35 m that fades out past 25 m. Keep it world-space and mipped - that part you already got right, ramp 1.68.
- Separate plains from desert again. Pull plains to hue 52-58, sat 0.30, value 0.50 (spec #97914E); leave desert at hue ~28 (spec #C6A874, sat 0.24-0.34 - it is currently at 0.402, so bring it down too). Acceptance: >= 20 deg hue separation between the two biomes.
- Cut global saturation by 0.12 and rotate vegetation albedo back to hue 78-85. Targets: grass to #5C7A3A, sat 0.30-0.42 (currently hue 59.8, sat 0.455); forest to #3C5E31, value 0.20-0.40 (currently value 0.462).
- Move nameplates out of the world pipeline: render 'Calyx' and 'Azrodo' as screen-space UI at native resolution, snapped to whole pixels, excluded from TAA, fog and DOF. Separately, give every prop a guaranteed AO decal at 1.4x footprint radius, opacity 0.55, that darkens the ground by >= 22 luma at the contact point - and find why the decal drops when a prop straddles a tile seam (that is the difference between +38 at (1262,842) and +5 at (1226,880)).

## critique 11 — 34/100
REJECT. Gate FAIL (water MID/HF 0.85 < 0.9), plus six separate automatic fails. Judged against your own bible, not my taste.

MEASURED, cited boxes: far-cliff mean 122.4 / sat 0.301 / HF 12.97 / MID-HF 0.95 | mid 82.4 / 0.357 / 14.49 / 0.98 | near-field 101.7 / 0.419 / 20.91 / 1.25 | water 98.4 / 0.366 / 13.91 / 0.85. nearFarHFRamp 1.61 against a 1.60 floor — 0.6% margin. blownPct 0.05 against a 0.05 ceiling — zero margin. crushedPct 0.05, fine.

Those four boxes are the only place this frame works. I swept sixteen evenly-spaced 200x140 land boxes: 2 of 16 pass. The far band (y=100) runs HF 5.76-13.73 and sat 0.246-0.400; the near band (y=550) runs HF 18.7-25.2 with two boxes over the 22 ceiling. Nudge your four boxes 100-300px and you get five failures at once: far-grass sat 0.259 and MID/HF 0.87, near-sand MID/HF 1.45, coast MID/HF 1.67. This is a frame tuned to four coordinates, not a converged look.

GRID LEGIBILITY — the automatic fail. On open sand at mid depth (900-1160, 380-560) the stroke is genuinely good: single, ~3px, warm dark brown, conforming to the surface. Everywhere else it is gone. On grass at 8x it is a broken, wobbly 1px squiggle at 6-8 luma against an L=86 base, under 8% contrast, dashing in and out. In the far band it does not exist: a 4-row-averaged scanline at y=170, x=420-510 spans 79-104 with a maximum dip of 8 luma and no periodic structure at all. I cannot tell you which tile Aurelia occupies, which tile the river crosses, or count tiles anywhere in the northern half. It also violates two clauses of your own non-negotiable #1: it does not fade over water, it hard-terminates mid-edge at the shoreline vertex near (998,474); and it is not one stroke per edge — parallel double strokes around (440-475, 430-480) are per-tile inset borders. For a game whose entire input model is "click the tile," this alone ends the review.

AERIAL PERSPECTIVE — running nowhere, and backwards in chroma. Matched material, sunlit rock at (205,115) vs sunlit sand at (855,518): #c0a078 hue 33.3 val 0.754 vs #bf9b74 hue 31.1 val 0.749. Delta value 0.005. Delta hue 2.2 degrees. Delta (R-B) is 3 out of 255. There is no aerial perspective. The gate's "far is lighter and less saturated" is bought entirely by aiming the far box at pale rock and the near box at dark vegetation. The only thing that changes with distance is the shadow colour, and it changes the wrong way: mountain shadow #756168 reads hue 338.1 against a lit hue of 33.3 — a 55.2 degree swing, 5.5x your 10-degree allowance. Your bible names navy-on-tan as the sin; this is the identical sin rotated into magenta. Meanwhile far grass at (430,150) measures sat 0.147 and a grass sample at (560,470) reads hue 320.5 at sat 0.062 — green ground rendering as grey-violet. The near sand lit/shadow pair is 31.1 vs 33.4, a 2.3-degree match, which proves the shading model can do this. It breaks on distant and steep surfaces only.

CRUSHED SHADOWS — not clipped, but worse. L<4 is 0.05%, L<12 is 0.228% of non-UI pixels. Nothing is welded to black. The problem is that the shadows are crushed to flat, not to black: the deep band at (300,680,90,30) sits at #3b3325 val 0.232 with no bounce and no material inside it, and that far-field 90px profile at y=170 varying by 8 luma total is a shadow region with zero separation. For a strategy game that is more damaging than clipping, because those tiles are unreadable rather than merely dark.

VIGNETTE HOTSPOT — there is no vignette, and there is an uncontrolled hotspot. UI-masked 100px luma grid: the left frame edge reads 96-121 top to bottom while the interior at x=300-700 reads 78-105 — the edge is brighter than the middle, so nothing is holding the frame. And there is a +50% specular lobe at (1000-1300, 400-600) reading 137-165 against a 78-110 field, centred around (1150,500). Ocean scanline at y=355-385 confirms it: L=148 at x=1070 falling to L=100-103 by x=1390-1510. That lobe lands squarely on the coastline and washes it out: the coast band at (950,430) measures #8b8291, hue 276.2, sat 0.101 — lavender-grey where your bible specifies coast #2E7C93, hue 194, sat 0.68. That is 82 degrees of hue error and 0.58 of saturation on the one element the bible lists under "already good — keep, do not rewrite."

REST OF THE AUTOMATIC FAILS. Per-pixel chroma noise: pure orange, red and magenta single pixels scattered over the violet wet-sand band and over green grass throughout (800-1100, 270-430) — nothing physical there is orange, and your own gate agrees at water MID/HF 0.85. Untextured flat-matte: every shadowed mountain face is one solid lavender polygon across hundreds of pixels with no grain, no normal detail, no cavity; the Aurelia keep's tower faces are flat tan rectangles. Props without contact shadow: the white shore pebbles, the red-roof huts at (830-890, 600-635), the tree clusters at (590-660, 570-660), the hut at (964,544), every mountain tree — all stickers. Toy/programmer-art silhouettes: the Warrior portrait is a minifig with a black bar for eyes and a 1px untapered spear; the field unit at (975,682) is a black disc with a chess-pawn glyph, not a model; the three leader portraits at the top are the identical faceless mannequin head three times, distinguished only by background tint. Streak artifacts: a translucent grey-lavender crescent decal at (830-1010, 290-380) floating across the coastline and out over open water with 8-16px blocky stair-stepping on its upper edge — a quarter-res buffer point-upsampled — plus stretched geometry streaks across the mountain slopes and black mesh holes near (80,95) and (103,155). Detail not shrinking with distance: the diamond-scale stamp on the mountains and the dash pattern on the ocean hold the same screen size at every depth; even on matched material the ramp is only 1.65, where a properly mipped material lands 2.5-3.5.

WHAT ACTUALLY WORKS, and I will say it plainly. The HUD is shippable: unit panel, notification rail, END TURN, minimap — typography, hierarchy, the disabled state on FOUND, gold on charcoal, no clipping, no overlap anywhere. The Aurelia keep reads: red roofs, tower, wall, banner, bridge. The grid stroke on open sand at mid depth is correct. The near-field sand lit/shadow hue pair at 2.3 degrees is exactly right. Someone here knows what they are doing; the frame is not being finished.

THE QUESTION, ANSWERED LITERALLY. Unlabeled, side by side with a real Civ VI screenshot, the player picks Civ VI, and it takes them under a second. The single tell is the tile boundaries. In Civ VI the hex edge is an authored line with consistent weight that survives every terrain type, so the board reads as a board at a glance. Here the grid exists on bare sand and dissolves across the entire green half of the map. The player will not consciously think "the grid is inconsistent" — they will think "I can't tell where I'd click," and they will pick the other image. If you fixed the grid tomorrow, the next tell would be the three identical faceless leader portraits in the top bar.

**Problems**
- Hex grid is illegible on everything except bare sand — 6-8 luma delta on grass (under 8% contrast, dashing in and out at 8x), and a 4-row-averaged scanline at y=170 x=420-510 spans 79-104 with a max dip of 8 luma and no periodic structure at all. Also breaks two more clauses of non-negotiable #1: hard-terminates mid-edge at the shoreline vertex near (998,474) instead of fading over water, and draws parallel double strokes around (440-475, 430-480) instead of one stroke per edge. Automatic fail.
- Zero aerial perspective on matched material. Sunlit rock (205,115) #c0a078 hue 33.3 val 0.754 vs sunlit sand (855,518) #bf9b74 hue 31.1 val 0.749 — delta value 0.005, delta hue 2.2 deg, delta (R-B) 3/255. The gate's far-is-lighter pass is an artifact of pointing the far box at pale rock and the near box at dark vegetation.
- Shadow hue violates the bible by 5.5x. Mountain shadow #756168 hue 338.1 against lit hue 33.3 = 55.2 degree swing where 10 is the limit. Magenta shadows on tan rock — the same sin as navy-on-sand, rotated. Bleeds into the terrain generally: grass at (560,470) reads hue 320.5 at sat 0.062.
- Per-pixel chroma confetti standing in for material — pure orange, red and magenta single pixels over the violet shore band and over green grass throughout (800-1100, 270-430). Gate agrees: water MID/HF 0.85 < 0.9. Automatic fail.
- Coast is lavender, not teal. (950,430) measures #8b8291 hue 276.2 sat 0.101 against the bible's coast #2E7C93 hue 194 sat 0.68 — 82 degrees of hue error on the one element listed as 'already good, do not rewrite'. Driven by an uncontrolled +50% specular lobe at (1000-1300, 400-600) reading 137-165 against a 78-110 field, and there is no vignette to contain it: the left frame edge reads 96-121 while the interior reads 78-105.
- Nothing is grounded and nothing is textured. Shore pebbles, the red-roof huts at (830-890, 600-635), tree clusters at (590-660, 570-660), the hut at (964,544) and every mountain tree have no contact shadow. Every shadowed mountain face is a single solid lavender polygon with no grain, normal or cavity; the keep's tower faces are flat tan rectangles. Two automatic fails.
- Toy/programmer-art unit art. The Warrior portrait is a minifig with a black bar for eyes and a 1px untapered spear; the field unit at (975,682) is a black disc with a chess-pawn glyph rather than a model; the three leader portraits are the identical faceless mannequin head, separated only by background tint. Automatic fail.
- The gate pass is a coordinate lottery. 2 of 16 evenly-spaced land boxes pass: far band HF 5.76-13.73 and sat 0.246-0.400, near band HF 18.7-25.2 with two over the 22 ceiling. Move the four cited boxes 100-300px and you get far-grass sat 0.259, far-grass MID/HF 0.87, near-sand MID/HF 1.45, coast MID/HF 1.67. Ramp clears its floor by 0.6% (1.61 vs 1.60) and blownPct sits exactly on its ceiling at 0.05.

**Fixes**
- Grid: stop multiplying into albedo — that is why it dies on dark terrain. Compute signed world-space distance d to the nearest hex edge, w = fwidth(d), a = 1 - smoothstep(0, 1.5*w, d - halfWidth) with halfWidth = 0.045 * hexInradius, and clamp the resulting screen width to [1.4px, 3.2px]. Composite bidirectionally: darken to base*0.55 where luma(base) > 0.35, lighten to base+0.18 where it is below, at a*0.85. Acceptance: minimum edge contrast >= 14 luma against the local 15px median on EVERY terrain type — grass currently measures 6-8, the far band measures 0 over 90px.
- Grid over water: replace the hard clip at the shoreline (visible mid-edge at ~(998,474)) with a *= 1 - smoothstep(0.0, 1.0, waterDepth/1.2m). Grid dedupe: draw only the 3 edges of each hex whose neighbour index is greater, or move the whole thing to one full-screen pass using world position reconstructed from depth — that kills the double strokes at (440-475, 430-480).
- Add real aerial perspective, in linear space before tonemap: col = lerp(col, fogCol, 1 - exp(-d*0.0016)) with fogCol at 6500-7000K (~#A8BCD0), plus sat *= 1 - 0.35*fogAmount. Tune so matched sunlit rock at the far cliff lands +0.10 value, -0.09 saturation and -45 in (R-B) relative to the same material near-field. Currently those deltas are 0.005, -0.018 and +3.
- Fix the far albedo BEFORE applying that fog, or you will push far-grass from sat 0.147 further under the 0.28 floor. The mips are box-filtering the green stamps against the dark base in sRGB and greying out the chroma. Generate the mip chain in a perceptual space with chroma-preserving weights so far grass sits at 0.34-0.38 pre-fog and 0.28-0.32 post-fog.
- Shadow hue: the ambient term is a constant cool-violet added independently of albedo, which is what produces the 55.2 degree swing on rock. Replace with the bible's albedo-weighted form: ambient = skyColor*albedo*0.35 + groundBounce*albedo*0.20, and if the SH probe still misbehaves, post-clamp shaded hue to lit hue +/-10 deg. Acceptance: every material's lit/shadow pair within 10 degrees, matching the near-sand pair already at 2.3.
- Kill the chroma confetti: the detail-normal and detail-albedo layers are being sampled with no explicit LOD and then post-sharpened. Use textureGrad or explicit LOD with lodBias +0.5, cap the detail-albedo saturation contribution at 0.08, and drop post-sharpen amount below 0.15. Acceptance: water MID/HF from 0.85 to >= 1.05, and land HF inside 14-19 across all 16 sweep boxes, not 5.76-25.2.
- Water specular: floor roughness at 0.06 and clamp the sun-glitter NDF with min(spec, 8.0) pre-tonemap so the (1080,460,150,120) box drops from val 0.668 / L~164 to L~125-135. Then add the missing vignette at 0.18 strength, 1.6 falloff exponent, frame-centred, so the corners read 0.82x of the interior instead of the current 1.15x.
- Ground every prop: screen-space contact shadow pass at 8 steps, 0.35m max trace, 0.05m thickness — sufficient at this camera pitch. Plus an AO decal disc for any prop with a footprint over 1m: radius 1.15x the footprint, 0.55 centre opacity falling to 0 at the rim, multiplied into albedo. Verify on the shore pebbles, the huts at (830-890, 600-635) and the trees at (590-660, 570-660).
- Give the rock an actual material instead of albedo x N.L: triplanar rock albedo at 1.5 m/tile with a 0.25 m detail layer, roughness 0.72 +/- 0.12 driven by a cavity map, and a normal map — that is why the facets read as crumpled paper. While you are in that mesh, fix the black holes near (80,95) and (103,155), which are open faces or a backface-cull error.
- Bind the mountain scale-stamp and the ocean dash pattern to world-space UVs — both currently hold the same screen size at every depth. Re-measure the ramp on MATCHED material only: 1.65 today, target 2.5-3.5. And fix the (830-1010, 290-380) crescent decal: its 8px stair-step period at 1600x900 means a quarter-res buffer point-upsampled — render it at full res, or add a 1.5px depth-guided bilateral upsample and depth-test it so it stops floating over open water.

## critique 12 — 31/100
REJECT. Gate FAIL on your own boxes ("water: MID/HF 0.84 < 0.9", "near/far HF ramp 1.57 < 1.6"), and that 0.84 is a charity number — the box 1150,200,240,160 is half notification rail, and the rail's sharp text is the only thing propping the HF up. On clean water-only samples it is worse in the other direction: water-far 980,90,240,100 HF_rms 1.74 (target 7-15) MID/HF 1.94; water-mid 1140,330 HF 7.16 MID/HF 2.05; water-near 1250,440 HF 10.36 MID/HF 1.77; water-shallow 1005,405 HF 9.73 MID/HF 2.33. Every water band busts the 1.3 ceiling. Your ocean is all blob and no material.

Wave structure vs noise: at 6x (crop 1150,350,140,90) the sea is ONE dash sprite — same comma shape, same ~30 deg angle, same length — tiled edge to edge, with hard cyan 1px rims and orange/red single-pixel confetti riding on top. Chroma-speckle (isolated opponent-channel outliers >28) is 2.00% of near-ocean pixels, 3.52% on land. That is per-pixel noise standing in for material. Automatic fail. Worse, horizontal autocorrelation puts the secondary peak at lag 14px in the far ocean (980,90) and 17.5px in the near ocean (1250,440) — 1.25x over a depth range that should be 2x+. The wavelength does not shrink toward the horizon; only the contrast does (HF 1.74 far vs 10.4 near). You are handling distance by blurring the same texture instead of tiling more of it. Automatic fail on detail-does-not-shrink, and the far band at HF 1.74 is a flat untextured gradient — a third automatic fail.

Specular: there is none. Highlight coverage (lum>190) is 0.00% across the entire upper ocean (y60-180) and climbs to 40.2% at the shore (y420, x1040). Shadows fall down-right, so the sun is upper-left — your bright zone is anti-correlated with the sun. That is not a glitter path, it is a shore/depth brightness ramp with foam in it. Related: far water measures lum 109.9 vs near 121.8, so farther is DARKER. The bible says never darker.

Shallow caustics: absent. Shallow strip (1005,415,70,30) HF_rms 8.10 vs deep strip at the identical screen row (1300,415,70,30) HF 7.59 — a ratio of 1.07. No caustic web, no seabed read, no depth hue. The shallow band is just a grey fog wash: rgb(159,164,173), sat 0.289, against a spec of coast #2E7C93 = (46,124,147). And it is the blobbiest region in frame at MID/HF 2.33.

Rivers as channels: they are not channels, they are alpha decals. Cross-section at y=545 goes bank lum 63 -> river lum 98: the river is BRIGHTER than its banks, so it floats. No incision, no bank slope, no waterline AO, no thalweg darkening; terrain reads straight through it at ~50% alpha (crop 540,480,220,140), and it passes under a tree that casts nothing on it. Mean colour (72,84,106) vs bible river #2C6E86 = (44,110,134) — green 26 low, red 28 high, hue off by ~22 deg into periwinkle. Inside that 20px ribbon sit 4-6px blobs at lum 211 over a body at lum 90.

Grid bleed-through is the one thing you got right: scanlines at y=200/370/500 over open ocean show no periodic 130-160px stroke, only wave dashes. The grid fades over water per spec. It is also the only item in this pass that passes.

Two more that this water pass drags in with it. The near-field bay (860,660,200,140) is flat-shaded polygons — hard straight seams, abruptly different tones, zero wave normal; the water tessellation is showing. And the coastline, which the bible lists as ALREADY GOOD, has regressed to a raw polygon cut: 152 lum in a single pixel at y=465, 119 at y=460, with sharpen ringing at y=450 (124 -> 79 -> 141). No analytic AA, no wet band, foam is a static 15-31px white slab that stops dead at the edge. Plus a translucent grey-green glass ribbon lying across the coast at roughly (820,300)-(1000,400) that reads as a straight rendering bug.

Unlabeled beside a real Civ VI shot, the player picks Civ VI in under a second, and the single tell is the ocean: Civ VI's water has a coherent swell that gets finer toward the horizon with a sun-anchored glitter track, and yours is a field of identical 15px commas at one angle that fades to flat grey-blue with red confetti on it. The player will not say "screen-space noise" — they will say the water looks like a texture the camera is dragging around. Everything else in this frame (HUD, keeps, terrain silhouette) is competent enough that the water is the only thing that gives it away, which should tell you where the whole pass went.

**Problems**
- AUTOMATIC FAIL - per-pixel noise as material: 2.00% chroma-speckle in near ocean, 3.52% on land (isolated >28 opponent-channel outliers). Orange/red/cyan 1px confetti visible at 6x in BOTH far (1020,95) and near (1250,440) ocean, identical size at both depths.
- AUTOMATIC FAIL - detail does not shrink with distance: wave ACF secondary peak 14px at y=90 vs 17.5px at y=530 (1.25x) over a 2x+ depth range. gate reports nearFarHFRamp 1.57 < 1.6. Distance is faked with blur, not with finer tiling.
- AUTOMATIC FAIL - untextured flat-matte surfaces: far ocean band (980,90,240,100) HF_rms 1.74 against a 7-15 target, a dead gradient; and the near bay (860,660,200,140) is flat-shaded facets with hard seams and no wave normal at all.
- No specular. Highlight coverage lum>190 is 0.00% for the entire upper ocean y60-180 and 40.2% at the shore — brightness is anti-correlated with the sun (shadows fall down-right, sun is upper-left). It is a depth ramp, not a glitter path.
- No shallow caustics: shallow HF 8.10 vs deep HF 7.59 at the same screen row = 1.07x. Shallow band is rgb(159,164,173) grey fog, not coast #2E7C93 (46,124,147), and is the blobbiest region in frame at MID/HF 2.33.
- Rivers are alpha decals, not channels: river lum 98 vs bank lum 63 (it floats), no incision, no waterline AO, terrain visible through at ~50% alpha, mean rgb(72,84,106) vs spec #2C6E86 (44,110,134).
- Every water band busts the MID/HF ceiling on clean samples: 1.94 / 2.05 / 1.77 / 2.33 against a 0.9-1.3 target. All blob, no pixel-scale material.
- Coastline regressed from the bible's locked-good list to a raw polygon cut: 152 lum in one pixel at y=465, sharpen ringing at y=450 (124->79->141), no wet band, static foam slab. Plus a translucent glass ribbon artifact across the coast at ~(820,300)-(1000,400).

**Fixes**
- Replace the single dash layer with a 3-octave world-space Gerstner stack: wavelengths 48m / 17m / 6m, amplitudes 0.90 / 0.28 / 0.09m, steepness Q 0.55 / 0.35 / 0.20, directions spread +/-35 deg around one wind vector (you currently have every crest at one angle). UV = worldXZ / 12.0, never screen-derived; 512^2 normal map, aniso 8x, mip bias capped at +1.0. Acceptance: ACF secondary peak 14px at y=90 -> 26-30px at y=530 (>=2.0x), and water-far HF_rms 7-10 instead of 1.74.
- Kill the confetti with geometric specular AA: roughness' = sqrt(rough^2 + 2*|grad n|^2), clamped to 0.60, plus roughness = lerp(0.02, 0.35, saturate(mip/5)). Clamp pre-tonemap radiance to 8.0 before TAA so single HDR spikes stop leaking chroma fringes. Acceptance: near-ocean chroma-speckle from 2.00% to <0.30%.
- Add a real sun lobe: GGX roughness 0.06-0.10, solar disc angular radius 0.27 deg, Schlick F0 = 0.020 with full Fresnel at grazing so the far band brightens. Acceptance: lum>190 coverage becomes 3-8% in the UPPER-LEFT ocean (toward the sun) and <1% in the lower-right — the exact inverse of the current 0.00% / 40.2% distribution.
- Caustics: two scrolling worley layers at world scale 3.5m and 1.2m, additive 0.35 on the seabed albedo, attenuated by exp(-depth/2.4m) so they die past ~6m. Acceptance: shallow/deep HF ratio 1.8-2.4, up from 1.07.
- Shallow colour: drive the transmitted term as lerp(#2E7C93, #123A63, saturate(depth/9m)) and composite foam ON TOP rather than replacing water with white. Acceptance: the 1005,405 band moves from rgb(159,164,173) sat 0.289 to roughly (60,120,140) sat 0.34-0.40.
- Shoreline as an SDF blend, not a polygon cut: alpha = saturate(0.5 - sdShore/fwidth(sdShore)). Add a 1.2m wet-sand band (albedo x0.62, roughness 0.15). Acceptance: the land->water lum transition at y=465 spans 4-6px instead of 152 lum in one pixel, and the y=450 ringing dip (124->79->141) disappears.
- Foam driven by the SDF and wave phase: width 0.8m + 0.6m*|waveHeight|, so it pulses and wraps headlands instead of sitting as the current static 15px (y400) / 31px (y480) white slab.
- Rivers: carve them into the heightfield before shading — 0.8-1.4m depth along the spline, 6m trapezoidal section, 35 deg banks; fit the water plane to the carved bed. Add a 0.6m waterline AO band at 0.55. Depth-darken with lerp(#4E8FA0, #1C4E63, saturate(depth/1.5m)). Clamp river specular: roughness floor 0.12, sun-lobe max 1.6x body luminance (currently 4-6px blobs at lum 211 over a body at lum 90). Acceptance: river lum sits BELOW its banks (target ~50 vs bank 63) and mean colour lands within 8 units of #2C6E86.
- Near-field water: force smooth vertex normals and apply the same world-space normal chain on EVERY water LOD (the bay at 860,660 is per-face shaded), and cap water edge length at 1.5m inside 40m of camera to remove the visible facet seams.
- Fix aerial perspective on water and the sand shadow hue in the same pass: apply inscatter (#9FB4C8, strength 0.55 over 220m) AFTER specular so the glitter survives, targeting far water lum 135-150 / sat 0.30-0.34 instead of the current 109.9 (darker than the near field's 121.8). Build the shadow term as albedo*(ambientSky*0.35 + albedo-weighted bounce*0.65) instead of the flat violet currently swinging coastal sand from lit hue 23.0 deg to shadow hue 356.7 deg — a 26 deg rotation against the bible's 10 deg cap.

