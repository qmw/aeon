# Phase 5 critic findings — unfixed backlog

## critique 1 — 37/100
Reject. Side by side with a real Civ VI in-game screenshot, unlabeled, the player calls Civ VI better inside half a second — and the single tell is that nothing here is standing on the ground: every hut, tree, bush, farm plate and unit sits on the terrain with zero contact darkening, so the whole map reads as cutout props pasted onto a photo of dirt. Everything else follows from that first read. Second look confirms it: the terrain has noise where texture should be. I measured the high-pass sign-flip rate (fraction of adjacent pixels where the detail signal changes sign — correlated material texture runs 0.15-0.25, uncorrelated white noise runs 0.50) and got 0.36 on deep ocean, 0.37 on near sand and grass, 0.39 on dry flats, 0.47 on the cliff faces. The cliff material is statistically indistinguishable from white noise; that is why it looks combed/hatched rather than stratified. Meanwhile MID/HF sits at 0.66-0.96 across every ground region — a shipping AAA ground shader lands 1.0-1.2 because its variation lives in painted macro shapes with grain on top, and yours is inverted: grain carrying the whole material with no macro structure under it. Then the LOD ramp runs backwards. Detail energy by depth band: far 16.5, mid 27.6, near-field 14.5. The band closest to camera, with the largest texels and the selected unit in it, is the flattest and darkest part of the frame (mean 85.7 vs 128.9 mid), and 0.46% of the image is crushed below L=4. Civ VI at gameplay zoom does the opposite — near hexes gain gravel, cracks and colour breakup. Beyond that: the ocean is per-hex flat-tinted with a hard hexagon silhouette visible in open water (ocean-shelf HF 6.9 = a flat plate, ocean-deep hp_rms 11.3 = unconverged speckle on top of it); the hex grid over water is 1px aliased, dashing and doubling along diagonals; the beach is a near-untextured cream matte whose boundary follows the hex chord exactly; the city, huts and units are flat-matte vertex-shaded solids with an ink outline, the unit groups reading as featureless tan eggs on sticks. Five automatic fails hit independently. Composition, coastline shape and the UI frame are competent — that is the only reason this scores in the 30s and not lower. It is not close to shippable.

**Problems**
- AUTOMATIC FAIL - props with no contact shadow. Trees at ~(1110,692), the round bushes at ~(773,813), the hut cluster at ~(600,247), the dock props and every unit group have no contact bed and no cast shadow in the sun direction. The village huts instead sit on a literal flat grey elliptical disc, which is grounding-by-programmer-art, not shading.
- AUTOMATIC FAIL - untextured/flat-matte surfaces. City keep walls, maroon roofs, hut cones, farm plate walls, harbour district blocks and the beach are single-value fills with only facet shading. The beach sand at the coast is a flat cream mass with no grain at all. This is greybox with vertex colour.
- AUTOMATIC FAIL - toy/programmer-art unit silhouettes. The unit stack at (600,790)-(700,890) is featureless tan spheres with stick spears and a flat blue banner, wrapped in a uniform hard black outline. At gameplay zoom they read as eggs or mushrooms, not soldiers.
- AUTOMATIC FAIL - visible noise/streak artifacts. High-pass sign-flip 0.36-0.47 across all ground and water means the detail is uncorrelated per-pixel noise, not texture. The cliff faces at 0.47 are effectively white noise, which is what produces the combed/hatched streaking on every rock face. Ocean carries hp_rms 11.3 of salt-and-pepper speckle that TAA has not resolved.
- AUTOMATIC FAIL - stair-stepped and double-drawn hex lines. At 6x nearest on the water at (1080,300)-(1240,420) the grid is a 1px un-antialiased dark polyline that steps, dashes and doubles along the diagonals where two hexes each draw their shared edge.
- LOD ordering is inverted. HF by depth band: 16.5 far, 24.1, 27.6, 27.0, 23.3, 14.5 near. Peak detail is in the middle of the screen and the near field is the flattest band in the frame, at 53% of mid-band energy. Nothing gains material as it approaches camera.
- No colour script, no aerial perspective. Value runs dark-bright-dark top to bottom (81 / 129 / 86) so the interaction zone is the darkest, lowest-detail region. Distance is merely darker, not desaturated or shifted toward sky hue (far grass sat 0.363 vs mid 0.433, no hue shift), so depth reads as shadow rather than atmosphere. 0.46% of pixels crushed under L=4.
- Ecotones and water bodies are hex-shaped. Sand-to-grass and sand-to-water transitions follow the hex chord as a straight cut with a constant-width airbrush blend, and open ocean shows a hard hexagon boundary between shallow and deep tint. Foam ribbons float across open water disconnected from any shoreline. Grass is blotchy at sat 0.433 with a visible repeating light-green mottle.

**Fixes**
- Rebuild ground variation as a macro stack instead of one fixed-frequency noise: octave A at 24m wavelength driving albedo value +/-18%, octave B at 6m at +/-10%, and a detail albedo tiled at 0.5m blended in only inside 12m with a 6-12m fade. Acceptance: sign-flip drops from 0.36-0.47 to 0.18-0.24 and MID/HF rises from 0.66-0.96 to 1.05-1.20 in every ground region.
- Bind detail-albedo and detail-normal strength to view distance with a linear ramp 1.0 at 8m to 0.0 at 40m, and stop letting the near band fall into shadow. Acceptance: HF by band must run monotonically 26-30 near, 22-26 mid, 14-18 far - currently 14.5 / 27.0 / 16.5.
- Give every prop instance a contact decal: radial gradient at 1.4x footprint radius, multiply-blended, alpha 0.55 at centre to 0.0 at edge, plus a real cast shadow along the sun vector. If the shadow cascade cannot reach prop scale, add a screen-space contact trace at 12 steps over 0.35m max distance - cheap enough under a software raster. Acceptance: minimum luminance in the 4-8px annulus under a prop is at least 30% below the local ground median.
- Kill the stretched cliff noise. Triplanar-project a 2m-tiled rock albedo with blend sharpness 4.0, add a world-Y strata mask banding every 0.8m at +/-12% value, and carry the surface in the normal map rather than the albedo. Break each hex chord with 3-5 scattered boulder meshes so the silhouette is not a straight line. Acceptance: cliff sign-flip under 0.25.
- Replace the per-hex water tint with depth-based absorption: sample scene depth, Beer-Lambert extinction (0.35, 0.09, 0.05) per metre, shallow tint mixed by exp(-d*0.5). Delete the hexagon plate entirely. Add two scrolling normal maps at 0.9m/3.5m wavelength scrolling 0.04/0.11 m/s so ocean detail comes from wave normals. Acceptance: ocean-deep hp_rms falls from 11.3 to 3-5 and no hexagon edge is visible in open water.
- Draw the hex grid as a signed-distance decal inside the terrain shader at 1.6px screen-space width with smoothstep AA over 1.2px, and dedupe shared edges by winding so each edge is rasterised once - the doubling on water is both hexes drawing the same edge. Drop grid opacity over ocean to 0.25.
- Height-blend the terrain layers instead of cutting at the hex chord: per-material heightmap, blend width 0.6-1.2m, boundary jittered by a 1.5m noise at +/-0.4m so it never traces the hex geometry. Add a wet-sand band 0.8m wide at 22% darker with roughness dropped 0.85 to 0.35, and drive foam off the actual coastline spline rather than a painted decal.
- Fix the colour script: shape cloud shadows at 60-120m masks instead of a horizontal bright band, hold near-field mean at 0.9-1.1x the mid band (currently 0.66x), and add real aerial perspective - desaturate 18% toward sky hue and lift value 12% at max view distance. Raise the ambient floor from L=4 to L=18-24 to recover crushed near-field material.
- Cap ground chroma at sat 0.30-0.35 and carry variation in value, not saturation - mid-grass at 0.433 is what makes the mottle read as garish. Drive the blotch through a 12m macro mask into a two-tone gradient LUT rather than tinting albedo directly.
- Texture the props at minimum with roughness plus cavity AO: thatch roughness 0.9 with a 0.15m thatch normal, stone roughness 0.6 with a 0.08m course normal, cavity AO at 0.35 strength. For units, replace the uniform ink outline with a contrast-conditional rim (only where background luminance is within 15% of the silhouette) and give helmets and shields at least 3 albedo value steps. Replace the grey hut disc with a height-blended dirt decal feathered over 0.5m.

## critique 2 — 36/100
Rejected, and not close. Side by side and unlabeled, a player calls the Civ VI shot better inside a second, and the single tell is this: in your build the water is shaded per hex facet — you can read the hexagon tessellation through the sea and the lake as straight-line brightness steps, and the lake hex at (900,800) measures HF_rms 5.84 / MID_rms 3.97, i.e. a flat-shaded polygon. Civ VI's water is one continuous surface that ignores the hex grid entirely; nothing about its ocean tells you where the tiles are. The runner-up tell is that your open ocean is covered in TV static: deep water at (1380,120) runs MID/HF = 0.54 with 4.01% of pixels above L=150 on a median of 44 — isolated pixel-scale hot dots, uniform density, no glint lobe. That is noise, not waves and not specular. Against the focus brief specifically: wave structure fails (no band-limited swell at any scale), specular fails (no lobe, no Fresnel — the grazing far ocean at mean 55.8 is darker than the near ocean at 72.3, which is backwards), foam fails (stroked along a bathymetry isoline in open sea, not generated at breaking depth), rivers fail hardest (a colour-multiply decal with the ground texture reading through it and tree canopies standing in mid-channel), shallow caustics are entirely absent, and streak/seam artifacts are present in three places — the circular shelf cutout, the straight material seam in the lake, and the doubled hex grid over the coastline. The macro composition, the coastal colour ramp and the beach surf blob are genuinely pleasant at a glance, which is why this reads as a competent artist's terrain pass with an unfinished water shader bolted underneath. Multiple automatic fails trip: flat-matte water surfaces, visible noise artifacts, doubled hex lines. Fix the water plane and the noise band-limiting before anything else; everything else on this list is cosmetic by comparison.

**Problems**
- Water is rendered on the terrain hex mesh and flat-shaded per facet: straight brightness steps run across the lake at (850-1020,760-890), and the flat facet measures HF_rms 5.84 / MID_rms 3.97 — an untextured matte polygon. Automatic fail.
- Open water is per-pixel salt-and-pepper noise, not waves. Deep ocean (1380,120,120,100) is MID_rms/HF_rms = 0.54; 4.01% of pixels exceed L=150 against a median of 44, peaking at 240. Isolated blown dots at uniform density. Automatic fail on visible noise.
- No specular lobe and no Fresnel anywhere. The scattered hot pixels are isotropic, not sun-aligned or clustered. Grazing far ocean (mean 55.8) is darker than near ocean (mean 72.3) — the sky-reflection gradient is inverted or missing entirely.
- Depth ramp is a hard step, not a gradient. The shallow lobe at (1290-1490,260-440) has a perfectly circular boundary with a 1-2 px transition — a radial mask with zero falloff width, sitting in open ocean.
- Foam is stroked along the depth isoline, not generated at breaking depth. A dashed white ribbon runs through open water at (1150-1300,250-600), far from any land, with no parallel breaker lines and no trailing wash.
- Rivers are decals, not channels. At (590-770,470-600) the grass and dirt texture reads through the water band unchanged, there is no bed, no bank lip, no waterline darkening, no flow, and tree canopies at (620-670,500-530) stand inside the channel with no contact.
- Doubled hex grid over coastal water: two parallel hex chevrons ~23 px apart at (1010-1100,155-235), the terrain grid and the water-plane grid projected from different heights. Strokes are 1 px hard with visible stair-stepping on shallow diagonals. Automatic fail.
- Hard material seam inside the lake: a straight boundary near (851,790)-(900,780) between an olive flat wash and a bright noise patch — two different water materials butted together with no blend. Plus blotchy olive-blue mottling along the lake shore.

**Fixes**
- Detach water from the terrain mesh. Render each body as a single flat quad at its water level, depth-tested against terrain; derive depth as (waterY - terrainY) from the depth buffer or a heightfield sample, never from per-hex geometry. This alone removes the facet steps and the flat-matte read.
- Replace the point noise with two octaves of scrolling world-space normal map: tiles at ~14 m and ~5 m (roughly 90 px and 32 px on screen at this zoom), scrolling 0.06 and 0.11 m/s along directions 35 degrees apart, amplitudes 0.35 and 0.18. Sample the fine octave with an explicit LOD bias of +0.75 so it never resolves finer than ~3 px. Target MID_rms/HF_rms >= 1.4 in deep water, up from 0.54.
- Add one GGX specular lobe, roughness 0.06-0.10, sun-aligned, horizon-stretched. Clamp specular contribution to 2.5x local diffuse so highlights land at 180-200 luma in the dark deep-water region rather than 240-252 on a median of 44.
- Add Schlick Fresnel with F0 = 0.02 driving a sky-colour reflection. Grazing hexes at the top of frame should reach 0.25-0.40 reflectance and near hexes 0.02-0.05, so far ocean ends up 1.6-2.2x brighter than near ocean — currently it is 0.77x, i.e. inverted.
- Rebuild the depth ramp as Beer-Lambert absorption: exp(-depth * sigma) with sigma_RGB = (0.36, 0.075, 0.045) per metre, clamped over 0-14 m, and pre-blur the depth field with a 3-tap gaussian at ~6 px. The shallow-to-deep transition must span 45-70 px on screen instead of the current 1-2 px, and no analytic circle should survive the blur.
- Gate foam on depth < 1.2 m AND shore distance < 2.5 m AND dot(waveDir, shoreNormal) > 0.2, then modulate with a scrolling foam mask so it forms 2-3 parallel breaker lines advancing shoreward. Band width 8-18 px, peak alpha 0.55, with a 25 px trailing wash at alpha 0.15. Remove any foam term keyed to the depth isoline.
- Carve the rivers as real geometry: drop the spline vertices 0.6-0.9 m below the adjacent bank, give a 1.5-2.0 m flat bed with a 0.5 m bank fillet, run the same water shader and depth ramp, add a 3-5 px wet-bank darkening at the waterline and a 0.35-alpha AO gradient 6 px up the bank.
- Give rivers flow: scroll the normal map along the spline tangent at 0.25 m/s at amplitude 0.25, plus a cross-flow octave at 0.4x speed. Exclude tree and bush scatter within 1.2 m of the river centreline so nothing stands mid-channel.
- Add shallow caustics: two additive scrolling caustic textures at 4.5 m and 2.2 m tiles, triplanar-projected onto the seabed, intensity fading linearly from 0.30 at 0.3 m depth to 0.0 at 3.0 m, multiplied by sun NdotL. This is what sells the shelf and it is currently absent everywhere.
- Draw the hex grid exactly once — on the water plane where water exists — as a 1.6 px SDF line with 1 px feather, and fade grid alpha to 0 over ocean beyond the second ring. Separately, unify the lake's two water materials into one shader driven by continuous depth, replacing the binary shore mask with a smoothstep over 0.8 m of depth. Then lift shallow-water saturation from 0.253 to 0.42-0.50 in the 1-4 m band and close the mean gap to deep water from 116 down to ~70 by brightening the deep via sky reflection, not by washing the shallows chalky.

## critique 3 — 54/100
Rejected. Six automatic fails, any one of which kills it: flat-matte untextured surfaces, props with no contact shadow, double-drawn hex lines, salt-and-pepper/streak noise, clipped UI, and a toy unit silhouette.

Unlabeled and side by side with a real Civ VI in-game screenshot, a player calls Civ VI better, immediately, and the single tell is SHADOWS. In Civ VI every hut, tree, cliff and unit throws a hard directional sun shadow that lands on the neighbouring hex and anchors the object to the board; the whole map reads as one lit diorama. In this build nothing casts anything. The village at (600,250) sits on a hard-edged grey elliptical decal — a 1998-era blob shadow, not a shadow. The Aurelia keep at (790,600) is a five-storey tower on a hill that throws zero shadow onto the grass or the water beside it. The cliff wall at (0-320,60-240) does not darken the meadow at its base. So every prop reads as a sticker pasted onto a painted backdrop, and the board looks flat even though the individual assets are decent. That is the tell, and it is visible at thumbnail size before a player has read a single hex.

Second-order, the frame is also fighting itself on detail budget: sand at (500,600) measures HF_rms 32.71 / MID_rms 27.85, while the lagoon water at (900,830) measures HF_rms 6.57 / MID_rms 5.02. A 5x swing in pixel-scale energy across one frame means the ground is screaming near-Nyquist noise while the water is bald plastic. Ratios of 0.60–0.92 across every terrain region are not "under the 1.2 ceiling", they are failing from the wrong side: more energy at the pixel scale than at the readable blob scale, which is the numeric signature of a noise function standing in for art.

Colour script is the one genuinely competent part — sand 131-138, grass 133, crushed 0.48%, blown 0.02%, no garish blocking on the board. Then the minimap dumps pure-primary Tailwind blue/red/orange dots on top of it, and they are the most saturated pixels in the frame.

The cloud shadows the brief asks about do not exist. I sampled the patch I read as a shadow at (860,350): mean 157.8, against "lit" sand at (620,330) mean 131.5. The supposed shadow is 20% BRIGHTER than the light. There is no coherent cloud layer, only mottle that happens to go both directions.

**Problems**
- AUTO-FAIL, contact shadows: no prop in the frame casts a directional sun shadow. Village huts at (600,250) and the boulder at (490,270) sit on hard-edged grey elliptical decals; the Aurelia keep at (790,600) throws nothing onto adjacent grass or water; the cliff at (0-320,60-240) does not shadow the meadow at its base. Everything reads as a sticker on a painted backdrop. This is the single tell against Civ VI.
- AUTO-FAIL, double-drawn hex lines: the move-path chevrons at (880-1000,560-640) are the same cream, same ~2px width, same softness and same height as the hex border, and they run PARALLEL to it about 10px inboard. At gameplay zoom that reads as one hex outline drawn twice with an offset. Compounded by a third, unrelated grid language — thin dark 1px territory lines over sand and ocean at (1080-1300,300-420) that stair-step visibly. Three grid treatments, none legible, brief asked for one.
- AUTO-FAIL, flat-matte untextured surfaces: the water plane inside the selected hex (930-1090,600-690) is faceted flat teal, HF_rms 6.57 with visible straight tessellation seams crossing it. The hut cones at (990-1050,480-540) are single-value tan with no thatch or material. The city apron at (700-780,640-690) and the cliff top faces at (60-260,80-160) are flat-shaded polygons.
- AUTO-FAIL, noise and streak artifacts: water carries a field of 1-2px salt-and-pepper white specular singles across (1000-1400,180-420) that TAA never resolved, and they cut off dead at the shallow/deep seam. The cliff faces are pure vertical smear — a triplanar projection stretched on Y, no strata, no rock form. Sand at (300-620,480-660) is a per-pixel scribble at roughly 1.2px per feature, HF_rms 32.71, which will crawl the instant the camera moves.
- AUTO-FAIL, clipped UI: '2720 BC' in the turn banner at (770-830,40-52) is sliced horizontally by the bottom edge of the top bar — the glyph descenders are sheared flat. In the minimap the compass rose at (1345,855) is cut in half by the panel frame, and three unit icons stack and overlap the capital star at (1420-1450,640-660).
- AUTO-FAIL, toy silhouette: the on-map warrior at (895,505) is an unreadable ~22px smear of blue and tan with no rim separation from the grass behind it — measured MID/HF 1.03, the only region in the frame where blob energy beats pixel energy, i.e. it is pure mush. The UI portrait at (60-130,700-780) is a faceless flat-shaded mannequin, straight placeholder art next to Civ VI's painted portraits.
- No cloud shadows at all. Sampled (860,350) mean 157.8 vs 'lit' sand (620,330) mean 131.5 — the region reading as shadow is 20% brighter than the light. There is no cloud layer, only random mottle at the same spatial frequency and contrast as the ground texture, so nothing separates.
- No aerial perspective and no local contrast plan: grass 133.1, lit sand 131.5, mid sand 137.8 — the entire playfield sits inside a 7-unit luminance band from foreground to horizon. Nothing recedes, nothing focuses, and the eye has no path to the capital. Meanwhile the minimap icons are pure-primary blue/red/orange, the most saturated pixels in a frame whose board palette is otherwise disciplined.

**Fixes**
- Ship a real sun shadow before anything else. One CSM cascade, 2048² over a 60m near-field, sun at 38° elevation / 135° azimuth to match the existing cliff shading. Depth bias 0.0015, normal-offset 0.6 texel, 3x3 PCF at 1.5-texel radius. Every prop over 1m writes to it — huts, keep, trees, units, cliffs. Then DELETE the grey ellipse decals entirely; replace with capsule AO capped at 25% albedo darkening inside a 0.4m radius, not the ~45% grey blobs currently under the huts at (600,250).
- Kill the two highest fBm octaves on sand and grass. At this camera a hex spans ~155px, so the smallest useful feature is 4px; anything below 3px is guaranteed crawl. Replace with a 512² tiling sand-ripple albedo at 6.5m world tiling plus 40-60 scattered pebble/rock decal instances per hex at 8-20px each. Target sand HF_rms 15-18 and MID_rms 26-30, i.e. MID/HF ≈ 1.7, up from today's 32.71/27.85 = 0.85.
- Fix the cliff smear: the vertical streaking at (0-320,60-240) is a triplanar blend that is far too soft on the Y axis. Set blend sharpness to 4.0, swap in a 1024² strata texture with horizontal bedding, and scatter 3-5 large rock-form decals per cliff face so the silhouette is not one flat-shaded slab. Cliff top faces must run the same ground shader as the plateau they belong to.
- Fix water specular aliasing: clamp minimum roughness to 0.06 and add Toksvig or LEAN normal-map variance so the specular lobe widens with mip level instead of collapsing to 1px singles. Add +0.5 mip bias on the detail normal past 40m. That removes the salt-and-pepper across (1000-1400,180-420) without touching TAA.
- Rebuild the surf. The current foam at (1030-1220,240-400) is one sprite stamped along the shore spline at ~14px spacing and reads as a bead chain. Replace with a distance-field foam band off the shoreline SDF: 18-26px wide, 3 octaves of scrolling noise at 0.15/0.4/0.9 m/s, alpha ramping 0.0→0.85 over the last 6px at the waterline.
- Give the shallow/deep water transition a gradient. Right now it is a 1px binary cut at (1120-1260,200-300) with a faceted polygonal edge. Drive colour from depth with per-channel extinction (R 0.35/m, G 0.12/m, B 0.08/m) evaluated in the pixel shader, and move the current per-vertex lighting term to the pixel shader too — that is what is producing the straight seams across the lagoon at (930-1090,600-690).
- One grid, three jobs, three visual languages. Hex border: 2.0px #E8D9B0 at 55% alpha with a 1px #2A2018 outer stroke so it holds on both sand and water. Move path: 6px team-blue #4F7FC4 ribbon with a 1.5px dark outline, drawn at +0.15m height, terminating 8px short of each hex edge so it can never run parallel to a border — that alone kills the double-line read at (880-1000,560-640). Territory: keep it thin but give it a 3px soft outer glow instead of the current 1px hard line to stop the stair-stepping.
- Add an actual cloud-shadow pass: one scrolling 1024² mask, feature size 8-14 hexes, drift 0.4 m/s, multiplying DIRECT light only (never ambient) by 0.62-1.0, with a 3-hex-wide soft falloff. Two or three shadows across the visible board, deliberately placed to leave the capital lit. Feature size must be at least 8x the ground texture's macro scale or it reads as more mottle, which is exactly what happened at (860,350).
- Retune TAA, but only after the noise fix. 3x3 YCoCg neighborhood variance clamp at γ=1.25, feedback 0.92, then RCAS sharpen at 0.4 post-resolve. Sharpening today's ground would amplify the Nyquist scribble — fix the source texture first, then sharpen.
- Separate near from far. Depth-based ramp from 0 to 120m: saturation 1.0→0.72, lift toward sky #8FA9BF by 0-18%, and drop the top of the frame's local contrast by 15%. Add an 8% vignette over the bottom 20% of the frame. That breaks the current 131-138 flat luminance band and gives the capital somewhere to sit. Separately: unit screen height to ~34px with a 1.5px dark rim shader plus a 26px team plinth ring; desaturate minimap icons 25% and give each a 1px dark outline; raise the top-bar content box 6px or shrink the date to 11px so '2720 BC' stops being sliced.

## critique 4 — 41/100
Rejected. Five automatic fails in the graded area alone.

Side by side, unlabeled, with a real Civ VI in-game shot: the player calls Civ VI better, instantly, and it is not close. The single tell is the units. Your Warrior is a clay pawn — no hands, no readable weapon, a sphere helmet with a black slot for a face, and two flat blue discs. I measured the shield face at (924,362,10,8): HF_rms 8.68 on a mean of 97.5, i.e. ~9% relative surface contrast, and most of that is the rim edge bleeding into the patch. That is a painted disc, not a shield. Civ VI's ancient melee units carry plank seams, a boss, a rim, cloth folds and metal spec that survive gameplay zoom, and you can name the unit from silhouette without the tooltip. Here I could not identify an 8-STR melee unit from the 3D model — I had to read the HUD. Then you put a raw engine render of that same blob in the portrait slot, which advertises the weakness rather than hiding it. No shipping 4X does that.

The rest, briefly. The village hut cone at (1042,372,20,14) reads HF_rms 0.79 with sat 0.129 — that is not "low detail," that is a mathematically flat untextured surface, dither noise only. The keep courtyard sits at mean 190.7 as a near-white bare plane. The 220px-tall keep tower casts nothing: terrace directly downsun of it reads 141.0 against 143.2 beside it, a 1.5% delta, while the village huts 300px away throw 157.6 → 82.8 (a 47% drop) as hard black zero-penumbra spikes with visible peter-panning gaps. So half your props are missing from the caster pass and the other half are unfiltered. Scale is broken independently of all of that: keep spans y302→522 (220px), the warrior group spans y305→405 (100px), and the gate door is 28px tall — a man cannot walk into your city, and the gate is nearer the camera than the soldier, so the real-world error is worse than the pixel ratio suggests. The flagpoles are 190px, 86% of the entire keep's height, ~6px thick, and two of them pierce the tower and the courtyard wall with no socket, no intersection treatment and no shadow.

What is actually good: the composition and camera framing, the water, the HUD (clean, no clipping, no overlap — crushed 0.45%, blown 0.02%, no clipping anywhere), the notification stack, and the overall colour temperature. Ground MID/HF is 1.02 at (1080,600), inside the ≤1.2 shipping target. You have a competent frame wrapped around an incompetent prop and character pass. Fix the props and this is a different conversation.

**Problems**
- AUTOMATIC FAIL — untextured flat-matte surfaces. Village hut cone (1042,372,20,14): HF_rms 0.79, sat 0.129 on mean 49.8. That is a gradient with dither, zero material. Warrior shield core (924,362,10,8): HF_rms 8.68 on mean 97.5. Keep courtyard (640,470,20,16): mean 190.7, a bare near-white plane. Tree canopies are hard-faceted flat green slabs with no leaf texture, no translucency, no facet-to-facet AO. Every unit's cloth is a solid lambert fill.
- AUTOMATIC FAIL — props with no contact shadow. The two large village cones at (1000-1080, 350-400) sit on uniformly lit sand. The isolated tree at (1380,435) has its trunk intersecting grass with no base darkening. The desert rock at (1215,262) is a sticker on the sand. The 220px keep tower casts nothing: terrace under/downsun 141.0 vs 143.2 beside it = 1.5% delta, against the 47% drop the village huts produce (157.6 → 82.8). The 190px flagpoles cast nothing. City/district meshes are evidently not in the shadow-caster set.
- AUTOMATIC FAIL — toy/programmer-art unit silhouettes. Capsule torsos, sphere helmets, a black bar for a face, no hands anywhere, no weapon breaking the outline on an 8-STR melee unit. The settler group at (470-550, 760-850) is three lumps and a staff quad. Unidentifiable at gameplay zoom without the HUD; the unit portrait is the same blob rendered again.
- AUTOMATIC FAIL — double-drawn and stair-stepped hex lines. At the three-way vertex (995,405) the cream edge carries a second dark parallel line hugging its underside, disconnecting in places, and the right-going segment steps down in discrete 1px jumps rather than resolving as an AA'd line. Line width varies along the run and the vertex join is unmitred, producing a visible blob. TAA has converged; this is geometry, not temporal noise.
- AUTOMATIC FAIL — visible streak artifacts. Parallel diagonal light/dark banding runs across both grass and desert (clearly visible in 1130,240,200,180), reading as anisotropic smear rather than terrain detail. Separately, the harbor base slab at (1258,472,36,60) carries concentric arc contour bands along its left edge — a stepped height-blend falloff, not a material.
- Scale is broken. Keep total screen height 220px (y302→522), warrior group 100px (y305→405), gate door 28px tall and 35px wide. The door is 0.28x a soldier's height while sitting nearer the camera than the soldier. The whole multi-storey keep is 2.2 soldiers tall. Flagpoles are 190px — 86% of the keep's height — and roughly a man's torso in diameter. This is a doll-house read no shader pass can rescue.
- Geometry hygiene. Two flagpoles pierce the keep tower body and the courtyard wall with no socket and no intersection treatment. An untextured pale lump sits at the tower base (700,438,18,20) with no material and no shadow — reads as a hole in the art. The harbor settlement (1240,360,280,210) is a pile of intersecting roof planes at random angles with poles driven through them and a boat half-embedded in the dock. Masonry is a single 4-course brick pattern tiling identically up the tower with mirrored UV seams at x≈690 and x≈745, zero albedo variation, zero joint AO.
- City variety and overlay handling. All three settlements share one tan/olive/red-brown palette and one silhouette language — no read difference between a capital keep, a harbor town and a tribal village at a glance. The blue territory wash is a screen-space quad: it cuts a hard straight diagonal across the village huts at (1100,300)→(1160,420), desaturating and blue-shifting the props under it instead of tinting ground only, and the selection hex at (960,370,200,190) turns terrain and trees inside it into blue plastic.

**Fixes**
- Prop albedo pass, target HF_rms 22-34 on any 20x14px flat interior patch at this zoom (current: hut cone 0.79, shield 8.68, courtyard ~0 material). Cheapest version under a software raster: triplanar 3-octave value noise on albedo at 0.10 linear amplitude with feature sizes ~8px and ~32px in texel space, multiplied by a curvature-driven dirt mask, plus a detail normal tiled at 1 world-unit with 0.6 strength. Do the hut cones, the tree canopies, the keep courtyard and every cloth surface first.
- Grounding decal on every prop, unconditionally: a radial gradient decal at 1.6x the prop footprint, 0.45 peak opacity, falloff exponent 0.35, projected onto sampled terrain height at the base. Pair it with vertex-baked AO darkening the bottom 12% of each mesh to 0.55x albedo. This is near-zero cost and is the single largest grounding win available in this frame — it fixes the floating rock at (1215,262), the tree at (1380,435), both village cones and every grass tuft in one change.
- Add all city, district, harbor and flagpole meshes to the shadow-caster set. Verification target: terrace directly downsun of the keep tower must read 45-55% darker than adjacent lit terrace, matching the 157.6→82.8 the village already produces. Currently 141.0 vs 143.2.
- Unify the sun vector across the prop passes — village shadows and harbor stake shadows currently point in different screen directions. Then filter: 5x5 PCF at 2.5 texel radius (or 3-tap Poisson if fill-bound), clamp shadow darkness to 0.30 of ambient instead of near-black, depth bias ~0.0015 world units with a 1.5-texel normal offset so the hard black spikes at (1000-1120, 250-340) reconnect to their casters and stop peter-panning.
- Rebuild the city-center kit on a 1.8m human module. Concrete target from this frame: shrink units ~40% (soldier group 100px → ~60px) and raise the keep ~1.6x (220px → ~350px), giving a gate door of ~54px against a ~60px soldier — door ≥0.9x soldier height. Rule: any opening a unit is implied to use must be ≥0.9x on-screen unit height.
- Cut flagpoles from 190px to ≤45% of keep height (~155px at the new keep scale) and halve their diameter to ~3px. Snap every pole to an authored socket on the parent mesh and run an intersection cleanup — no pole may pierce a roof or wall surface. Fixes the two piercing the tower at x≈690 and x≈745.
- Unit silhouette rebuild for a 3-value read at 60px screen height: dark helmet, mid torso, one bright weapon or shield accent. Add hands. Add a weapon that breaks the outer silhouette by ≥25% of body width — the Warrior currently shows no weapon at all. Shields get a boss, 5-7 plank seams and a rim at ΔL ≥ 40 from the plank field.
- Replace the unit portrait at (45,700,90,90) with a painted or studio-lit 3/4 bust. Shipping a raw engine render of the field model directly next to the field model doubles down on the weakest asset in the build.
- Redraw the hex grid as a single terrain-projected decal in the ground shader — signed distance to hex edge, smoothstep over 1.5px of screen-space derivative width — instead of extruded line geometry. Kills the doubled parallel line at (995,405), the unmitred vertex blob and the 1px staircase in one change, and makes the line follow terrain height for free.
- Base slabs: replace the stepped height-blend falloff producing the concentric arcs at (1258,472) with a single smoothstep over the full flatten radius, then add a 6-10px rubble transition ring (alpha-blended edge plus scattered pebble instances) and footpath/rut decals. Then give each settlement archetype ≥25 hue-degrees of roof separation — oxide red for the keep, bleached timber for the harbor, dark thatch for the village — so the three cities read apart at a glance.

## critique 5 — 34/100
Rejected. Multiple automatic fails, and the focus of this pass — unit and city craft — is the weakest thing in the frame.

Side by side, unlabeled, against a real Civ VI in-game screenshot: the player calls Civ VI better, instantly, and it is not close. The single tell is the unit. In your build the selected Warrior is a ~16x14px green lump sitting on green grass at (1005,592), with roughly 70% of it occluded by the count badge drawn dead-centre over it — I could not identify it as a unit at 12x zoom, and the metrics agree: unit-body sat 0.365 versus the shrub 6px to its right at 0.183, i.e. the unit is more saturated than a bush and otherwise indistinguishable from one. Civ VI at this camera height renders a unit at 55-70px with a rim-lit silhouette, a weapon breaking the top contour, a team-colour banner, and a stylised base disc with soft AO. A player does not need the badge to find a Civ VI unit. They cannot find yours without it. Everything else — terrain read, UI chrome, the postcard-at-a-glance impression — is downstream of that one failure.

Second tell if they squint: your three settlements are three different games. Aurelia is a European fairytale keep with red conical roofs, Calyx is smooth clay thatch cones, Selmere is an unresolved heap of intersecting tan wedges with a blue ribbon drawn straight through the buildings. Civ VI settlements share one module kit and grow by module count. Yours share nothing — not a palette, not a roof language, not a wall material.

The UI panel is the best-crafted thing on screen and the only part I would ship. That is a bad sign when the pass is about units and cities.

**Problems**
- AUTOFAIL - no unit silhouette. The selected Warrior at (1005,592) is ~16x14px, green-on-green, with no helmet, weapon, shield, base slab or cast shadow. unit-body sat 0.365 vs adjacent bush 0.183 - it reads as vegetation. This is below programmer-art; it is absent art.
- AUTOFAIL - UI occludes the model. The count/strength disc is centred on the unit and is ~1.8x its height, hiding ~70% of the projected pixels. The badge IS the unit.
- AUTOFAIL - flat-matte surfaces. Desert HF_rms 7.28 with MID/HF 2.01 (your own tool says shipping AAA lands <=1.2); open water HF_rms 6.93 with zero pixel-scale material. Calyx huts are smooth gradient cones - no thatch strands, no ridge, no doorway. They read as clay dollops.
- AUTOFAIL - visible streak artifacts, three separate ones. (a) A radial fan of light streaks in the desert around (1120,470) - a polar-UV artifact in the sand mask. (b) Parallel NE-SW combing across every grass slope (clear at 600-780, 300-430) - detail texture sampled along one fixed tangent. (c) A 2px ordered-dither crosshatch across the shallow water at (820-1000, 640-720).
- AUTOFAIL - props with no contact shadow. Desert rocks and scrub at 1050-1160,430-520 are 4-6px dark stamps with zero ground darkening. Pier posts at (880,655) enter the water with no shadow, no foam, no displacement. Hillside trees are flat dark asterisks with no AO.
- AUTOFAIL - stair-stepped territory/hex lines. The border polyline is 1px and aliased; the diagonal run at (610,375)-(700,300) shows a clean pixel staircase despite TAA convergence.
- AUTOFAIL - interpenetrating geometry and z-order violations. Selmere (1180-1300,530-650) is a pile of tan wedges whose roof planes clip each other, with a bright blue river/road decal drawn straight through the structures. At Aurelia the water hex plane cuts through the building bases at (840-960,600-640) - a hard straight teal seam across the city footprint. The water surface itself is faceted per-hex with visible straight tile seams.
- City variety is incoherent rather than varied. Three settlements, three unrelated art vocabularies and palettes, no shared module kit, no shared material. Aurelia's red conical roofs appear nowhere else. Reads as three asset packs, not one civilisation.

**Fixes**
- Unit scale: hexes are ~118px wide here, so the Warrior must be ~0.55 hex height = 58-64px screen height, not 16px. Scale unit meshes 3.7x and drive it as unitPx = 0.55 * hexScreenWidth so it holds across zoom.
- Silhouette contract for LOD0: three cues readable at 24px - a shaft (spear/axe) breaking the TOP contour by >=8px, a shield or shoulder mass breaking a SIDE contour by >=6px, a helmet notch. Build to 1.4:1 vertical aspect. Add a 2px dark rim (alpha 0.35) opposite the sun so green-on-green never happens again. Force the player hue onto >=35% of projected pixels at chroma >=0.45, hue >=90deg from the tile hue - the Aeonian blue (~#2F6FC0) in the UI portrait appears nowhere on the map model.
- Base slab: oval base disc 22x14px at this zoom, two-tone (rim value 0.60 / fill 0.35), plus a radial AO blob at 1.6x disc radius, alpha 0.45, multiply-blended, plus a directional drop shadow at the sun azimuth (~135deg), length 0.45 * unit height.
- Badge: move the count/strength disc off unit centre to the base-slab lower-right, scale to 0.45 of unit height (currently ~1.8x), add a 1px dark outer stroke. Hard rule: UI never occludes more than 15% of a model's projected area.
- Desert detail: raise HF_rms from 7.28 into the 14-18 band with a detail albedo at 2 tiles/hex, 0.25 albedo contrast, plus a 0.6-strength normal map. Kill the radial fan by replacing the polar-coordinate sand mask with 3-octave value noise at frequencies 4 / 9 / 21, each octave rotated 27deg and 61deg so no direction accumulates.
- Grass combing: rotate the detail UV per tile by hash(tileId) * 360deg and add a second detail octave at 0.7 scale rotated 40deg from the first. That alone removes the parallel NE-SW streaking on every slope.
- Water: stop shading per hex. One continuous world-space plane, two scrolling normal layers at 0.030 and 0.017 uv/s with flow directions 90deg apart, depth-driven colour ramp so the shoreline is a gradient not a hard tile edge. Replace the 2px ordered dither in the shallows with blue noise or raise alpha precision.
- Territory borders: draw as an SDF ribbon of constant 3.0-3.5px screen width with 1px feather each side, inset 2px from the true hex edge so adjacent borders can never co-locate or double-draw. Retire the 1px aliased polyline.
- Placement and grounding: reject any placement whose 2D OBB overlaps an existing footprint by >5%; sort props by (y + 0.5*depth) front-to-back; clamp every building base Y to the terrain height sampled at its footprint centroid. Render river/road decals before opaque props (depth-test on, depth-write off) or mask them by the building footprint so they stop drawing through walls. Every rock, bush and tree gets a multiply-blended contact ellipse at 1.3x footprint, alpha 0.40, drawn before the sprite - no exceptions under 8px.
- One culture kit for cities: 4 wall modules, 3 roof types, 3 wall values, 2 roof hues, shared across all settlements; drive variety by module COUNT per population, not by swapping art styles. Bake 0.35-strength cavity AO plus a 1px darkened mortar seam into Aurelia's masonry so stone reads at native 1600x900. Give the thatch huts a radial normal (48 strands), an alpha-cut 3-4px ragged eave, and a 4x6px dark doorway - one doorway is the cheapest cue that separates 'building' from 'haystack'.

## critique 6 — 36/100
REJECT. Seven automatic fails, any one of which ends the pass on its own.

Unlabeled, side by side with a real Civ VI in-game screenshot at gameplay zoom, the player calls Civ VI better and does it in under a second. The single tell: the ground texture. The left half of your map (roughly x=60-460, y=330-620) is a field of concentric fingerprint whorls and radiating comet-streaks — a domain-warped UV that has folded past its own tile period and is producing caustic singularities. It is the single loudest object in the frame and it reads as an oil-slick Photoshop twirl filter, not terrain. Metrics back it: that block runs HF_rms 21.54 with MID/HF exactly 1.00, i.e. all of its "detail" is pixel-scale noise with no supporting form energy. Nothing in a shipping Civ frame looks like that.

The runner-up tell, and the one that would decide it if the whorls were fixed: your coastline is the raw hex mesh silhouette. At (880-1220, 240-460) land meets water along a razor-straight diagonal, a razor-straight horizontal, then another razor-straight diagonal, with a hard 2-3px extruded dark wall at the seam and the foam floating 8-14px offshore as a detached soft cloud that does not follow the shore. Civ VI never shows you a straight-line coast; it has a shore-blend band, wet-sand darkening, a foam ring locked to the distance field, and displaced edge geometry. Yours reads as cardboard hexes laid on a blue sheet.

What is genuinely good, and I will say it once: the notification stack, the top resource bar, and the unit panel chrome are close to shipping quality — good gold-on-dark hierarchy, crisp type, clean icon set, no overlap, no clipping. The Aurelia keep and the Calyx settlement have competent silhouettes and the keep's brick actually has material. That is maybe 20% of the screen. The other 80% is broken art.

On the focus areas specifically:

SHARPNESS AFTER TAA — inverted detail budget. Your quietest surface is your noisiest. Deep ocean at (1330,150) measures HF_rms 31.64; the neighbouring water at (1480,150) measures 11.12; your hero city measures 19.66; your sand measures 8.62. A converged shipping ocean at this zoom sits at HF_rms 4-8. You have per-pixel specular dither with no swell direction, no scale falloff with distance, and unresolved firefly single-pixels scattered through it. TAA did not converge on this; it cannot, because sub-pixel specular is not a temporal problem.

COLOUR SCRIPT — there isn't one, there's a duotone. Hue histogram over the map region: 39.2% of saturated pixels sit in 30-60 degrees (sand orange), 16.7% in 180-210 (cyan), 11.3% in 60-90. Everything from 240-360 degrees totals 0.6%. Zero violet in the shadows, zero complementary accent, no tertiary family. Mean saturation 0.332 across the whole map with grass at 0.364-0.393 means the terrain is competing with your cities for attention instead of receding behind them. Shadow tinting is also inconsistent: warm brown in the whorl region, cool blue-grey in the grass 200px away.

LOCAL CONTRAST — what you have is noise contrast, not form contrast. p5/p50/p95 luma is 37/102/222 which is fine on paper, but only 6.1% of the frame is below luma 40 and 10.2% above 200, so the image is mid-crushed and the eye finds no anchor. Sand at (900,300) is MID/HF 1.91 — well past the 1.2 your own tool names as the AAA ground-shader target — meaning broad soft airbrush blobs with nothing underneath them.

SINGLE LEGIBLE GRID — you have three competing line systems drawn simultaneously. Around Aurelia and again around Calyx there are parallel dark-navy and light strokes offset by roughly 2px following the same hex edge. The movement overlay draws its own bright rim on top of both. Diagonals staircase visibly at (700,760)-(1000,1120) in the near field and at the field corner below Calyx. Some hexes have a border, some have none, and the upper-left third of the map has no grid at all.

CLOUD SHADOWS — none on land, and the one thing that looks like one is a bug. The ellipse at (1330,150) has a hard mathematical arc for a rim, no penumbra, it is BRIGHTER than the water around it (mean 58.5 inside vs 45.7 outside), and it triples the pixel noise inside its boundary (HF_rms 31.64 vs 11.12). That is an inverted, un-feathered, noise-amplifying decal, not sky occlusion.

**Problems**
- AUTOFAIL - blotchy colour / streak artifacts: domain-warped UV has folded past its tile period across the whole left-centre of the map (x=60-460, y=330-620), producing concentric fingerprint whorls and radiating comet-streaks. HF_rms 21.54, MID/HF exactly 1.00 - pure pixel noise, no form energy. Same artifact visible more faintly in the grass around Calyx at (420-760, 120-340).
- AUTOFAIL - untextured flat-matte surfaces: the teal river/water planes over land at (900-1150, 480-700) and (700-1040, 620-840) are flat alpha quads with zero refraction, zero normal detail, hard faceted polygon boundaries that tint the sand and dirt underneath, and hard coplanar seams where two different teal shades meet mid-hex. Also the beige dome huts at Calyx (500-620, 220-360 in crop space) are pure gradients with no material, and the map trees are single-colour green blobs with a hard dark rim.
- AUTOFAIL - toy/programmer-art unit silhouette: the on-map warrior at approximately (1005, 600) is a 25x30px untextured green-and-tan lozenge sitting under a plain gold coin token. No head, no weapon, no banner, no readable silhouette. The UI portrait at (45-105, 715-800) is a smooth featureless mannequin - untextured helmet blob with two black slot eyes and a flat blue torso - and it occupies the highest-attention 60x60px in the entire frame.
- AUTOFAIL - double-drawn and stair-stepped hex lines: parallel dark-navy and light strokes offset ~2px along the same hex edge near Aurelia (640-1060, 420-680) and again below Calyx. Visible 1px staircases along diagonals in the near field around (700,760)-(1000,1120). Three separate line systems (world grid, territory border, movement-overlay rim) are drawn at once with different widths and AA behaviour.
- AUTOFAIL - visible noise: deep ocean HF_rms 31.64 at (1330,150) and 23.6 at (1300,120) - the noisiest surfaces in the frame are the ones that should be calmest. Uniform per-pixel hash with no swell direction and no distance falloff, plus unresolved firefly single-pixels. City is 19.66, sand is 8.62; the budget is exactly backwards.
- AUTOFAIL - props with no contact shadow: the dolmen at (255,405), the crates at (1230,725), both bushes inside the movement overlay at (955,600) and (985,635), and the right-hand base of the Aurelia keep all float. The desert pebbles across (880-1220, 240-460) have radially symmetric dark discs under them that ignore the sun vector entirely - the keep casts down-right, the pebbles cast nowhere.
- AUTOFAIL - hard-edged seams reading as z-fighting: the water plane crosses the beach at (700-900, 700-840) along a hard coplanar straight edge with no depth fade, and the hex diagonal continues INTO the water as a faint dark line past the shore corner near (1150, 400), double-drawn against the land silhouette.
- Colour script is a bare orange/teal duotone: 39.2% of saturated map pixels in hue 30-60, 16.7% in 180-210, and only 0.6% total across 240-360 degrees. No violet shadow family, no tertiary, no complementary accent. Shadow tint flips from warm brown to cool blue-grey between adjacent regions.

**Fixes**
- Kill the UV fold. Clamp the ground detail warp amplitude to <= 0.35x the detail tile period and drop the warp to 2 octaves. Replace the warp entirely with hash-based stochastic tiling: 3 samples blended by simplex-grid barycentric weights at a 1.5m tile. Target grass HF_rms 12-15 at MID/HF 0.9-1.1 (currently 21.54 / 1.00 at region 120,380,300,200).
- Fix the sand. Add a 512px dune-ripple normal at 0.6m tile, normal strength 0.25, and cut the low-frequency mottle mask amplitude by 40%. Target region 900,300,240,160 from HF_rms 8.62 / MID-over-HF 1.91 to HF_rms 12-16 / MID-over-HF <= 1.15.
- Fix ocean specular aliasing at the source, not with TAA. Apply Kaplanyan/Frostbite specular AA: rough = max(rough, saturate(length(fwidth(N)) * 1.6)), bake per-mip roughness on the water normal map, and fade the detail-normal contribution to zero beyond ~40 world units. Add a pre-resolve neighbourhood firefly clamp at 4x the 3x3 max luma (or resolve in c/(1+luma) space and invert after). Target deep water HF_rms 5-8, down from 31.64 at (1330,150).
- Build a real shoreline. From the distance-to-water field, over the outer 0.35 hex-radius of every coastal hex: (a) displace the mesh edge with 3-octave noise at +/-0.18 hex radius so the silhouette is never a straight polygon, (b) darken sand albedo 22% and drop roughness to 0.25 over the last 0.12 radius for wet sand, (c) drive the foam mask from that same distance field so surf hugs the shore instead of floating 8-14px offshore as it does at (1000,300)-(1150,400). Delete the extruded dark wall at the land/water seam.
- Collapse to ONE grid. Draw hex borders as a world-space extruded ribbon (2-quad strip, screen width scaled as width/dist to hold 0.9-1.1px), depth-tested and jittered into the TAA sample sequence so it actually resolves. Delete every screen-space stroke pass. Territory border becomes the same ribbon at 2.5px with a different colour. The movement overlay loses its rim entirely and becomes a 0.22-alpha fill with a 6px inner feather. That removes the parallel doubles at Aurelia and the staircase at (700,760)-(1000,1120).
- Make the water planes actual water. Add scene-colour refraction at 0.02 normal-offset strength, Beer-Lambert depth absorption with extinction (0.35, 0.09, 0.05) per metre, a flow-map-driven normal at 0.8m tile, and a 3px soft depth-fade at every shore contact so the (900-1150, 480-700) quad stops producing hard coplanar seams against the beach and stops tinting the dirt underneath it.
- Contact shadows on every prop, no exceptions. Per-prop capsule/disc AO decal at 1.15x prop footprint radius, 0.55 opacity, offset 0.35x prop height along the sun vector (which must match the keep's down-right cast, not be radially symmetric). Layer a 12-step screen-space contact-shadow trace at 0.4m max ray distance. Priority targets: dolmen (255,405), crates (1230,725), bushes (955,600) and (985,635), keep base right side.
- Rebuild the map units. The warrior at (1005,600) is 25x30px; Civ VI gives you 55-70px of readable model at this zoom. Scale to >= 3.5x current screen height, give it a 4-value material ramp (skin / leather / metal / cloth), a rim light at 0.35 intensity from opposite the sun so the silhouette separates from the ground, a weapon held clear of the body outline, and a coloured civ banner. Add the 0.6-opacity drop disc. Replace the UI portrait mannequin at (45-105, 715-800) outright - it is the highest-attention pixels in the frame and it is currently featureless.
- Write an actual colour script. Push shadows into 250-265 degrees (at luma < 0.25 lift blue +8, drop red -6) to break the 0.6%-of-pixels violet vacuum. Introduce a third hue family - desaturated olive/ochre at 45-70 degrees pulled to sat 0.18. Drop grass saturation from 0.364-0.393 to 0.26-0.29 so terrain recedes and cities/units carry the accents. Reconcile the warm-brown vs cool-grey shadow tint split between the (120,380) and (380,600) regions - pick one and apply it globally.
- Fix cloud shadows. Delete the elliptical decal at (1330,150) - it is brighter than its surroundings (mean 58.5 vs 45.7), has a hard mathematical rim with no penumbra, and triples local noise (HF_rms 31.64 vs 11.12). Replace with a 2048px cloud mask projected along the sun vector, multiply-only at 0.68-0.82 darkening, 80-120px screen-space penumbra, scrolling, and crossing terrain and water continuously so it also lands on land - currently there is not a single readable cloud shadow anywhere on the ground.

## critique 7 — 49/100
REJECT. Side by side and unlabeled with a real Civ VI gameplay shot, the player calls Civ VI better, and the single tell is that your ocean is visibly cut into hexagons. Open water at /home/piotr/looping_opus_5_test/shots/water-p5r2.png around (1030-1260, 70-300) and (1300-1600, 330-520) is a mosaic of flat translucent hex plates with hard straight seams, per-tile tone steps (17.7/255 single-pixel step at x=1103,y=230) and visible double-blend where adjacent plates overlap. Civ VI's ocean is one continuous surface; the hex grid exists only as an overlay on land. Everything else follows from that same architecture: because water is shaded per tile, the shallow/deep ramp is quantized to tile centers (the Selmere bay at 1140,490 goes mint-to-teal across a hexagon edge), there are no wave normals, and there is no specular lobe at all - what you have instead is uniform salt-and-pepper single-pixel glitter that appears even inside the dark deep plates, which no real water shader does. Measured: open ocean HF_rms 7.14 vs MID_rms 7.08 (MID/HF 0.99) - the detail energy is pixel noise, not wave structure; a shipping ocean sits well above 1.2. On top of that the whole ocean carries a ~3px diagonal weave: D45/D135 anisotropy 1.32 (openR) and 1.26 (deepw) with alternating-sign diagonal autocorrelation (-0.148, -0.103, +0.275 at lags 1-3). That is a texel-grid aliasing artifact on the raster diagonal, not a swell - a real swell would be anisotropic along the wind vector, not along the screen diagonal. Zero shallow caustics anywhere on the sand shelves. Rivers are the one genuinely good idea here - they are carved channels with real banks rather than Civ VI edge decals - but the water inside them is an unlit cyan wash with no flow direction, no bank darkening and no shore foam; river_only mean 116 against sunlit grass at ~120 means the water is the same brightness as the land it cuts through, so it reads as a painted ribbon, not a recess. Automatic fails triggered: hex tile seams and double-drawn overlapping plates in the water surface; visible diagonal weave/streak artifact across the entire ocean; flat-matte water in the harbor and river; jetty at (1180,510), piers at (850,640) and the bridge at (570,395) all sit on the water with no contact shadow, no waterline wet-darkening and no foam collar. Unrelated to this pass but I am flagging it: the desert at (1050-1370, 690-890) has a severe marbled fingerprint/wood-grain swirl from over-cranked domain-warped noise - same noise system, same problem.

**Problems**
- Water is rendered as one plate per hex tile: hard straight seams, per-tile tone steps and overlapping double-blended edges across open ocean (17.7/255 step at x=1103,y=230; large lighter hexagon visible at 1330,340 and a facet edge at 1100,150-1160,300). This is the single tell against Civ VI and an automatic fail.
- ~3px diagonal weave/streak artifact over the entire ocean - D45/D135 anisotropy 1.32 (openR 1330,350) and 1.26 (deepw 1070,100), diagonal autocorrelation alternating -0.148, -0.103, +0.275 at lags 1-3. Texel-grid aliasing, not wave structure. Automatic fail.
- No wave structure at any scale. Open ocean HF_rms 7.14 vs MID_rms 7.08 (MID/HF 0.99) - detail energy is pixel-level noise, not swell. Mottling is isotropic blobs with no dominant wind direction and no consistent light on wave faces.
- No specular lobe. Sun glitter is uniform single-pixel speckle covering 100% of the water including shadowed deep plates - no GGX highlight, no Fresnel falloff, no sun path. Real water at this camera pitch concentrates glitter in a narrow lobe.
- Zero shallow caustics. Every sand shelf (Selmere bay 1140,490; harbor 830,560; the whole shore band 880,60-1100,230) is a flat mint wash over the seabed with no caustic banding whatsoever.
- Depth ramp is quantized to tile boundaries instead of per-pixel seabed depth, so shallow-to-deep transitions happen as hexagon-edged steps. Depth ordering is also inconsistent across the frame: deepw mean 143 / sat 0.361 is brighter than the further-out ocean_BR at 96.8 / 0.261.
- Every prop touching water has no contact: jetty (1180,510), piers (850,640), bridge over the river (570,395) - no contact shadow on the water, no waterline wet-darkening on the prop, no foam collar, no reflection. They read as decals on a flat plane. Automatic fail.
- River channels are geometrically right but shaded wrong: unlit flat cyan, no flow direction, no bank contact darkening, no shore foam. river_only mean 116 vs sunlit grass ~120 - the water is as bright as the land, killing the recess read.

**Fixes**
- Weld the water. Merge coplanar per-hex water quads into ONE mesh per contiguous body and dissolve interior edges so no seam is ever blended twice. Acceptance: max luminance step across any interior water seam < 3/255 at 100% zoom (currently 17.7/255 at x=1103,y=230).
- Drive depth per pixel, not per tile. Read seabed scene depth minus water-plane height as d in world units, then colour = lerp(shallow, deep, saturate(d/D)) with D ~= 2.2 hex-radii, plus Beer-Lambert extinction exp(-d * sigma) with sigma_rgb ~= (0.35, 0.12, 0.06) per world unit. Pin deep ocean at L 55-75 / sat 0.30-0.40 and let shallow reach L ~150 so the depth ordering stops inverting across the frame.
- Real wave normals: three scrolling Gerstner-derived 512^2 normal maps at world scales 1.0 / 0.37 / 0.13 hex-radii, scroll velocities 0.030 / -0.019 / 0.047 hex-radii/s, directions within +/-25 degrees of one dominant wind vector, blended with UDN. Acceptance: anisotropy along the wind axis D_wind/D_cross >= 1.6, and D45/D135 (screen diagonal) back to 1.00-1.05.
- Kill the diagonal weave by fixing mip selection on the normal/detail sample: apply a +0.5 LOD bias over the computed pixel footprint and fade the highest detail octave to zero once the sample rate passes 1.5 px/texel. The alternating-sign lag-1-to-3 autocorrelation is a 1:1 texel-to-pixel aliasing signature, not something TAA will ever converge away.
- Replace the uniform speckle with a GGX specular lobe: alpha_roughness 0.045-0.08, F0 = 0.02 with Schlick Fresnel (at this ~55 degree camera pitch that gives F ~= 0.06-0.10, so the surface must NOT glint uniformly). Gate the sparkle term by NdotH^128 against the actual sun vector so glitter covers roughly 15-20% of the ocean in a coherent path, and multiply it by the shadow/occlusion term so it stops appearing inside dark deep plates.
- Add shallow caustics on the seabed only: two scrolling voronoi caustic textures multiplied, contrast ~1.8, sunlight-tinted, amplitude = smoothstep(0.6, 0.05, d) so they die at the beach line and never appear past 0.6 hex-radii depth. Target +12-18% luminance on the brightest bands over the sand shelf, with a ~0.6px R-vs-B chromatic offset on the caustic sample.
- Rebuild foam from a shoreline SDF, not from shelf polygon boundaries: foam = smoothstep(0.14, 0.0, sdf) * (0.6 + 0.4*noise), plus a second offshore band at sdf in [0.18, 0.26] gated by a scrolling wave phase so it breathes. Erode the alpha with a 3-octave 256^2 foam mask scrolling at 0.05 hex-radii/s for crest breakup instead of the current airbrushed white band.
- River flow: bake a flow map from the channel centerline (direction = tangent, magnitude by slope) and do two-phase flow-map distortion of the normal map (period 0.6s, phase offset 0.5). Multiply the water albedo by 0.75-0.80 within 0.12 world units of the bank so the channel reads recessed, and add a 2-3px darker wet rim on the bank geometry. Target: river surface mean luminance 15-20% below the adjacent sunlit grass (currently 116 vs ~120).
- Contact-ground every water prop: an AO contact disc under the jetty (1180,510), piers (850,640) and bridge (570,395); multiply prop albedo by 0.55 for the lowest 0.08 world units above the water plane for wet-darkening; and a static foam collar of radius ~0.35 world units at each water/prop intersection.
- Separate issue but same noise system - the desert at (1050-1370, 690-890) shows a marbled fingerprint swirl from over-cranked domain warping. Drop the warp amplitude to roughly a third and cap the warp octaves at 2 before this ships next to the water fixes.

## critique 8 — 31/100
Unlabeled and side by side with a real Civ VI in-game screenshot, every player calls Civ VI better, and not by a margin that needs thinking about — it's a glance, under a second. The single tell is the shadows: nothing in this frame casts one. The Aurelia keep is a ~22m stone tower under a ~55° sun and the ground beside it is unbroken; every scatter bush, pebble, tuft, unit and the entire Solmere settlement sit on the terrain with no cast shadow and no contact-AO darkening. That one absence is what makes the whole board read as a printed map with stickers on it rather than a world with objects standing in it — and it's the thing Civ VI does most conspicuously, since its long directional shadows are what anchor every forest and district to its hex. Runner-up tell, the one that survives even if you fixed shadows tomorrow: the ground is a single screen-space ellipse decal tinted per biome. Same pill stamp, same size, same yaw, same lattice, running unchanged from the grass straight across the ecotone into the sand and on into the near-field gravel. It reads as bubble wrap or frogspawn. There is no grass material, no sand material, no gravel material — there is one blob layer and a tint. That is the definition of failing ground material truth, and it is the load-bearing subject of this pass. The metrics back the eye everywhere I pointed them. LOD ordering is inverted: near-field grass HF_rms 9.37, far-field grass HF_rms 14.56 — the most distant ground in frame is 56% crunchier than the ground under the camera, because a fixed-screen-size stamp turns into per-pixel aliasing as its world footprint shrinks. Shipping wants the opposite by 2x. Open ocean measures HF 25.10 against MID 14.17 (MID/HF 0.56) — the flattest surface in the frame carries the highest pixel-scale energy on screen. That is not water, that's paper fibre grain, and at 3x it is unmistakably a matte paper texture with two hard-edged pale discs punched into it near (1440,110) and (1350,300) and a white foam ribbon meandering through open water 200px from any shoreline. Cliffs measure MID/HF 1.03 on the face and 0.97 on the top with visible vertical smear — one planar projection stretched over both vertical and horizontal faces, no triplanar, no strata, no cavity AO. Ecotones are straight polygon cuts with a ~3px feather. Shallow water shows its own triangulation as hard luminance facets and the coastline is a raw stair-stepped notch with no wet-sand zone. The entire frame lives inside a 0.14-wide saturation band (0.196 to 0.339) with the deep ocean the least saturated thing on screen and the near-field ground among the darkest — there is no colour script, no aerial perspective, no warm/cool separation between light and shadow families. Automatic fails hit, plural: untextured flat-matte surfaces (Solmere is a pile of unmaterialed brown boxes; the harbour props likewise), props with no contact shadow (universal), toy/programmer-art silhouettes (the Warrior is spheres on sticks; the blue assembly at 880,500 is unreadable primitives), blotchy colour (the ocean discs), and visible noise and streak artifacts (the 25.1 HF ocean grain, the stray foam ribbon, the cliff smear). Any one of those ends it. Credit where it's owed, because this isn't spite: the HUD is genuinely shipping-tier — clean gold and dark-wood hierarchy, real typographic discipline, no overlap, no clipping anywhere; the exposure is disciplined at 0.42% crushed and 0.04% blown, better than a lot of shipped work; the camera pitch and composition are correct; and the Aurelia keep is a real asset with real masonry, tile roofs and a silhouette that reads. That keep proves the team can build. It also makes the surrounding terrain look worse, because it's the only thing in frame that was authored rather than generated. Fix the ground and the shadows and this jumps thirty points; ship it as-is and the first screenshot on a forum gets called an asset-flip.

**Problems**
- Zero cast shadows and zero contact AO on every object in frame - scatter props, both settlements, all units, and the ~22m Aurelia keep under a ~55deg sun. Objects are printed on the ground, not standing on it. Automatic fail.
- Ground material truth is nonexistent: one screen-space ellipse decal, identical size/aspect/yaw on a near-regular lattice, tinted per biome. It runs unchanged from grass across the ecotone into sand and into near-field gravel. Reads as bubble wrap. There is no grass, sand or gravel material - only a tint.
- LOD ordering inverted. NEAR-grass HF_rms 9.37 vs FAR-grass HF_rms 14.56 - the most distant ground is 56% crunchier than the nearest. Fixed-screen-size stamps alias as their world footprint shrinks; near field has the least detail energy in the frame.
- Open ocean is a flat matte paper texture: HF 25.10 vs MID 14.17 (MID/HF 0.56), the highest pixel-scale energy on the flattest surface on screen. No waves, no specular, no caustics, no depth absorption ramp - shallow-to-deep is a hard polygon cut.
- Blotchy colour and streak artifacts in water: two hard-edged pale discs at approx (1440,110) and (1350,300) with no falloff (unclamped patch blend), plus a white foam ribbon meandering through open ocean ~200px from any shoreline. Automatic fail.
- Cliffs are single-planar UV smear - MID/HF 1.03 on the face, 0.97 on the top, visible vertical stretch on both vertical and horizontal faces. No triplanar, no strata banding, no cavity/crevice AO. Reads as stretched fur, not bedded rock.
- Toy/programmer-art silhouettes. The Warrior is a featureless sphere head on a slab body with stick arms; the blue assembly at (880,500) is an unreadable pile of primitives; Solmere is untextured flat-matte brown boxes with a single lambert albedo. Automatic fail on two counts.
- No colour script. The whole frame occupies sat 0.196-0.339 (0.14 spread), with deep ocean the LEAST saturated element and near-field ground among the darkest. No aerial perspective, no warm/cool light-vs-shadow separation, no focal saturation peak. Ecotones are straight polygon cuts with a ~3px feather.

**Fixes**
- Contact shadows, non-negotiable and first: near cascade 2048sq at 12m range, mid 2048sq at 60m, PCF 5x5. On top, multiply a baked AO disc into ground albedo under every prop and building - radius 1.3x the prop footprint, 0.55 opacity at centre, cosine falloff to 0 at the rim. At the frame's ~55deg sun elevation the Aurelia keep must throw a ~15m shadow across its tile.
- Delete the screen-space ellipse decal layer entirely. Replace with world-space 3-octave splat: macro variation at 48m, meso at 6m, micro grain at 0.75m, weighted by height and slope, each biome with its own albedo/roughness pair. Acceptance: every ground region MID/HF >= 1.6 and no visible repeat period under 20m.
- Fix LOD ordering: fade a near-field detail layer (pebbles, tufts, normal-mapped grain) in over 0-35m camera distance, and force trilinear + 8x aniso mips on far ground. Target near HF_rms 16-20, far HF_rms 6-9, near/far ratio >= 2.0. Currently 9.37/14.56 = 0.64.
- Rebuild the ocean shader: 3 summed Gerstner waves (wavelengths 24m/9m/3.5m, steepness 0.08/0.12/0.15), scrolling normal detail at 0.8m scale and 0.15 m/s, GGX specular at roughness 0.04. Kill the paper grain outright. Acceptance: open-water MID/HF >= 1.8 (currently 0.56) and HF_rms <= 12 (currently 25.10).
- Replace the shallow/deep polygon cut with Beer-Lambert depth absorption, extinction (0.35, 0.09, 0.045) per metre, sampled from the depth buffer. Also enable smooth vertex normals on the water mesh - the shallow water at (930,180)-(1190,360) is currently showing its own triangulation as hard luminance facets.
- Clamp splat weights to sum exactly 1.0 and feather all patch boundaries over >= 8m world space. That removes the hard-edged pale discs at (1440,110) and (1350,300) and the warm brown bleed contaminating the blue water at approx (1400,430).
- Drive foam from a shoreline distance field only: full opacity 0-1.2m from the water line, ramping to 0 at 4m, modulated by a 2.5m scrolling dissolve. No foam permitted beyond 4m from land - that kills the stray ribbon at (1180,260)-(1260,420).
- Cliffs to triplanar projection, blend sharpness 4.0, plus a horizontal strata layer at 1.8m period with dL 22, plus a curvature/cavity AO term darkening crevices 35%. Acceptance: cliff MID/HF >= 1.5 (currently 1.03) with no directional smear visible at 2x zoom.
- Ecotones: replace straight-line cuts with height-based blending, blend = saturate((h_a - h_b)/0.15 + w), broken by 4m-scale noise. No straight terrain-type boundary anywhere in frame. Add a 2-3m scrub fringe (sparse grass tufts at falling density) across every grass-to-sand transition.
- Colour script pass: push open ocean to 0.42-0.48 saturation and cool it ~400K; desaturate the top 25% of the frame by 25% and lift it 8% toward sky colour for aerial perspective; hold peak saturation on the focal city tile. Target >= 0.25 saturation spread frame-wide, up from the current 0.14. Separately, rebuild unit silhouettes: one hero prop breaking the body outline by >= 40% of body width, a profile-changing helmet, 15deg hip-shoulder counter-rotation - validate by rendering to pure black at 26px and confirming the unit class is still identifiable.

## critique 9 — 34/100
Reject. Unlabeled and side by side with a real Civ VI in-game screenshot, a player calls the Civ VI shot better within about a second, and the single tell is the units: in Civ VI you can name the unit from its silhouette alone at gameplay zoom (helmet, spear, shield, and a clean base disc reading against the tile), whereas here the selected unit at (860-950, 490-570) is an unidentifiable blue-and-tan smear with a pink spike on top, and a gold "1" chip floats over empty grass at (985-1035, 565-610) with no model under it at all. Everything downstream of that confirms it. Grounding runs backwards and I can measure it: terrain directly under the Calyx shack is BRIGHTER than the grass 20px away (L=111.6 vs 72.9), and the castle footing is +7.5L against its surroundings (111.9 vs 104.4) — you have a long sun shadow map and zero contact AO, so every asset hovers. The dome prop at (962-1020, 512-555) is flat-matte Lambert facets at 10x with no albedo and no normal, a blocked-out clay maquette left in a shipping frame, and its neighbour ruin at (1010-1060, 440-495) is textured with a literal 4px checkerboard. The river at (895-1010, 570-640) is a hard-edged stair-stepped ribbon ending in a floating rectangle at (975-995, 618-628). Solmere at (1160-1260, 545-640) is not a city, it is five slabs interpenetrating at random angles with no shared ground plane, and Calyx's dome is ~40px tall, smaller than a soldier. The Aurelia keep is the one asset here with AAA bones — real masonry albedo (interior HF 31.6 against open sand at 7.8), layered wall silhouette, tiled roof — and it only makes the rest look worse by proximity. Four separate automatic fails (untextured flat-matte surfaces, toy/programmer-art unit silhouettes, props with no contact shadow, visible streak artifacts) put this nowhere near the 88 bar.

**Problems**
- Unit silhouettes fail completely: the selected unit at (860-950, 490-570) is an unreadable blue/tan blob with a pink spike, no helmet, weapon, shield or legs discernible at 10x, despite occupying ~70px of screen height — Civ VI reads a warrior clearly at half that footprint. The second unit at (600-700, 800-900) is three tan spheres on a hard black coin. Both fail an alpha-only silhouette test.
- Orphan UI chip: the gold '1' badge at (985-1035, 565-610) floats over bare grass with no unit model beneath it, no leader line, and no drop shadow. Either the unit failed to render or the badge is placed on an empty tile — a shipping blocker either way.
- Contact grounding is inverted, not merely weak. Ground under the Calyx shack measures L=111.6 vs L=72.9 for grass 20px away, and the castle foot measures L=111.9 vs L=104.4 for surrounding grass. Both deltas are positive where a shipping title runs -25% to -40%. There is no contact AO decal anywhere in frame.
- Untextured flat-matte hero prop: the dome at (962-1020, 512-555) is pure Lambert value ramp across flat facets at 10x magnification — no albedo, no normal, no grain. Reads as an untextured greybox left in the build.
- Programmer-art texture shipped in frame: the desert ruin face at (1010-1060, 440-495) is surfaced with a literal 4px checkerboard pattern.
- Streak/decal artifact: the river at (895-1010, 570-640) is a hard-edged grey-white quad with stair-stepped borders that terminates abruptly in an unfeathered floating rectangle at (975-995, 618-628), with no depth-fade against terrain.
- City craft collapses outside the capital. Solmere (1160-1260, 545-640) is five interpenetrating slabs at random angles with no shared ground plane and no district layout. The pink roof plane at (895-940, 578-612) is a zero-thickness quad with visible backface.
- Scale ladder is broken: the Calyx dome at (555-600, 240-280) is ~40px tall, smaller than a unit at ~60-70px, so the city reads as a prop a soldier could step over, while the Aurelia keep at ~140px is correct. No consistent unit-to-building ratio.

**Fixes**
- Re-author unit silhouettes for a 64px screen height: force three distinct value bands (helmet/head, torso, base) at >=25% luminance separation, and hold weapons OUT from the body with >=8px screen clearance so they survive TAA. Gate every unit on an alpha-only silhouette test at 64px — if you cannot name it from the black shape, it does not ship.
- Lean units toward camera. You are rendering the tops of heads. Apply a per-unit lean of 15-20 degrees about the camera-right axis and scale units to 1.5-2.0x true world scale, the same cheat Civ VI uses, so face, chest and weapon face the viewer instead of the sky.
- Give every unit a proper base slab: a soft ellipse decal at 1.6x the unit footprint, 45% opacity black multiply, 6-8px gaussian feather, plus a 2px hard team-colour ring. Replace the current hard-edged black coin under the unit at (600-700, 800-900) and the aliased flat-cyan chevron ring under the selected unit.
- Add capsule contact AO decals under every unit and building: target -30% to -40% luminance at the footprint, falling to zero over 12-16px. Concretely, Calyx shack contact must go from +38.7L (111.6 vs 72.9) to roughly -25L, and the castle foot from +7.5L to about -35L.
- Texture the dome at (962-1020, 512-555): 2K tiling stone/thatch albedo at 1.5-2 texels/cm plus a normal map. Target interior HF_rms of 28-35 to match the castle wall's measured 31.6 — it currently sits at 19.8 and nearly all of that is TAA sharpen halo, not detail.
- Replace the 4px checkerboard on the ruin at (1010-1060, 440-495) with a real sandstone-block albedo, and add a 3-5cm edge bevel with chipping so block edges catch a rim highlight instead of dying into flat facets.
- Fix Solmere's layout: snap all city buildings to one district ground plane, cap per-building lean at 4 degrees, and enforce a 0.4m no-intersect margin between building bounds. Give the zero-thickness roof quad at (895-940, 578-612) real geometry thickness of 15-20cm.
- Clip the river strip at (895-1010, 570-640) to the terrain with a 0.15m depth-fade and feather the tail alpha over 8-10px so it stops terminating in a hard floating rectangle at (975-995, 618-628).
- Either render the unit or kill the chip at (985-1035, 565-610). Then give all map chips a 2px 60%-opacity drop shadow so they read as hovering over the world rather than pasted onto the framebuffer.
- Enforce a scale ladder across city kits: unit = 1.0, outbuilding = 1.2-1.5, city centre = 2.5-3.5. That takes the Calyx dome from ~40px to ~110px and keeps Aurelia's keep at ~140px, so cities always dominate the units standing next to them. Separately, break up the featureless sand at (1050-1120, 280-460): detail normal at 0.5m tiling plus macro breakup at 32m, targeting HF 11-14 against the current 7.8.

## critique 10 — 42/100
Unlabeled, side by side with a real Civ VI gameplay screenshot, the player calls Civ VI better instantly — not after study, in the first half-second. The single tell is that there is no hex grid. Not a faint one, not a stylised one: zero tile boundaries anywhere in the playfield. A 4X screenshot without a readable board reads as a diorama photograph, and the eye that came looking for "how many tiles to that coast" finds nothing to count. The floating "1" movement pip near Aurelia makes it worse — it points at a destination hex that does not exist. Your own focus list asks for a "single legible grid" and the build ships zero legible grids, which is a harder miss than shipping two.

Runner-up tell if you fixed the grid tomorrow: the ocean. It is one dash-shaped normal map at one angle, one frequency, one amplitude, from shoreline to frame edge, with no glint lobe and no distance falloff (deep-sea HF_rms 11.32, MID/HF 1.02 — flat and uniform). That is a 2005 RTS ocean and it covers a third of the frame.

Beyond the tells, this is rejected on five automatic fails, not one. Untextured flat-matte surfaces: the tan dome tent east of Aurelia, Calyx's dome and farm pads, the hut roofs, and the bright-green bush blobs at bottom-centre are single-colour faceted polys with no albedo at all. Props with no contact shadow: crates, bushes and the bottom-centre unit's base disc float on the ground. Toy/programmer-art silhouettes: the blue units are unresolvable confetti at gameplay zoom, and the Warrior portrait in the unit panel is a smooth-shaded eyeless mannequin — a Blender default material with a helmet. Visible streak artifacts: the ocean dash tiling plus the corduroy hatch repeating across the NW cliffs. Stair-stepped tile geometry: the NW plateau is raw polygon shards with stepped, unblended edges.

What is genuinely good: composition and camera framing, the UI chrome (typography, gold trim, hierarchy on the notification stack and unit panel are close to shippable), and the keep silhouette. Clipping discipline is clean — 0.42% crushed, 0.04% blown. That competence is why this scores 42 and not 20. But the world art is pre-alpha, and no amount of frame finish rescues a ground shader whose blotch energy dominates its pixel detail.

**Problems**
- No hex grid rendered anywhere in the playfield. Tile boundaries are visible only as accidental terrain-plate seams (razor-straight albedo cuts between grass and dirt in the mid-left), so the board is unreadable and the floating '1' movement pip points at nothing.
- Ground shader is low-frequency blotch, not material. sand-mid MID/HF = 1.71 and sand-flat = 1.46 against the <=1.2 ceiling metrics.mjs itself names for a shipping AAA ground. At 3x it is a Photoshop clouds layer with a bevel: no grain, no pebbles, no normal-mapped ripple.
- AUTOMATIC FAIL - untextured flat-matte surfaces: the tan dome tent east of Aurelia, Calyx's dome and its grey farm pads, hut roofs, and the bright-green bush polys at bottom-centre are single flat colours on hard facets with zero texture.
- AUTOMATIC FAIL - toy/programmer-art unit silhouettes: the three blue units read as scrap piles of blue and steel shards with no head, weapon or body mass at 40px tall. The Warrior portrait is a faceless smooth-shaded bust with no eyes.
- AUTOMATIC FAIL - props with no contact shadow: crates near Aurelia's docks, the green bush blobs, and the bottom-centre unit's oval base all float. No capsule AO, no SSAO at the keep's wall-to-ground junction.
- AUTOMATIC FAIL - visible streak artifacts: one dash-pattern normal map tiled uniformly across the entire ocean at a single angle and frequency, plus a repeating corduroy hatch across the NW cliff plates.
- Cloud shadow is a dead zone, not weather. NW-gloom mean 72.8 vs sunlit-grass 144.1 is a full stop of luminance loss across ~25% of the frame, and saturation drops 0.351 to 0.259 with a violet hue shift. It also stops dead at the shoreline - the water is never shadowed.
- No value structure in the colour script. Every land region sits between mean 109 and 145 with saturation 0.33-0.42; 0.04% blown, no true black under canopy, no sun-hot sand. Uniform chroma everywhere means nothing reads as a distinct material, and the post-TAA vegetation is watercolour mush (bottom-centre at 4x has no pixel-scale detail at all).

**Fixes**
- Hex grid, one draw in the terrain pass before TAA: signed distance to hex edge, line width held at 1.5px via fwidth(ddx/ddy) so it never doubles or stair-steps at any zoom. Colour = terrain albedo multiplied 0.75, never black. Alpha 0.22 on land, 0.10 over water. Add a 3px inner glow at alpha 0.35 on the selected unit's reachable hexes so the movement pip has a destination.
- Ground material: cut the 40-80px albedo octave amplitude by 60% to bring sand MID/HF from 1.71 to <=1.1. Add a triplanar detail albedo + normal layer at 2-4 px/texel, normal strength 0.35, mip bias -0.5. Target rule: within one hex, albedo-only luminance spread <= 12%; all remaining variation comes from the normal map under the sun.
- Cloud shadows: clamp the mask to a 0.80-0.85 multiplier on direct sun only, leaving ambient and sky untouched, so luminance drops 15-18% instead of the current 50% and hue holds (no violet). Real cloud texture at 60% coverage, edge softness ~1.5 hexes, scroll 0.02 hex/frame, and apply it to the water plane too - shadows that stop at the shoreline are the giveaway. Never let one park over the player's city.
- Colour script: push sunlit sand to mean 185-200, drop forest-canopy interiors to 45-55, and let 0.5-1.0% of the frame blow out on water specular. Cut global saturation ~15% (sand 0.42 -> 0.34) and separate materials by hue instead of chroma: sand +8deg toward yellow, shadows +6deg toward blue-cyan rather than violet.
- Units: enforce a 24x24px black-fill silhouette test - if it does not read filled, it does not ship. One dominant read-shape per unit (vertical spear, shield disc, horse mass), max 3 value steps on the body, faction colour restricted to <=25% of pixel area as one contiguous block, 2px rim light at 1.4x sun intensity on the sun side. Delete the greebles; at 40px they alias into blue confetti.
- Contact shadows: per-prop capsule AO decal, radius 0.6x footprint, alpha 0.45 at contact falling to 0 at 1.2x radius, multiplied into terrain albedo. Plus a half-res SSAO pass at 0.35m radius, intensity 0.7, to catch the keep's wall-to-ground junction and the crate stacks.
- Water: two normal maps at 1.0x and 0.37x UV scale, scrolling at different speeds 37deg apart, amplitude fading to 0.25 by 60% screen depth. Add a GGX sun glint lobe at roughness 0.06 and depth-based extinction so shallow-to-deep is a gradient. Kill the flat-shaded shelf quad whose straight edge currently cuts the coastline north of Solmere.
- TAA/sharpness: drop the history blend from ~0.95 to 0.88-0.90 with a 3x3 YCoCg neighbourhood clamp, jitter <=1px, then RCAS sharpen at 0.4-0.5 strength on resolve. Sharpen before the UI composite, never after.
- Terrain plates: select cliff material by world-space slope (>35deg, 8deg blend band) instead of per-tile, and scatter a rock-mesh decoration pass along cliff edges to break the polygon silhouette. That kills both the NW shard look and the corduroy hatch in one pass.
- UI: the HP bar is a flat #5cc94a rectangle in an otherwise bevelled ornate frame - give it the same inset treatment plus a 2px dark end cap. The MOVE tab's gold corner bracket clips outside the panel's left border. The STR icon reads as a chess pawn, not strength.

## critique 11 — 36/100
Rejected. Four automatic fails, all of them in the water. Side by side, unlabeled, with a real Civ VI gameplay screenshot: the player calls Civ VI better, and it is not close once the eye lands on the coast. The single tell is the hex grid drawn over the water — a hard, aliased, stair-stepped hairline that appears at (840-1000, 690-760), (1150-1330, 380-500) and (780-1050, 600-700), doubled near (845,700)-(900,730) where the terrain shading seam and the blue overlay line run parallel ~40px apart. Civ VI never shows a jaggy grid edge; one look at that staircase and a player knows this is a build, not a shipped game. Everything else compounds it. The ocean has literally zero specular: 0 pixels above L=235 in a 250x140 open-water box at (1150,60), and 0 above 245. A sunlit sea with no glitter path reads as painted denim, and that is exactly what the 6x crop shows — a single-octave symmetric mottle where every crest is a bright smudge with no paired dark lee, meaning the wave normal is modulating albedo instead of feeding N·L. The wave field is isotropic: correlation half-length 15px along x, 13px along y, ratio 1.15, where wind sea should be 3-5:1. Frequency balance is inverted versus your own ground shader — deep-ocean HF_rms 19.41 / MID_rms 12.15 = 0.63, i.e. pixel-scale noise dominates and wave-scale structure is missing, while land-grass sits at 20.51/21.27 = 1.04. In the bays it flips the other way into flat matte: river-lower HF 9.64, estuary HF 9.07, against land-dune 25.52 — large hard-edged flat quads of teal with a per-tile depth classification boundary visible as a contour at (1180-1310, 250-320). Foam is an alpha ramp, not foam: constant width, no internal cells, no dissipation, detached from the sand with clean water between at (1020,300)-(1080,330), and the sand albedo is identical on both sides of the waterline — no wet band at all. Rivers get the one genuine win, the channel is cut with real banks rather than painted on, but the surface has no flow, no shear, no riffles, and the centreline spline is leaking through as a dead-straight 3px constant-width pale blue line at (880,600)-(950,690), with a smeared pale beam over the water at (960-1080, 590-640). Shallow caustics: none exist anywhere in the frame. Props sit in the water with no contact shadow — pier posts at (1235,555)-(1270,600), boat hull at (1200,640), the tree at (930,760) standing on the river. The estuary is blotchy, an olive-to-teal wash with a garish yellow-green fringe at (900,790)-(1010,830) sitting on an unclamped grey shadow smear. Credit where due: the grade is coherent, the terrain has real craft, and the surf ribbon does carry the silhouette at thumbnail scale. But the focus of this pass is water shading truth, and the water is a painted texture wearing a water palette.

**Problems**
- AUTO-FAIL: stair-stepped, unfiltered hex lines drawn over water at (840-1000,690-760), (1150-1330,380-500), (780-1050,600-700); double-drawn near (845,700)-(900,730) where a terrain shading seam runs parallel to the blue overlay line ~40px away
- AUTO-FAIL: flat-matte untextured water — river-lower HF_rms 9.64 and estuary HF_rms 9.07 against land-dune 25.52, with large hard-edged flat teal quads and a per-tile depth contour at (1180-1310,250-320)
- AUTO-FAIL: props with no contact shadow in water — pier posts (1235,555)-(1270,600), boat hull (1200,640), tree standing on the river at (930,760); all intersect the surface with a hard cut, no AO, no waterline ring
- AUTO-FAIL: streak artifacts — dead-straight constant-width 3px river centreline spline visible at (880,600)-(950,690), plus a smeared pale-blue beam over the water at (960-1080,590-640)
- Zero specular anywhere on water: 0 pixels above L=235 in a 250x140 open-water box at (1150,60), 0 above 245. No sun glitter path exists in the frame
- Waves are not lit — the ripple field is symmetric, every crest a bright smudge with no dark lee, so the normal is modulating albedo instead of feeding N·L with the scene sun (which runs upper-left, terrain shadows to lower-right at ~35 deg)
- Wave field is isotropic and single-octave: correlation half-length 15px x vs 13px y (ratio 1.15, should be 3-5:1); deep-ocean MID/HF = 0.63 means pixel noise, not wave structure
- Foam is a constant-width alpha band with no internal texture, detached from the sand at (1020,300)-(1080,330), and no wet-sand darkening — sand albedo identical either side of the waterline. Shallow caustics absent entirely

**Fixes**
- Grid: draw the hex edge ONCE, as a screen-space analytic SDF in the fragment shader — w = fwidth(d); a = 1.0 - smoothstep(0.0, 1.5*w, d - 0.6px) for a 1.2px line that is AA'd at every camera angle. Delete the terrain-side shading discontinuity that currently doubles it. Fade overlay alpha to 0.25 over water so the grid never outranks wave detail.
- Specular: add a GGX lobe on the water normal at roughness 0.045-0.07 with the terrain's sun vector. Feed it a dedicated 4th normal octave at ~0.35 world-unit wavelength that contributes to the specular normal ONLY, not diffuse. Target: 0.3-0.8% of open-water pixels above L=240 inside the glitter band, p99.9 ~250 (currently p99.9 = 223.5, max 226.5).
- Wave structure: replace the noise scroll with 4 Gerstner waves sharing a dominant direction within +/-25 deg — wavelengths 9 / 5.5 / 3 / 1.7 world units, steepness Q = 0.22 / 0.16 / 0.10 / 0.06, amplitude ~ L^0.9. Re-measure until correlation-length along:across is >= 3:1 (currently 1.15:1).
- Lighting truth: build the normal from the analytic Gerstner derivative, not a finite-difference of a height texture at pixel scale, and run it through the same N·L as terrain. Crest faces should land ~1.4x mean, lee faces ~0.6x. Right now both are ~1.0x, which is the whole 'painted denim' read.
- Frequency balance: mip-bias wave normals +0.5 and clamp the smallest octave's screen-space wavelength to >= 4px; convert anything finer to a roughness increase via Toksvig/LEAN rather than a normal perturbation. Target deep-ocean HF_rms 8-10 and MID/HF 1.2-1.6 (currently 19.41 and 0.63).
- Foam: drive the mask from saturate(1 - depth/0.6m) times a dot(shoreNormal, waveDir) term so it thickens 2-3x in bays and thins on headlands; multiply by two scrolling foam-cell textures at 0.8 and 0.25 world-unit tiles advected up the beach normal at wave phase speed; add a dissipation ramp so the seaward edge breaks into cells instead of a clean alpha cut. Fix the detachment at (1020,300)-(1080,330) by sourcing depth from the depth buffer, not the tile flag.
- Wet sand: darken terrain albedo x0.62 and drop roughness 0.25 for 1.5-2.5m inland of the waterline, ramped by the same depth term. Currently there is no albedo difference at all across the shoreline.
- Rivers: advect normals along the spline tangent at 0.35-0.6 m/s with flowmap UVs and a two-phase cross-fade (period 1.4s, phase offset 0.5) to kill popping; scale velocity by 1 - (2d/W)^2 across the channel for shear; add riffle whitewater where bed slope > 0.12 and depth < 0.4m. Stop rendering the centreline spline — that is the 3px line at (880,600)-(950,690).
- Caustics: project two voronoi-crack layers at 1.2 and 0.55 world-unit tiles scrolling opposite at 0.06 / 0.09 m/s onto the seabed, masked by exp(-depth/1.1m), multiplied by direct sun visibility so they vanish in shadow. Add contrast 0.25-0.4 additively (not a multiply over 1.0) and split chroma by 1.5px to sell refraction.
- Depth and colour: sample true depth from the terrain depth buffer instead of a per-tile shallow flag, and extinct as exp(-depth * sigma) with sigma_RGB = (0.35, 0.09, 0.045) per metre — that alone removes the hard contour at (1180-1310,250-320). Model estuary sediment as a separate scattering term (turbidity albedo ~0.42,0.38,0.24 ramped 25-40m from the mouth) rather than lerping the tint green, and depth-clamp the shadow receiver on water to kill the grey smear under (900,830).

## critique 12 — 38/100
Rejected — six automatic fails. Unlabeled and side by side with a real Civ VI in-game screenshot at gameplay zoom, the player calls Civ VI better, and it is not close. The single tell is the ground: your terrain detail does not change size with distance. Measured on the same sand material, HF_rms is 20.8 near-field (380,780), 23.2 mid (250,430) and 20.5 on the far cliff (60,90) — a flat curve across a 5:1 depth range, when a mip-mapped shipping ground should fall roughly 3:1. Because the grain is the same number of pixels everywhere, the desert at (1020,780) reads as per-pixel confetti sprayed on the monitor rather than a material lying in the world, and the far cliffs at (0,60)-(400,320) read as vertical smear rather than rock. Civ VI's ground announces its scale — dune ridges, rock scatter, wet sand — and holds material identity from thumbnail to full zoom. Yours holds at thumbnail and dissolves into noise the moment a player actually looks at a tile. Runner-up tell, visible without zooming: your distance ramp runs backwards. Far land measures mean 70.8 / sat 0.263 against mid-band 110.1 / 0.342 — distance gets darker AND muddier. Real aerial perspective, and Civ VI's colour script, does the opposite: farther is lighter, cooler, lower contrast. What you have is a vignette wearing a lighting model, with a bright diagonal hotspot band through screen centre and mud at both ends. Credit where earned: the composition and coastline silhouette are strong, the UI chrome and typography are genuinely shipping-grade, the grass/sand ecotone at (180,330)-(340,440) is soft and irregular exactly as it should be, the castle at Aurelia is a real asset, and the image is clean on exposure (0.42% crushed, 0.04% blown) with no z-fighting and no UI overlap. That is a good frame under a broken ground shader. Fix the material layer and the colour ramp and this is a different conversation; as it stands it is a competent mockup, not a shipping build.

**Problems**
- AUTO-FAIL, visible noise: near-field sand at (1020,780)-(1120,850) is uncorrelated per-pixel RNG — 1-2px light/dark specks, no dune structure, no direction, no albedo variation above 3px. MID/HF at (380,780) is 0.65, i.e. pixel noise dominates blob structure by 1.5:1 where a shipping ground shader wants 0.9-1.2.
- AUTO-FAIL, streak artifact: mid-field sand at (180,330)-(340,440) is one noise octave stretched ~6:1 along a single fixed screen-diagonal, producing identical ~10px comet-dashes on a lattice. Reads as brushed metal, not sand. Far cliffs at (0,60)-(400,320) have the same defect rotated to vertical — HF 20.5 with MID/HF 0.63, i.e. noise where strata should be.
- AUTO-FAIL, flat-matte surfaces: the cliff faces at (880,110)-(1260,390) are untextured mauve-brown gradients with a hard specular lip and zero rock detail; the rock mesas at (1120,470)-(1190,560) are flat-shaded cream polygons; the hut walls at (700,690)-(800,730) are a single uncontrasted brown fill per face — no planks, no grain, no dirt breakup.
- AUTO-FAIL, props with no contact shadow: the Warrior at (900,510) casts nothing and has no base AO — it hovers over the sand. Same for the pebble scatter at (1060,820), the boulders across (0,60)-(400,320), and the shrub scatter on the beach at (1000,800). Only the larger huts get a shadow, and it is a hard dark smear rather than a contact gradient.
- AUTO-FAIL, hex geometry announcing itself: the grass at (180,180)-(340,300) carries a hard-edged straight-sided lighting wedge — flat per-face terrain normals turning sunlight into a knife-edged polygon on open ground. Inside that lit wedge a regular ~8px diamond UV-repeat lattice is plainly visible. Civ VI never lets a tile reveal itself as a flat-shaded facet.
- AUTO-FAIL, aliased hex/border line: the territory decal over water at (845,690)-(925,745) is a flat ~3px unfiltered strip with 1px stair-steps and no intermediate blend pixel, drawn after resolve so TAA never touches it, and unlit — it does not pick up the water's shading, so it sits on the glass instead of in the world.
- AUTO-FAIL, toy/programmer-art unit art: the unit-panel portrait at (40,700)-(110,790) is a featureless beige egg-head with a black rectangle visor on a flat blue body — placeholder mannequin, not a Warrior. In-world at (870,470)-(960,560) the silhouette is unreadable at 1:1: armour value ~110 against sand ~112, no rim light, no value hierarchy, head and shoulders merged.
- Colour script inverted and hotspotted: far land 70.8 mean / 0.263 sat vs mid 110.1 / 0.342 vs near 95.8 / 0.368 — distance darkens and desaturates instead of hazing, and both near and far fall away from a bright centre band. Additionally the cyan selection glow under the Warrior bleeds unshaped across the sand as a light leak, and a grey unresolved shadow blob floats in the river at ~(800,750).

**Fixes**
- Replace the screen-space noise with a world-space material and a real mip chain. Target 512 px per hex at the default camera height; build sand as 3 world-space octaves at 2.4 m / 0.6 m / 0.15 m wavelength and scale each octave's amplitude by saturate(1 - depth/LODdist) so HF_rms lands 26-30 near, 16-19 mid, 8-11 far — a monotonic 3:1 ramp instead of today's flat 20.8 / 23.2 / 20.5.
- Fix the anisotropy: hash the dune direction per hex over 0-360 deg and drive the stretch axis from the terrain slope gradient so ridges run perpendicular to the fall line, and cap the stretch at 3:1 instead of the current ~6:1 fixed-screen-diagonal smear.
- Lift macro structure without adding pixel noise: multiply in a second albedo layer on a 6 m wavelength mask at 0.12 luminance amplitude. This raises MID_rms while leaving HF_rms alone and pulls MID/HF from 0.65 into the 0.9-1.2 band on near-grass (380,780) and far cliff (60,90).
- Give cliffs their own stratified material: quantize world Y at 0.8 m into 4-6 strata bands with +/-0.06 albedo offset per band, add a matching normal map, bake curvature-based cavity AO at 0.35 strength, and add a grass-drip mask that creeps vegetation 0.3-0.6 m over the lip. Applies to (880,110)-(1260,390) and the mesas at (1120,470).
- Smooth terrain vertex normals across hex boundaries with angle-weighted averaging, keeping hard edges only where the slope delta exceeds 40 deg, then overlay a detail normal at 0.35 m wavelength. This kills the straight-edged lighting wedge at (180,180)-(340,300).
- Kill the ~8px diamond UV lattice with a stochastic hex-tile sampler (Heitz-Neyret), or as a cheap fallback blend 3 UV taps rotated 0 / 37 / 113 deg, plus a 1/17-scale macro breakup multiply.
- Ground every prop and unit: add a blob/capsule AO decal at radius 0.6x footprint and 0.45 opacity, include all props in the sun shadow cascade, and add a 0.15 m screen-space contact-shadow trace so scatter pebbles darken at the base. Non-negotiable for the Warrior at (900,510) and the scatter at (1060,820).
- Reverse the depth ramp: lerp the far field toward a haze colour around #9FB4C8 by 0.25-0.35 at frame top, lifting far land mean from 70.8 to 118-128 and pulling saturation down to ~0.20, so mean rises and saturation falls with distance. Then remove the radial vignette so near (95.8) and mid (110.1) sit within 8% of each other.
- Redraw the territory border as an SDF decal written into the GBuffer before TAA resolve with a 1.5px feather, and modulate it by terrain lighting (multiply by 0.6*NdotL + 0.4) so it stops reading as an unlit sticker on the water at (845,690).
- Make units read at gameplay zoom: enforce a 3-value silhouette (dark 0.12 / mid 0.35 / accent 0.65), add a 1.5px sun-keyed rim light at 0.7 intensity, add the standard ground disc at 0.45 hex radius fading 0.5 to 0.15 opacity, clamp the selection glow to a ground-projected ring decal with additive contribution capped at 0.25 and saturation under 0.5, and replace the placeholder panel portrait at (40,700).

