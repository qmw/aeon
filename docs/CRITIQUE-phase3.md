# Phase 3 critic findings — unfixed backlog

## critique 1 — 31/100
Rejected. Side by side and unlabeled against a real Civ VI gameplay screenshot, a player picks Civ VI in under a second, and the single tell is the ground underneath things. Every object in this frame floats. I measured it: the ground directly under the selected Warrior's feet reads luma 111.8 while the dirt immediately to its left reads 106.2 — the footprint is BRIGHTER than its surroundings. The archer at bottom-right: 38.3 under-feet vs 43.1/46.8 to either side and 37.7 below, i.e. the terrain's own gradient, not a shadow. Neither city casts a single directional shadow onto the sand. The dock pilings enter the lake with no waterline, no ripple ring, no darkening. Civ VI's entire read at gameplay zoom is built on hard sun-direction contact shadows anchoring every unit, tree and building to its hex; without them this is a screenshot with stickers pasted on it, and the eye clocks that instantly even if the player can't name it. Runner-up tell, half a second later: the lake at frame centre is an untextured lavender-grey plane, and the top-right ocean is so dead I sampled 22 consecutive pixels at y=170 that ran 30,33,32,31,35,33,30,29,33,31,32,31,32,30,33,31,34,32,31,30,33,31 — literally zero variance, no wave normal, no specular, no depth absorption, no foam. Beyond those two: the near-field is mip-smeared to mush (the Aurelia city, dock and mid-ground all soft at 1:1; edge SD in the city region is 53.9 against 63.3 in the untouched sand, so the detail is being blurred away by post, not by texture budget), there is no anti-aliasing whatsoever (a border edge at y=480 goes 89 → 169 → 98 across single pixels with no intermediate value, and the hex line over ocean at y=150 breaks into 140,140,116,98,136,105 — a doubled, fragmenting wire that will crawl the instant the camera moves), hex edges are triple-stacked (base grey grid + territory ribbon + selection rim all drawn on the same shared edge, plus a stray orphaned gold chevron floating inside the tile below the "1" stack marker), the ribbons float over and z-fight the slope break right of Solmere, Solmere itself is a kitbash of interpenetrating roof boxes with a tan dome half-buried in a wall, the grading is a flat un-tonemapped log image (world median luma 85, 99th percentile 224, 0.000% of pixels above 250 — the frame never reaches white, so nothing ever glints), the frame is lit by two different suns (left field B−R = −8 neutral, right sand B−R = −58 warm), white fog cards intersect the terrain and sort inconsistently against the border ribbons, and the "…antis" city banner is bisected by the Unit panel with no collision clamping. The HUD is the only genuinely shippable thing here — the toasts, End Turn and top bar read like real product. The world does not. This is a tech demo with a good UI artist on it.

**Problems**
- AUTOFAIL — no contact shadows on anything: selected Warrior's under-feet ground measures luma 111.8 vs 106.2 beside it (brighter than its surroundings), archer at (1175,835) reads 38.3 vs 43.1/46.8/37.7 around it, both cities cast nothing onto sand, dock pilings meet water with no waterline or ripple, tree blobs have no AO ring
- AUTOFAIL — untextured flat-matte surfaces: centre lake is a bare lavender-grey plane with a noise wisp over it; top-right ocean sampled across 22px at y=170 gives 30-35 luma with zero variance, and sits at RGB (51,53,49), a desaturated near-grey with no wave normal, specular, depth absorption or foam
- AUTOFAIL — blurry/mip-smeared near-field: Aurelia, the dock and the whole mid-ground are soft at 1:1; city-region edge SD is 53.9 vs 63.3 in untouched sand, so post is destroying detail. Terrain samplers look trilinear/aniso-1 with a positive LOD bias
- AUTOFAIL — zero anti-aliasing and guaranteed shimmer: border edge at y=480 transitions 89→169→98 across single pixels with no intermediate value; hex line width varies 2-3px across the frame (145-147, 275-276, 339-340 at y=250), which crawls under any camera motion
- AUTOFAIL — illegible and double-drawn hex lines: base grey grid, territory ribbon and selection rim all render on the same shared edge; the line over ocean at y=150 fragments into 140,140,116,98,136,105; an orphaned gold chevron floats inside the tile below the '1' marker; ribbons float over and z-fight the slope right of Solmere
- AUTOFAIL — clipped/overlapping UI: the '…antis' city banner is bisected by the Unit panel with no clamp or fade, left half destroyed and unreadable. Plus a duplicated 'Aeonian engineers raise earthworks.' toast at T30
- AUTOFAIL — toy/programmer-art silhouettes: Solmere is interpenetrating roof boxes with a tan dome half-buried in a wall; banners are flat single-quad ribbons with hard polygon edges; the Warrior portrait is a flat vector helmet on black, not a rendered bust
- AUTOFAIL — crushed, untonemapped grading with two suns: world median luma 85, 95th 189, 99th 224, and 0.000% of pixels above 250, so nothing in the frame ever glints. Left field measures B−R = −8 (neutral) while right sand measures B−R = −58 (warm) — the biomes are lit by different keys

**Fixes**
- Contact shadows, non-negotiable: 3-cascade CSM at 2048² with splits at 12/40/120m, constant depth bias 0.0015, slope-scale 1.8, normal-offset 0.35 texel, 5x5 Poisson PCF at 1.8-texel radius. On top of that a per-object contact term — SSCS raymarch 12 steps over 0.35m world distance, or a blob-shadow decal ellipse 0.9 x 0.55m at 55% opacity with a 0.25m soft edge for units, 1.4x canopy radius at 40% for trees. Single sun at azimuth 135°, elevation 38°.
- Kill the near-field blur: set texture LOD bias to −0.5 and force 16x anisotropic on every terrain sampler (currently reads as aniso-1). Raise bloom threshold to 1.15 and cap intensity at 0.25. If there is a full-screen Gaussian haze pass, delete it outright. Finish with CAS at 0.4 (or RCAS 0.25 if TAA lands first).
- Terrain needs three splat layers, not one noise field: macro at 48m tiling, meso at 6m, detail at 0.75m, blended by height-map weight with blend sharpness 0.25 (not a linear lerp). Detail-layer normal map at strength 0.8 near, faded to 0.2 past 60m, plus per-layer roughness variation so grass stops reading as a matte lambert wall.
- Water rewrite: Beer-Lambert depth absorption from #5b8f8a shallow to #1b3a4a deep with extinction 0.35/m over 0-6m; 2-octave Gerstner normals at wavelengths 4.0m/1.2m, amplitudes 0.06m/0.02m, speeds 0.35/0.9; GGX specular at roughness 0.04 with F0 0.02 for a real sun track; cubemap or SSR reflection; shoreline foam masked from depth over 0-0.7m with 0.5m-tiling noise scrolling at 0.08 m/s; 0.4m wet-darkening band inland.
- Hex grid as one SDF terrain decal, not three meshes: signed distance to hex edge, width pinned to 2.2px at every zoom via fwidth(), core alpha 0.85, plus a 1.1px dark outer rim at alpha 0.35 so it stays legible over both sand and ocean. Merge base grid + territory ribbon + selection rim into a single pass with a style enum. Depth-offset with polygon-offset −1.0/−1.0 instead of a world-space Y lift — the Y lift is what makes the ribbon float and z-fight on the slope by Solmere.
- Anti-aliasing: TAA with 8-frame Halton(2,3) jitter, 0.9 history blend, YCoCg neighbourhood clamp at variance gamma 1.0, then RCAS 0.25 to claw back the sharpness. If TAA is off the table, SMAA T2x. Right now there is nothing — a 1px 89→169→98 spike is untouched geometry.
- Grading: ACES or AgX tonemap, white point 1.0, exposure +0.4 EV, shoulder tuned so sun-facing sand and water specular actually clip past 250. Shadow toe lifted to 0.03 so the left field stops bottoming at 18 on the 1% mark. Targets to hit: 0.3-0.8% of world pixels above 250, 5th percentile 28-32, median 105-115 (currently 85).
- One light rig for the whole map: single directional + sky IBL, no per-biome colour multiply. If a biome tint must stay, cap it at an 8% saturation shift — the current left/right split is B−R = −8 vs −58, which is two suns. Push ocean albedo to roughly (0.02, 0.08, 0.11) and let sky reflection carry it; target a top-right ocean mean near (38, 78, 96) instead of (51, 53, 49).
- Replace the white fog alpha cards with exponential height fog: density 0.008/m, height falloff 0.02, start distance 45m. If cards must stay, give them soft-particle depth fade over 1.5m and force them to sort after the border decals — right now they intersect terrain with hard cuts and sort inconsistently against the ribbons.
- UI collision pass: clamp world-space city banners to a safe rect inset 24px from every HUD panel's bounds, or alpha-fade them to 0 across 40px of overlap — the '…antis' banner is currently sliced in half by the Unit panel. Separately, dedupe toasts on (event type, actor, turn); 'Aeonian engineers raise earthworks.' fires twice at T30.

## critique 2 — 31/100
Reject. Side by side, unlabeled, against a real Civ VI gameplay screenshot: the player picks Civ VI in under a second, and the single tell is the trees — they are not attached to the ground. I measured it, and it is worse than "missing": at the canopy at (665,685) the ground in the contact band reads 114.9 luma while the control ground 30px away reads 99.3/101.2, and at the second canopy (540,410) contact reads 88.2 against a 75.7 control. The terrain under every tree is BRIGHTER than the terrain beside it. That is inverted ambient occlusion. Every forest blob in Civ VI is welded down by a hard contact darkening plus a cast shadow on the sun azimuth; yours are lit cabbages hovering a few centimeters above a noise field, and the eye reads "not real" instantly even when the viewer cannot name why. The second thing that gives it away, and the reason the whole frame reads as a render preview rather than a game, is the full-screen ~2px ordered screen-door dither sitting on top of every surface — visible at 8x on terrain, on water, and on the tree canopies alike. It is in 100% of the frame. In motion it will crawl. Beyond those two: the hex grid draws straight THROUGH the "Aurelia" city banner text, and it renders at three different widths and colors within a single frame (thick cream double-stroke by the city, thin gray hairline in the green band, soft 4px blur near the crater). Biome boundaries are razor-cut on the hex polygon with zero feathering or scatter overlap. The near-field sand is one octave of procedural noise — autocorrelation decays smoothly to 0.44 by lag 4 with no periodic revival, meaning no tiling texture, no normal map, no macro breakup, just static painted on the ground plane. The dark-green hexes are near-uniform flat matte. Far terrain is mip-smeared (HF 6.1 vs 12.7 near) with no anisotropic filtering. And the grade is dead: p99 luma is 219, literally 0.000% of terrain pixels exceed 247, 90% of the image is squeezed into 36-186. Sunlit desert at 2720 BC noon does not top out at 86% white. This is a competent lighting-and-composition tech demo with placeholder art in it. It is not a shipped 4X.

**Problems**
- AUTO-FAIL: zero contact shadow on vegetation, measurably inverted. Ground under canopy at (665,685) = 114.9 luma vs 99.3 control 30px away; second tree 88.2 vs 75.7. Trees are brighter at the contact point than the surrounding ground. Nothing in the frame is grounded.
- AUTO-FAIL: full-screen ~2px ordered screen-door dither over 100% of the frame — confirmed at 8x on sand, lake, city stonework and tree canopies alike (see /home/piotr/looping_opus_5_test/shots/zz_treebase.png). Guaranteed pixel crawl in motion. This is the single artifact that makes the whole build read as a tech demo.
- AUTO-FAIL: hex grid renders on top of world-space UI — a grid line runs straight through the letterforms of the 'Aurelia' city banner at (560-660, 290-360). Grid pass is ordered after banner compositing.
- AUTO-FAIL: hex lines double-drawn and inconsistent within one frame — thick cream double-stroke right of the city, thin aliased gray hairline in the west green band, soft 4px illegible blur near the crater lake. Diagonal edges stair-step every 3-4px with no AA.
- AUTO-FAIL: toy/programmer-art vegetation silhouettes. Every tree is a two-tone lumpy metaball ~28px across — no trunk, no branch structure, no concavity in the outline, no rim light, no variant rotation. Civ VI reads forest props at ~40px with visible trunks and cut leaf cards.
- AUTO-FAIL: untextured flat-matte and mip-smeared terrain. Dark-green hexes at (250-520, 380-520) are near-uniform vertex color. Near-field sand is single-octave noise (autocorr 0.849/0.639/0.506/0.441 at lag 1-4, no periodic revival = no tiling material, no normal map). Far terrain HF 6.1 vs near 12.7 with no anisotropic filtering.
- AUTO-FAIL: washed-out grade. Terrain p1/p5/p50/p95/p99 = 19/36/85/186/219, with 0.000% of pixels above 247 and 0.24% below 8. Sunlit desert never breaks 86% white; 90% of the image lives in a 150-level band. No specular pop anywhere on sand or water.
- Biome transitions are hard-clipped to the hex polygon — the sand/green boundary at (150-550, 300-540) stops dead on the edge line with zero blend feather and zero prop overlap straddling the seam. Shoreline decal is a string of evenly-spaced orange dots, not a beach.

**Fixes**
- Kill the screen-door dither. If it is dithered alpha on the cloud/fog cards, switch those to premultiplied alpha and resolve properly. If it is a deliberate quantization dither, replace with triangular-PDF blue noise at ±0.5 LSB (1/255) applied only AFTER tonemap, sampling a 64x64 blue-noise LUT offset by frame_index * golden-ratio. Current amplitude measures ~±6-10 LSB — 12-20x too strong.
- Ground the props. Per-prop AO decal: radial multiply gradient, outer radius 1.6x canopy footprint, 0.42 opacity at center falling to 0.0 at the rim, blended into ground albedo before the grid decal pass. Then a real cast shadow — sun reads at roughly 38 deg elevation / 135 deg azimuth from the sand-region shading, so offset each canopy shadow 1.28x canopy radius along that azimuth. Target: contact band 25-40 luma DARKER than control, versus the +14 it is today.
- Rebuild the tree props. Minimum ship bar: 3 canopy meshes x 3 yaw rotations x 2 scales (0.85 / 1.15), each with a trunk 4-6px wide at gameplay zoom, an alpha-cut silhouette carrying 8-14 outline concavities, and a 3-band canopy gradient with a warm rim term at 135 deg azimuth. Randomize per-instance hue ±6 deg and value ±8%.
- Layer the terrain material. Per biome: (a) 2048px tiling albedo at 1.5 tiles/hex for grain, (b) normal map at 4.0 tiles/hex, strength 0.6, (c) macro variation mask at 0.08 tiles/hex multiplied over albedo to kill tile repeat, (d) triplanar projection on any slope past 25 deg. Target near-field HF (mean abs neighbour delta) 18-24; you are at 12.68 and all of it is noise, not structure.
- Fix the far field: clamp mip LOD bias to -0.5 and turn on 16x anisotropic filtering. Far terrain HF of 6.12 against near-field 12.68 with visibly smeared texels means aniso is off or set to 1x. Target far/near HF ratio 0.75 or better, not 0.48.
- Dress the flat green tiles. Scatter decal set of 6 grass-clump variants, 12-20 instances per hex, alpha 0.4-0.9, random yaw, plus a detail normal at 8 tiles/hex strength 0.35. The dark-green hexes currently carry almost no albedo variance.
- Feather the biome seams. Sample the neighbouring tile's material weight per-vertex and blend across 25-35% of the hex apothem with a noise-perturbed mask (3-octave, 0.15 apothem amplitude) so the seam is never a straight line. Add 8-12 scatter props straddling each transition edge.
- Rebuild the grid as ONE pass. Single deferred decal written into the GBuffer BEFORE any UI composite: constant 1.5px screen-space width, 0.5px smoothstep AA falloff, one color, alpha 0.22 on land / 0.12 on water. Delete the other two grid treatments. Depth-sort city banners and unit flags above the decal pass so no line ever crosses banner text.
- Regrade. Push the S-curve so sunlit sand and water specular reach 240-250 at the top 0.4-0.6% of pixels (currently 0.000% above 247), pull the black point down to 12-16 at p1 (currently 19 with p5 at 36), hold p50 at 95-105 (currently 85). Give sand a real spec response — roughness 0.55, specular 0.35 — so you get highlight pops instead of a flat matte ceiling at 219.
- Replace the shoreline bead-string. Foam / wet-sand gradient decal, 3 variants, 18-24px band width, driven by distance-to-water rather than snapped to the hex edge, plus 5-9 rock props per shore edge at randomized spacing. Also: the notification stack is printing 'Aeonian engineers raise earthworks.' twice at T30 — duplicate toast, worth a look even though it is outside this pass.

## critique 3 — 26/100
Rejected. Six automatic fails, and the pass fails its own brief: of the nine items named (ocean shading, sky reflection, specular, coast foam, wet sand, rivers, ship interaction, distant shimmer) exactly one — a passable deep-ocean hue ramp — is present. There is no ship anywhere in the frame, so "ship interaction" is untested, not shipped. Side by side with a real Civ VI screenshot, unlabeled, a player calls Civ VI better inside half a second, and it is not close. The single tell: this sea never catches the sun. The frame is lit from the upper-left at roughly 40 degrees — every building and tree throws a hard shadow down-right — yet the brightest pixel in 8,000 sampled deep-ocean pixels is luminance 142/255, and 0.000% of the entire map area reaches 250. No glint lobe, no Fresnel sky term, no highlight of any kind. Civ VI's ocean has a broad moving specular sheet and a hard white surf ring welded to every coast hex; this one is matte construction paper with weather painted on it. The corroborating tell, once you look for a second: the only white anywhere near the shoreline is a fog volume that also sits on top of the mountain ridge and the red city roofs — the sea and the land are wearing the same cloud, so nothing reads as surf. And the harbour water at (700-830, 560-660) measures RGB 148,139,142 with 55% of its pixels below chroma 20 and the red channel ABOVE the green: that is not water, that is grey smoke with a pier standing in it.

**Problems**
- No specular and no sky reflection anywhere on water. Deep ocean (1180,120)-(1280,200) peaks at luminance 142/255 with std 14.4 — a flat matte gradient. Whole-map p99 is 224 and nothing hits 250. Untextured/flat-matte surface: automatic fail.
- Zero coast foam. There is not one shoreline-parallel surf band in the frame. What reads as foam is a fog/cloud volume that ignores the land mask entirely — the same puff crosses the beach at (1250,430), climbs the ridge, and lands on Solmere's roofs. Land/water boundary is a bare vertex-colored polygon edge.
- Harbour water at (700-830,560-660) is achromatic: RGB 148,139,142, mean chroma 22.8, 55% of pixels under chroma 20, R>G. It is rendering as smoke, not liquid. Four incompatible water materials in one frame — navy river, teal shelf, cyan pond at (880,470), grey harbour. No unified shading model.
- Double-drawn hex networks over the ocean at (1130,150)-(1260,250): a sub-pixel pale grid that dashes in and out and is illegible over teal, PLUS a second network of hard single-pixel straight tint seams that do not follow the hex grid at all — flat-shaded seabed triangles reading through an unlit water plane. Automatic fail.
- No contact shadows on water. The two pier pilings at ~(730,600) and (745,560) and the whole dock structure sit on the harbour surface with no shadow, no waterline darkening, no ripple ring, no reflection. Objects float. Automatic fail.
- Near-field terrain is mip-smeared to mush. The beach/hex junction at (1240-1340,430-500) at 8x has zero texel structure — soft-focus paint. The city and pier at (660-760,470-540) are unreadable blobs. Automatic fail.
- Washed-out grading. Shallow shelf at (1330,380)-(1450,470) measures RGB 174,179,150 — the fog has pulled the sea to a warm neutral beige and hue-shifted it yellow-green. Map median luminance 87 with p1 at 24: heavy, milky midtones across the entire water pass. Automatic fail.
- Reads as a tech demo at the top edge: the ocean simply stops at y<8 into a flat navy band the same value as the UI chrome. No horizon, no sky, no atmospheric terminator. Plus no distant shimmer of any kind, and no wet-sand band at any waterline in the frame.

**Fixes**
- Specular: GGX on the water plane, roughness 0.045-0.08, F0 0.02 (IOR 1.333). Sun at the ~40deg elevation the existing shadows already imply; size the glint lobe to cover 8-14% of visible ocean and peak at 230-245 luma pre-tonemap. Break it with geometry, not a sparkle texture: three scrolling normal maps at world-tile scale 1.0 / 0.37 / 0.11, strengths 0.55 / 0.30 / 0.15, scroll 0.035 and -0.019 uv/s, directions 28deg apart.
- Sky reflection: Schlick Fresnel, F0 0.02, power 5. At this ~57deg camera pitch that puts frame-edge water at 0.25-0.40 reflectance. A 64px sky cubemap is enough; even a two-stop ramp (zenith 88,124,168 -> horizon 176,196,206) times Fresnel beats the current zero.
- Kill the per-triangle water tint that is causing the hard straight seams at (1130,150)-(1260,250). Replace with Beer-Lambert absorption off the scene depth buffer: sigma_rgb (0.42, 0.11, 0.055) per world unit, depth-0 color 0.44,0.72,0.68, deep asymptote 0.035,0.10,0.15.
- Coast foam, two layers. (a) Jump-flood an SDF from the land mask at map gen, 8px per hex-edge; alpha = smoothstep(0, 0.55 hex-widths, 1-d), masked by noise scrolling perpendicular to the coast at 0.02 uv/s, strength 0.35. (b) Depth-buffer intersection foam for a crisp contact line 0.15-0.25 world units wide at 0.85 alpha. Foam color 246,248,244, falling to 0 over 0.6 hex-widths.
- Cut the fog volume off water entirely inside 6 hex-widths of camera, and drop its density 65% globally. It is currently doing the foam's job, the shoreline's job, and destroying the grading — that one change recovers the shelf from RGB 174,179,150 toward 96,178,172 and deep water toward 22,52,74.
- Fix the harbour material assignment before tuning anything: it is on a fallback shader or the fog is z-sorting in front of it. Inland water should be the ocean shader with sigma scaled 0.6x and depth-0 color 0.38,0.55,0.50. Target mean chroma above 55 and R below G.
- Wet sand: 0.35 hex-widths inland of every waterline, multiply albedo 0.62, raise smoothness 0.05 -> 0.55, neutral specular tint. Breathe the band width +/-0.06 hex-widths at 0.11 Hz.
- Contact shadows and ship: screen-space contact shadow on every water-intersecting object, 8 steps, 0.4 world-unit max ray, 0.55 opacity, plus a 0.12-unit waterline darkening ring at 0.3 alpha and a 0.06-unit foam ring. Then actually put a hull in the shelf water with a Kelvin wake — 19.5deg half-angle, twin foam arms decaying over 6 hull lengths, displaced bow-foam quad — or this bullet stays unreviewed.
- Water grid: render at 2.0px constant screen-space width, 0.28 alpha, colored as the local water darkened 35% rather than a pale overlay, and cull it entirely past 14 hexes where it is only aliasing. Fixes the illegible dashed grid over the ocean.
- Near-field sharpness: terrain mip bias -0.5, 8x anisotropic, and audit splat resolution — at this camera height the beach hex at (1240-1340,430-500) needs roughly 1024px of albedo per hex and is getting about 128. For distant water, lerp the normal-map far mips toward (0,0,1) by 0.8 beyond 20 hexes and render water at native res under TAA, or the new glint will crawl worse than the current flat matte.

## critique 4 — 34/100
Reject. Nine of your ten automatic fails are present in a single frame, and the unit pass — the thing this review was scoped to — is the weakest layer in the build.

Side by side, unlabeled, with a real Civ VI gameplay screenshot: the player picks Civ VI, instantly, and it is not close. The single tell is the units' feet. In Civ VI every unit is nailed to the terrain by a soft contact shadow plus a sun-direction cast shadow, and you can see boots on ground. In /home/piotr/looping_opus_5_test/shots/units-p3r1.png every unit is legless and shadowless — the tunic hem simply stops in mid-air and the ground texture continues underneath, completely unmodified. The spearman at ~(1180,790) on the sand is the cleanest example: zoom it and the figure is a decal, not an object in the world. A player will not name "contact shadow occlusion," they will say "the little guys look pasted on," and that one read poisons every other judgement they make about the frame.

Second tell right behind it, and the reason this reads as a tech demo rather than a shipped 4X: your figures are stacks of untextured spheres. The unit at ~(490,760) is two brown spheres for shoulders, one tan sphere for a head, no neck, no hands, no face, one flat blue quad for a tabard. That is asset-store placeholder geometry. The HUD around it — the top resource bar, the notification stack, the unit panel, the city banner typography — is genuinely competent, better than the 3D by a wide margin, and that gap is itself damning: it tells a reviewer the UI shipped and the content didn't.

Third: nothing in this frame has a specular highlight. Measured luma across the gameplay area runs 24 to 237 — no black point, no white point, zero pixels below 8 or above 247. Combined with the milky ground fog sitting on the NEAR-field hexes (bottom center, closer to camera than the cities, and blown to featureless white) the whole play surface is a low-contrast brown-teal wash. Civ VI hits 10 in the tree cores and 250+ on armor and water glints in the same frame.

And the frame ships visible UI defects: "Atlantis" is truncated at the left viewport edge, that same banner is drawn opaquely straight through a unit's torso at ~(490,770), and the XP stat tile in the unit panel is clipped mid-border at the panel's right edge. Three separate clipped/overlapping-UI fails in one screenshot.

Note also this is a 1600x900 capture. Every aliasing and mip problem below gets worse at 1440p, not better — you are being flattered by the low resolution.

**Problems**
- AUTOFAIL - no contact shadows, systemic. Zero units, trees, or props cast or receive any shadow or AO. The spearman at (1180,790) and the selected warrior at (800,420) both float; the tunic hem terminates and untouched ground texture runs beneath it. There is no grounding cue of any kind in the entire frame.
- AUTOFAIL - toy/programmer-art silhouettes. Units are sphere stacks: the figure at (490,760) is two brown spheres (shoulders/pack), one tan sphere (head), no neck, no hands, no face, no legs. The unit panel portrait at (95,760) is a mannequin with two black slots for eyes. This is placeholder geometry, not shipped content.
- AUTOFAIL - untextured / flat-matte surfaces. Tunics, helmets, arms, and shields are single flat albedo with no normal, no roughness variation, no specular anywhere in frame. Solmere's gold dome at (1055,435), the blue roof quads at (1150,470), and the cyan river ribbon at (1090,415) are pure flat-shaded polygons.
- AUTOFAIL - double-drawn and inconsistent hex lines. Three different edge treatments coexist: a thick ~5px cream ribbon, a 1-2px hard white geometry line, and a faint low-contrast stroke that vanishes in fog. Shared edges near (880,340) and (500,690) draw two parallel rails because each hex insets its own border. The selected hex uses the same cream ribbon as territory borders, so selection is not distinguishable.
- AUTOFAIL - aliasing with guaranteed motion shimmer. The grid line scanned at y=600..612 jumps x=440,439,437,436,433,430 with peak luma swinging 174-195 row to row. No AA of any kind: flagpoles, spear shafts, roof edges, and banner cloth are all hard stair-stepped. This will crawl and sparkle the moment the camera moves.
- AUTOFAIL - blurry near-field terrain plus washed grading. The near-field band at (600-900, 700-880) is a milky low-frequency smear with the ground-fog decal blowing terrain to featureless white in front of the camera, and a mip transition is visible mid-crop. Gameplay-area luma sits entirely in 24-237, 0% below 8, 0% above 247 - no black anchor, no highlight.
- AUTOFAIL - clipped and overlapping UI, three instances. 'Atlantis' truncated to '...ntis' at the left viewport edge (x=0, y~782); that banner drawn opaquely through a unit's torso at (490,770); the XP stat tile clipped mid-border at the unit panel's right edge (x~395, y~795).
- City growth does not read, and geometry interpenetrates. Aurelia (pop 7) and Solmere (pop 5) are the same kitbashed pile of grey blocks with red cones - Solmere arguably reads larger. No footprint growth, no wall tier, no landmark. The river spline clips straight through Solmere's masonry at (1085,420); roof planes interpenetrate walls at (1175,455). Banners carry no civ emblem and no growth/food indicator, so ownership and growth state are both unreadable.

**Fixes**
- Ground every unit. Cascade-0 shadow map 2048x2048 over a 60m radius around camera focus (~3cm/texel), PCF 3x3 at 1.5-texel radius, normal-offset bias 0.02m, depth bias 0.0008. Match the existing key light (upper-left, ~135deg azimuth, ~48deg elevation) so cast length is ~0.9x unit height. Add a multiply-blended blob-AO decal underneath: ellipse 0.9x hex inner radius, alpha 0.55 at center smoothstepping to 0 at rim, projected along -Y with a 4-sample height probe so it conforms to slope.
- Give units legs and a footprint. Extend the mesh 0.12-0.18 hex-radius below the tunic hem with two leg cylinders and foot planes, then sink the root 0.02 units into the terrain so the intersection line is hidden. Sample terrain height at 3 points under the footprint and blend the unit up-vector 0.35 toward the terrain normal so units lean on slopes without tipping.
- Turn on AA. TAA with 8-sample Halton(2,3) jitter, 0.92 history blend, YCoCg neighborhood clamp at 1.25 variance gamma; or minimum MSAA 4x plus FXAA on composite. Non-negotiable before any further art review.
- Stop drawing the hex grid as geometry lines. Move it into the terrain material as a signed-distance decal: smoothstep(w, 0, abs(sdHexEdge)) with w = fwidth(sdHexEdge)*1.2. Pixel-width-stable at every zoom, perfectly AA'd, and the crawl disappears.
- Kill the doubled border rails. Build territory borders from the outline of the owned region - march boundary edges once, dedupe shared edges - and extrude ONE ribbon per territory at 0.09 hex-radius width, offset 0.015 along the terrain normal, 0.4-alpha inner falloff. Make the selected hex a different treatment entirely: animated dashed white inner line at 0.05 width, never the cream territory ribbon.
- Real materials on units. 512x512 albedo + ORM + normal per unit. Roughness 0.75 cloth, 0.35 leather, 0.22 metal; metallic 1.0 / roughness 0.28 on shield boss and helmet rim so something in the frame finally catches the sun. Add a triplanar detail normal at 0.25 strength, 12 tiles/m, to stop the plastic look at close zoom.
- Fix silhouette and scale. Unit height including banner is currently ~62% of hex height - bring it to 42-45% and shrink the personal banner, which is presently taller than the figure. Widen shoulder-to-hip taper to 1.6:1, add a helmet crest or shield edge that breaks the circular top-down read, and add a 2-3px rim shader (fresnel power 3.0, civ color at 0.6 strength) so units separate from busy terrain.
- Get the fog off the play surface. Move fog start from ~0 to 0.55x camera distance, cap ground-fog alpha at 0.35, and convert from screen-blend to exponential height fog (density 0.02/m, height falloff 0.15). The camera-centered hex must render 100% fog-free at gameplay zoom.
- Regrade. Pull the black floor from 24 down to 6-10 and let speculars reach 250+; target p1 ~10 and p99 ~245. Filmic S-curve mapping 0.18 mid-grey to 0.38 output, plus +12% saturation weighted to the shadow end so terrain stops going grey-brown.
- Make growth read, and rebuild terrain detail. Drive city mesh count off population in visible steps: pop 1-3 = 4-6 huts at 0.35r footprint; 4-6 = 12-16 buildings plus palisade at 0.55r; 7-9 = 24-30 buildings plus stone wall at 0.7r plus one landmark tower - footprint radius is the read, not height. Add a food ring around the banner pop chip and a civ emblem disc. For terrain, replace the single 45deg-streaked top-down noise with a 3-layer triplanar splat at 0.8m/4m/16m tiling, height-blended not alpha-blended, plus 6-10 instanced rocks and 20-40 grass tufts per hex LOD-culled at 45m, and blend biome transitions across 0.25 hex-radius instead of cutting hard on the hex polygon edge.

## critique 5 — 41/100
Reject. Side by side, unlabeled, at gameplay zoom, the player calls Civ VI better within about a second, and the single tell is that your frame gets BLURRIER toward the camera. I measured it: mean edge energy in the near-field sand band (700-1150 x, 760-890 y) is 20.7, while the mid-field grass is 56.5 and even the far-field grass is 29.3. Detail falls off toward the viewer instead of away. Civ VI's nearest tiles are the sharpest, busiest thing on screen — detail albedo, detail normal, and a carpet of scatter meshes all kick in inside ~30m. Yours are a soft watercolor marble smear with zero texel-scale information; the bottom-right third of the frame at 3x zoom (/tmp/rev_G_closest_bottom.png) is a low-frequency fbm cloud with brown veining and literally nothing else. A player does not need to know the word "mipmap" to read that as out of focus. Everything else — the contour rings, the dither bands, the extruded-prism cliffs — is a second-order confirmation of the same verdict. Multiple hard automatic fails present: near-field mip smear, visible ordered-dither stipple, untextured flat-matte cliff faces, a prop with no contact shadow, illegible and double-drawn hex lines with three styles in one frame, toy sphere-clump tree silhouettes, and aliased grid staircases. This is a competent tech demo of a hex renderer, not a shipping 4X terrain.

**Problems**
- Near-field terrain is the softest surface in the frame. Measured edge_mean 20.7 in the closest sand vs 56.5 mid-field and 29.3 far-field. At 3x (crop 560,740-960,900) the closest tiles are smooth fbm marble with no grain, no ripple, no pebbles, no scatter. Detail LOD is inverted; this alone is an automatic fail.
- Ordered-dither / screen-door stipple bands, plainly visible and unresolved. The water hex at (680,590)-(900,720) has a diagonal checkerboard band cutting through it; more stipple appears in the shadowed band at (1130,540) and along the shore at (600,760). Dithered alpha with no TAA resolve — it will boil violently in motion.
- Topographic contour banding inside every hex. Crop (20,60)-(420,300) shows concentric onion rings centered on each hex and hard terracing in the grey-purple hexes at top-right. The terrain reads as a contour map. That is an 8-bit-quantized height or curvature term, and it is per-hex-radial, so the source is distance-to-hex-center with no domain warp.
- Cliffs are untextured extruded hex prism sides. The coast wall at (1140,360)-(1440,580) is a flat matte beige-grey polygon with a linear gradient and a razor-straight top edge. No strata, no normal map, no rubble, no talus, no AO at the base. It is a colored quad standing on end.
- Hex grid is drawn three different ways in one frame and double-drawn on shared edges. Thick cream/gold bands on some edges, 2px flat white on others, near-invisible 1px grey scratches over the near-field sand. The tile at (680,590) changes grid style between its own upper-left and lower-right edges. At (1130,470)-(1290,570) a white line and a parallel dark line sit on the same edge, hard-aliased into visible staircases.
- Props float. The pebble at approximately (925,690) has zero contact shadow. Tree shadow decals are detached ellipses offset up to ~18px from their trunks, and they disagree on sun direction: the big canopy near (960,540) casts down-left while the tree near (200,105) casts up-left. Trunks emerge from unshaded bright ground.
- Trees are unioned spheres with hard faceted silhouettes and a single LOD. The canopy at (940,510)-(1010,570) is a lumpy green mass with no branch structure, no leaf cards, no translucency. Every instance shares the same green and near-identical yaw. Programmer-art silhouette at the exact zoom where the player is looking.
- Grading is crushed and washed simultaneously, and the haze is non-monotonic with depth. Far-field grass sits at lum_mean 53.7 / std 28.5 (flat, near-black), while a milky white wash peaks in the MID-ground and blows the sand toward white. Aerial perspective that is strongest in the middle band reads as a smudged lens, not atmosphere. Water is albedo-only with per-vertex lighting on coarse triangles — visible flat triangular facets across the open ocean at (980,70)-(1380,310), zero specular, zero sun glint.

**Fixes**
- Add a detail-albedo + detail-normal layer keyed to view distance and fix filtering. Tiling grain texture at 0.35m world period (~8-12px on screen at this zoom), weight = 1 - smoothstep(18.0, 45.0, viewDist), albedo overlay-blended at 0.35, detail normal at 0.6 strength. Set terrain sampler to 16x anisotropic and mip LOD bias -0.75; at a 60-degree surface tilt with aniso=1 you are sampling a high mip on the nearest tiles. Target: near-field edge_mean 55-70, i.e. at or above the mid-field, never below.
- Replace the single fbm marble base with a 4-layer height-blended splat: sand-grain, gravel, dry-crack, vegetated patch. Use heightlerp with blend contrast 0.15 rather than linear alpha weights so transitions get a real interlock edge. Add a macro variation texture at 64m period, 0.12 strength, multiplied over the result to break the visible 6-8m repeat.
- Kill the contour rings. Store the height/curvature term as R16F or R32F, not R8. If any AO or occlusion term is baked to 8-bit, add interleaved-gradient-noise dither of +/-1/255 before quantizing. The rings are radial per hex, so the term derives from distance-to-hex-center: domain-warp it with 3 octaves at amplitude 0.25 hex-radius before use.
- Rebuild cliffs as a separate submesh, not a prism side. Triplanar rock material, world-space projection at 2.0m texture period, blend sharpness 8.0, real normal map plus 3-5cm parallax or displacement. Add a vertical AO gradient darkening the bottom 30% of the face to 0.55, and scatter 6-10 rubble meshes per cliff edge with a 0.5m talus wedge at the base so the ground intersection is not a razor line.
- One grid, one shader. Draw the base hex line as an SDF in the terrain shader with screen-constant width: w = fwidth(hexDist) * 1.2, giving a 1.2px line with free analytic AA at every depth. Delete the separate gold band mesh — it is z-fighting and double-drawing against the SDF. If territory borders need gold, that is a SECOND thicker SDF (3.5px) drawn only on ownership boundaries, offset 4px inward so it is never coincident with the base grid.
- Eliminate the screen-door dither. Either enable TAA with an 8-sample Halton(2,3) jitter, or switch the fading materials to alpha-to-coverage under MSAA 4x, or simply use straight alpha blending for the water/shore/LOD fades. Dithered alpha with no temporal resolve is not shippable at any zoom.
- Give every scatter prop a real contact shadow. Screen-space contact shadows at 8 steps / 0.15m max trace distance, or authored AO decals with radius 0.6x prop footprint, opacity 0.55, projected at the prop pivot with ZERO offset. Lock all shadow decals to the single sun vector — currently they disagree by roughly 90 degrees across the frame.
- Rebuild tree canopies from crossed alpha-tested leaf cards, 2-3 per branch, 4 LODs, with the vertex normal blended 60% toward the canopy sphere normal so lighting stays soft instead of faceted. Randomize per instance: yaw 0-360 degrees, uniform scale 0.85-1.2, hue shift +/-8 degrees, value shift +/-10%.
- Feather the biome boundaries off the hex edge. Blend splat weights across a band 0.35 hex-radius wide, masked by noise warped at 0.12 hex amplitude so the boundary is irregular rather than geometric, and scatter 8-15 transitional props into that band (dry tufts on the sand side, sand patches on the grass side).
- Fix the haze and the water. Swap the mid-ground wash for exponential height fog, density 0.008/m, start distance 25m, so density rises monotonically with depth; cut overall strength ~40% and lift the far-field shadow floor to reach lum_mean 70-80 with std 38+. For water: shade per-pixel (the ocean triangles are ~8m with per-vertex normals), add a GGX lobe at roughness 0.04 for sun glint, two scrolling normal maps at 4m/0.03 m-s and 11m/0.017 m-s in opposing directions, depth absorption transmittance = exp(-depth * vec3(0.35, 0.12, 0.08)), a wet-sand band over the nearest 1.2m of shore at 35% darker albedo and roughness 0.15, and a foam decal driven by 1 - smoothstep(0.0, 0.6, waterDepth) animated at 0.25Hz. Then carpet the near field: 40-80 clutter instances per hex, distance cull 35m with an 8m fade band. The clutter buys more 'shipped game' read than every shader change above combined.

## critique 6 — 47/100
Unlabeled, side by side at gameplay zoom, the player calls the Civ VI screenshot better, immediately and without hesitating. The single tell is the shadows: Solmere's keep, the flag mast, every plank of Aurelia's bridge, the selected Warrior and every tree sit on the terrain throwing nothing. There is one soft shapeless blob under Aurelia's south wall that doesn't match the castle's silhouette and doesn't agree with the sun the grass is lit by — that's an AO capsule, not a shadow map. The result is that every object reads as pasted onto a painted board rather than standing on ground, and a player will say "the other one looks like the pieces are stickers" without knowing why. Second tell, half a beat behind: the territory borders. A scanline across the selected hex at y=500 finds a 4px hard gold band at x821-824, a five-pixel gap, then a separate 5px cream band at x830-834 — two ribbons on one edge, both with a 1px transition to background. Civ VI's borders are one thin band that hugs the terrain; these are doubled, stair-stepped, and swing 40% in luma inside a single ribbon (161→237 within one band at y=640), which means they will crawl violently the moment the camera moves. Credit where due, and it makes the diagnosis worse: the UI is genuinely close to shipping. The END TURN plate, the notification stack, the minimap chrome and the unit panel typography are the work of someone who knows what they're doing. That polished frame wrapped around a world render with seven automatic fails is exactly the signature of a tech demo, and the contrast makes the map look worse than it would with placeholder UI. The grading is the one non-fail — p1=18, p99=245, mean 118, mean saturation 0.371, not washed and not crushed. Everything inside the frame is broken; the frame is fine. Reject.

**Problems**
- AUTOMATIC FAIL — no cast shadows. Solmere's entire city (crop 1050-1220, 400-510) throws nothing onto the sand despite a tall keep and mast with the sun clearly upper-left. Aurelia's bridge planks (700-800, 505-570) each float on flat grey with zero occlusion. The blue Warrior at (785,400) has no ground contact of any kind. The only shadow in frame is a soft blob under Aurelia that does not follow the castle silhouette — no tower shadows, no crenellation break-up, no penumbra structure.
- AUTOMATIC FAIL — near-field is the blurriest terrain in the frame, exactly backwards. Measured mean per-channel gradient energy: 7.65 in the near sand (760-1140, 700-890) and 8.24 at (430-700, 760-890), against 18.59 in the mid-field grass (150-430, 430-620). The largest texels on screen carry less than half the detail of the mid band. Sand streaks read isotropic — the classic aniso=1 signature — and Solmere's roof tiles are a mip-smeared speckle.
- AUTOMATIC FAIL — hex lines doubled and inconsistent. Scanline y=500 across the selected hex's left edge: gold band x821-824 (152,123,63), five-pixel gap, cream band x830-834 (218,201,163). Scanline y=470 right edge: cream at 950-953 with R clipped to 255, then a separate gold band at 959-962, then more gold at 967-968 and 974. Scanline y=640: gold at 470-482, gap at 483-485, gold again 486-505. Width ranges from a 4px hard-edged gold ribbon near-field to a ~10px desaturated grey wash at y=200 x220-232, and the grid disappears entirely over the bright sand around Solmere.
- AUTOMATIC FAIL — effectively zero anti-aliasing. Flag pole cross-section at y=360 steps 164→38 across a single pixel (126 luma). Castle tower silhouette at y=380 ping-pongs 117→46→116→145→210 with no coverage ramp — subpixel geometry thrashing, not an edge. The border ribbons transition to background in one pixel (824:L123 → 825:L73). Ocean box (1300-1580, 360-560) carries 274 isolated pixels more than 45 luma above their 4-neighbour mean — 0.49% of the surface will boil under any camera motion.
- AUTOMATIC FAIL — untextured, flat-matte surfaces. Aurelia's plaza (840-980, 480-570) is a smooth grey-blue gradient with no texel structure at all — lag-2/lag-1 gradient ratio 0.83, i.e. pure gradient, no material. The dock planks are flat brown boxes. The stepped hex side faces between elevation tiers are flat dark matte with no rock, no normal, no roughness break-up.
- AUTOMATIC FAIL — toy/programmer-art silhouettes. Trees are 3-5 solid faceted lobes with a hard specular facet on the top plane and no leaf-card breakup — cabbages, visible at (860-960, 440-500) and across the near-field. The selected Warrior resolves to roughly 30x45px of undifferentiated blue smear with a gold ellipse for a shield; you cannot read it as a soldier at gameplay zoom, where Civ VI units stay legible. The unit-panel portrait is a featureless grey mannequin with a blank helmet.
- AUTOMATIC FAIL — clipped UI. The third city's world banner is hard-cut mid-word by the unit panel, leaving the fragment "atis" with an orphaned hammer/4 chip at (400-580, 735-780). No fade, no reflow, cut through the glyph. Separately, the notification stack shows two byte-identical toasts — "Aeonian engineers raise earthworks." T30 twice — which is a feed dedupe bug, not art, but it reads as debug spam and I'm flagging it rather than leaving it for you to find in review.
- No aerial perspective, plus a per-channel highlight clip. Mean saturation by depth band is flat: 0.391 far (y60-240) against 0.378 near (y600-780) — distance does nothing to the palette, so the far field reads as mud rather than haze. What atmosphere exists is a single cloud-shadow multiply, which is the best-judged element in the whole render but is doing the job of four passes. Tonemapping clips per channel with no shoulder: R hits 254+ on 0.805% of the map, G on 0.148%, B never — so every warm sand highlight rotates orange as it saturates instead of rolling to warm white.

**Fixes**
- Shadows: ship 4-cascade CSM, splits at 6/18/50/140 hex-radii, 2048² per cascade, constant depth bias 0.0015 with normal-offset 0.35 texel, 16-tap rotated-Poisson PCF on cascade 0 and 3x3 PCF beyond. Drop sun elevation from the current ~55° to 38° at azimuth 315° so towers throw a silhouette ~1.3x their height. Acceptance: each Aurelia bridge plank shows a distinct 3px shadow offset down-right, and Solmere's keep lays a readable silhouette across at least one neighbouring hex.
- Contact shadows: add a screen-space raymarch on top of the CSM — 8 steps, 0.35 hex max trace distance, 0.02 hex thickness, 0.6 max darkening. This is what gives tree trunks, plank ends and unit feet their 2-4px seat where the cascade texel is too coarse. Pair every foliage and unit instance with a ground-blend decal (radius 0.9x trunk / 1.2x unit footprint, 0.45-0.65 opacity) so nothing floats even at LOD2.
- Anti-aliasing: TAA with 8-frame Halton(2,3) jitter over ±0.5px, YCoCg neighbourhood clamp at variance gamma 1.25, 0.4px sharpen on resolve. Minimum acceptable substitute is SMAA T2x. Enable alpha-to-coverage at 4x MSAA on foliage cards. Acceptance: the y=360 flagpole cross-section becomes a 3px ramp instead of a 1px 126-luma step, and the ocean isolated-speck rate drops from 0.49% to under 0.05%.
- Near-field detail: the detail blend is being driven by camera distance and peaks in the mid band — drive it by texel density instead, weight = saturate(1.5 - log2(texelsPerPixel)). Add a second detail octave at 0.25 hex tiling with 0.35 normal strength that stays fully on to the bottom of the frame. Force aniso 16x on all terrain samplers (currently reads as 1x) and set terrain albedo/normal LOD bias to -0.75 once TAA is in. Acceptance: near-field gradient energy at (760-1140, 700-890) rises from 7.65 to at least 16.
- Hex grid: delete the geometry ribbon entirely and render the border in the terrain shader from a signed distance to the hex edge. Seam width w = max(0.018 hex-radii, 1.4*fwidth(d)) with alpha = 1 - smoothstep(w-fw, w+fw, d) — one band per edge, ~1.6px at this zoom, AA'd for free. Territory colour uses the same SDF at 2.2x width and 0.55 alpha drawn *under* the seam, never beside it. Selection ring is the same SDF at 3px with animated 0.6-1.0 alpha drawn *on* the edge, not offset 5px inward as it is now.
- Border stability: composite the border as a fixed-luma overlay rather than a multiply against terrain albedo. The current multiply is why one ribbon swings 161→237 luma pixel-to-pixel at y=640; that is guaranteed crawl. Clamp the underlying terrain contribution to ±8% before compositing, and keep the border out of the cloud-shadow multiply so it stays legible over the bright sand near Solmere, where it currently vanishes.
- Flat surfaces: city plaza gets a cobble albedo at 0.5 hex tiling with normal and a roughness map varying 0.55-0.85. Dock planks get a wood albedo with anisotropic grain at roughness 0.6. Hex side/cliff faces get triplanar rock at 0.3 hex tiling, 0.8 normal strength, with a vertical AO gradient darkening 0.35 at the base of each ledge.
- Foliage: replace the solid lobes with alpha-tested leaf cards — minimum 3 LOD0 variants, 3 rotations, 0.85-1.15 uniform scale jitter, per-instance hue jitter ±0.04. Bend canopy normals 0.6 toward vertex-to-canopy-centre so the shading goes soft instead of faceted, and add a 0.15-strength cavity AO map. The current 3-5 hard-faceted lobes are the single loudest toy-art tell after the shadows.
- Tonemapping: replace the per-channel clamp with ACES or AgX, linear white point 4.0, highlight desaturation 0.6 above 0.8 luma. Acceptance: R-channel clip drops from 0.805% to under 0.15% and the R:B clip ratio lands near 1.0, so warm sand rolls to warm white instead of rotating orange.
- Atmosphere: add exponential height fog — density 0.006 per hex, height falloff 0.35 hex, in-scatter 0.55/0.63/0.72 sun-tinted — plus 0.35 distance desaturation at the far plane, targeting far-band saturation ~0.27 against near 0.40. Keep the cloud-shadow pass but soften its edge to 2.5 hex and lift its darkening from ~0.45 to 0.65 multiply so the far field stops reading as mud. UI side: add a panel-exclusion rect to the banner layout solver (banners overlapping a panel by >20% of AABB fade out over 120ms, <20% shift up by overlap + 8px), and dedupe the notification feed on (type, subject, turn).

## critique 7 — 34/100
Unlabeled, side by side with a real Civ VI in-game shot, a player calls Civ VI better within about half a second, and it is not close.

The single tell is the units. Crop a 60px box around the selected Warrior at (767,382)-(799,439): the body is a 25x30px lump of flat, untextured mid-blue and brown with a paper-cutout pennant, and it is glued to the ground by a soft warm-brown ellipse roughly four times its footprint, offset down-left, with a visible bright gap between the model's feet and the darkest part of the smear. That is a blob-decal shadow. Civ VI's Warrior at the same zoom is ~55-70px of body inside a ~100px banner-inclusive silhouette, with a club and shield that break the outline, a normal-mapped hide-and-bronze material, an idle breathing loop, and a shaped shadow that touches the feet. Your enemy unit at (1159,763) is the same story: a beige bucket-helmet pawn, no limb read, no weapon read, and the same detached brown smear. A player will not say "blob-decal shadow" — they will say "the little guys look like plastic" — but that shadow is the pixel that causes the sentence.

Everything else compounds it. Eight automatic fails are live in this frame: (1) near-field terrain is mip-smeared to mush with an unresolved diagonal dither weave at ~4px period running across every sand and water surface — visible in a still frame, so TAA never resolves it; (2) the water plane at (700,490)-(900,620) is flat matte with zero normal detail, no depth ramp, no shoreline foam, no wet-sand darkening; (3) the pier at (770,530) has no contact shadow and no waterline contact at all — daylight under the pilings, reads as a bed frame comped onto a blue rectangle; the trees and the city bases have none either; (4) hex lines are double-drawn — a grey neutral grid stroke AND a gold territory band offset ~3px, both 1px un-AA'd, and they stipple and z-fight where they cross the water plane at (860,560); (5) the shaded hills at (230,80)-(330,200) show 8+ concentric contour rings from a quantized heightmap/normal — a topo-map artifact; (6) aliasing on every grass card, flag pole, and border stroke, hard alpha-cut with no coverage AA; (7) the "...tis" city nameplate at (400,758) is cut mid-word by the unit panel — clipped UI; (8) toy/programmer-art silhouettes, confirmed canonically by your own unit portrait in the panel, which is a flat bucket helmet and a solid blue torso.

The HUD is genuinely the strongest thing here — the nameplate at (490,270), the unit panel chrome, and the toast stack are competent, near-shippable work. That competence is what makes the world look worse, not better: the frame reads as a shipping UI bolted onto a tech demo. Three cities at pop 7, 5 and 4 all render the same castle-plus-red-roof kit at the same footprint, so there is no growth read at all. Grading is the one thing I cannot fail you on — p1/p99 luminance 15/250, 0.47% crushed, 1.1% clipped, mean saturation healthy. Composition and palette are fine. Nothing else is.

Reject. This is not an 88-minus-polish build; it is a rendering foundation that needs shadows, materials, texel density, and unit silhouettes rebuilt before an art director should look at it again.

**Problems**
- Units are 25x30px flat-matte vertex-colored lumps with no readable silhouette, no weapon or limb read, and no animation — roughly half Civ VI's on-screen size at a fraction of the detail. The unit portrait in the panel confirms it is the intended look, not an LOD.
- Every object in the frame is lit by a soft warm-brown blob-decal shadow at ~4x footprint, detached from the contact point. The pier at (770,530) has no shadow whatsoever and floats over the water with daylight visible under the pilings.
- A regular diagonal dither/herringbone weave at ~4px screen period covers all near- and mid-field sand and water — visible in a still frame, so it is an ordered dither or LOD crossfade that TAA never resolves.
- Hex lines are double-drawn: a grey neutral grid stroke plus a gold territory band offset ~3px, both 1px with no AA, stair-stepping on every diagonal and stippling/z-fighting where they cross the water plane at (860,560).
- Water is a flat untextured plane — no normal detail, no depth-based color ramp, no shoreline foam, no wet-sand band. Tree canopies and banners are equally flat, hard-edged polygonal cutouts with no leaf or cloth material.
- Near-field terrain albedo is a blurred low-frequency splat with no grain, roughly 30-60 px/m effective texel density where it needs 500+; the shaded hills at (230,80) additionally show 8+ concentric contour rings from a quantized heightmap or normal.
- The city nameplate reading '...tis' at (400,758) is occluded mid-word by the unit panel with no push-away or fade — clipped UI.
- Cities at pop 7, 5 and 4 all render the same castle-and-red-roof kit at near-identical footprint; growth is communicated only by the number on the banner, not by the silhouette.

**Fixes**
- Unit scale and silhouette: raise unit world scale 1.6-1.8x so a melee body is 55-65px tall (95-110px with banner) at default zoom on 1600x900. Rebuild base meshes at 1800-2400 tris with shoulder width >=2.2x head width and a weapon that breaks the body outline by >=12 screen px. Gate every unit on a pure-black 64px silhouette test — if it is unnameable, reject the asset.
- Delete the blob shadow decal entirely. Ship CSM with cascade 0 covering 24m at 2048x2048, 3x3 PCF, plus a baked AO capsule of 0.6m radius under each unit at 0.5 strength. Tint shadows cool toward sky ambient (multiply ~0.42/0.47/0.58), never warm brown, and require zero gap between mesh contact point and the darkest shadow texel. Apply the same to the pier, trees, and city footprints.
- Dither weave: convert the offending layer to a true alpha-blended decal. If dither must stay, drive it with per-frame blue noise plus an 8-16 sample Halton jitter and run TAA at 0.9 history blend so it resolves inside 4 frames — a still frame must show zero pattern.
- Terrain detail: add a detail-normal plus detail-albedo layer tiling at 0.25m/tile, blended at 0.35 strength inside 12m camera distance. Set splat aniso to 8x and mip bias -0.5, and target >=512 px/m albedo density at closest gameplay zoom.
- Hex lines: enforce one stroke per edge — suppress the neutral grid wherever a territory border exists. Evaluate both as a hex distance field inside the terrain shader (no separate decal geometry, so nothing can z-fight): territory 2.5px core with 1.0px feather and a 30% inner glow, neutral grid 1.2px core at 25% alpha.
- Water: depth-based color ramp lerping teal at 0.35m to navy at 8m, two-octave flow-mapped normal, roughness 0.02-0.08, a 0.6m depth-driven foam band at the shoreline, and 0.35 albedo multiply on a 0.4m wet-sand band. Add screen-space reflection or a cheap planar reflection so the pier and city read as being on water.
- Banners: 3-bone cloth chain with 0.8Hz noise sway, two-sided material at 0.35 backface translucency, civ emblem at 48x48 on a 128x96 texture, and a cloth normal map. Replace the 1px un-AA'd zigzag poles with 3-4px tapered cylinders that survive MSAA.
- Aliasing: enable alpha-to-coverage with 4x MSAA on all foliage cards (or TAA plus a 0.4 sharpen), and generate alpha-test mips with a coverage-preserving downsample so distant grass does not thin.
- Terracing: store terrain height as R16F or R32F rather than 8-bit and recompute normals from the float height with a 1-texel Sobel. If the mesh is genuinely stepped, subdivide each hex tile to >=16x16 quads and smooth vertex normals across tile seams.
- City growth: define 4 tiers with distinct silhouettes — t1 3-5 huts no wall, t2 8-10 buildings plus palisade, t3 stone wall plus one tower plus 15 buildings, t4 double wall plus keep plus 25 buildings spilling into an adjacent hex — with roofline height scaling 1.5x per tier so the outline, not the building count, carries the read. Separately, add a screen-space collision pass that pushes any world nameplate 24px clear of a HUD rect or fades it to zero over 120ms, and sink city meshes 0.15-0.25m with a vertex-alpha skirt plus a 1.5m AO decal to kill the floating base plates at Aurelia's south wall and Solmere's west pilings.

## critique 8 — 34/100
Side by side, unlabeled, the player calls Civ VI better in under a second, and it is not close. The single tell: the waterline. In this build land meets water on a razor 1-pixel hex chord with zero foam and zero wet sand — you can literally read the hexagon silhouette of the mesh as the coastline (clearest in the crop at x950-1150 y120-270, and on the two inland lakes at x600-780 y480-720, which are flat blue-grey hexagon-shaped puddles with a painted white smudge and a different tone per tile). Civ VI puts an animated foam ring, a swash band and a darkened wet-sand strip on every single coast hex; that ring is the thing a player's eye locks onto, and its absence here reads instantly as "unfinished shader" rather than "different art direction."\n\nThe second tell, close behind: the sun is nowhere in the water. Every tree, wall and dune in this frame is directionally lit from upper-left with a cast shadow, and the ocean has not one specular glint, not one lit-versus-shadow side on a wave crest, no sky or cloud reflection. The whitecaps are baked white pixels inside the albedo texture — flat, unlit, and repeated from about three identical 3-6px dash glyphs. The result is a top-down turquoise marble photograph slid underneath the terrain, which is exactly what a tech demo looks like. Ocean mean is R95/G107/B94 — blue is LOWER than red — so it grades muddy mint, not sea.\n\nMultiple automatic fails are present: flat-matte untextured water on every lake and river; near-field mip-smeared grass and sand; toy/programmer-art silhouettes (the blue blocky units, the river drawn as a lozenge decal); triple-drawn hex edges (grey hairline + yellow ribbon + dark drop-shadow on the same edge, at x380-560 y215-330); the pier at x700-830 y480-570 casting no shadow and no contact darkening onto the water; a hard straight texture/mesh seam across the open ocean near x1300 y360; and a hard-edged translucent grey overlay polygon desaturating the ocean in the upper right with the Solmere banner straddling the shoreline underneath it. Rejected.

**Problems**
- Zero shoreline foam and zero wet sand anywhere. Land meets water on a hard 1px hex chord (x950-1150 y120-270). The only 'surf' is a flat desaturated grey-lavender wedge that follows hex edges, not the coast curve, with visible vertices where the beach hex chamfers.
- No specular and no sky reflection on any water surface. Not one sun glint despite obvious upper-left key light on every other surface. Water is pure unlit albedo — the brightest water pixels are baked white texels, flat, with no lit/shadow side.
- Water is drawn per-hex, not as a continuous surface. Lakes at x600-780 y480-720 are three adjacent hexagons with three different palettes (grey-blue, teal, cyan) and visible seams between them; open ocean shows a hard straight texture discontinuity near x1300 y360. Reads as a decal per tile.
- Inland water is flat-matte and untextured: a vertical gradient plus one soft airbrushed white blob per hex. No normal detail, no ripple, no depth, no caustics. Automatic fail.
- Rivers are per-hex lozenge decals (x380-560 y215-330), discontinuous across tile boundaries, misaligned with the neighbour hex, no flow, no banks, no bed, no bank AO. Drawn under the grid line and territory ribbon which cut straight across them.
- No ship or object interaction with water at all: no wake, no bow foam, no hull waterline, no displacement, and the pier at x700-830 y480-570 casts no shadow and has no contact AO on the water plane — the water is not in the shadow receiver set.
- Whitecaps are stamped albedo, not simulation. The same ~3 dash glyphs repeat at identical size and mostly horizontal orientation across the entire ocean; near shore they degrade into a 1px checkerboard of white dots that will strobe in motion. Full-frame cross-hatch weave on sand and foliage compounds it.
- Grading is broken on water specifically: shallows mean L=182 with 1.38% near-clipped pixels going milky white, deep water at (38,44,45) is near-neutral with no hue, and the entire mid-tone depth ramp is hidden behind the notification stack. Blue channel below red in the ocean average.

**Fixes**
- Replace the baked noise with 4 Gerstner waves: wavelengths 34/17/8.5/4.2m, amplitudes 0.42/0.19/0.08/0.035m, steepness Q 0.35/0.28/0.20/0.12, directions spread +/-35deg around one wind vector, speed sqrt(g*L/2pi). Layer two scrolling detail normals tiled at 6m and 1.7m, scrolling 0.055 and 0.13 UV/s with 18deg between them, UDN-blended at strength 0.65/0.35. Delete every white texel from the water albedo.
- Derive foam from the Gerstner Jacobian instead of albedo: J = det of the horizontal displacement Jacobian, foam = saturate((1-J)*3.5 - 0.2). Foam must be a lit material — albedo 0.85, roughness 0.55, receiving sun and cascade shadows — not additive white.
- Add the missing shoreline system. Bake an SDF from the land mask at 4 texels/hex; foam band = smoothstep(0, 0.9, 1 - sdf/1.8m). Two layers: an outer surf line scrolling shoreward at 0.35 m/s on a 4.5s period, and an inner swash on a 6.2s cycle offset 0.7m. Erode the alpha with a 3m worley bubble tile so the edge dissolves instead of hard-cutting on the hex chord.
- Add wet sand: within 0.9m inland of the waterline, albedo *= 0.52, roughness 0.12 (from 0.85 dry), F0 0.035, and animate the wet line on the same 6.2s swash cycle with 0.35m amplitude.
- Add specular. GGX with base roughness 0.045, roughness += 0.09*foam, F0 = 0.020, plus a second broad glitter lobe at roughness 0.18 / 25% weight so you get a glint path rather than one blob. Drive water sun intensity to 4-6x the terrain highlight; target peak pre-tonemap luminance 1.6-2.2 linear.
- Add reflection. Schlick F = 0.02 + 0.98*(1-NoV)^5 (about 0.06 at this ~57deg pitch), fed by a per-frame 128x128 SH-convolved sky probe plus a quarter-res (400x225) planar reflection for the top 3 mips, and reproject the skydome cloud texture along the reflection vector at 0.25 opacity.
- Beer-Lambert depth instead of a flat tint: sigma_rgb = (0.36, 0.11, 0.06) per metre. Target shallow (110,196,190), mid (36,124,152), deep (14,44,72) sRGB, ramped over 2.5 hexes rather than half a hex. Add refracted seabed: offset the scene-colour UV by normal.xz * 0.06 * saturate(depth/3) so sand reads through the first 2m. Pull water exposure -0.6 EV to kill the 1.38% clipping.
- Stop drawing water per hex. One continuous water surface (screen-space projected grid) clipped against the land mask, shared by ocean and lakes; lakes just swap sigma_rgb to (0.55,0.35,0.30) and scale wave amplitude to 30%. This single change removes the hexagonal puddles, the per-tile colour mismatch, and the ocean seam at x1300 y360.
- Rebuild rivers as a spline-swept mesh along hex edges with continuous UVs across tiles, width 0.35 hex, flow-mapped downhill at 0.4 m/s using two-phase blending (phase offset 0.5, triangle-wave weight) to kill the pop. Add 0.25 hex of darker wet mud on each bank plus a 3-4px AO contact line, and draw the grid/territory ribbon under, not over, the water.
- Ship and object contact: Kelvin wake at +/-19.5deg from the hull written into a 512x512 wake RT feeding both foam and normals with an 8s decay; 0.6m bow foam ellipse; -0.12m displacement dent under the hull; a 0.15m wet waterline band on the hull; and a 0.45-opacity contact AO ellipse. Add the water plane to the shadow receiver list so the pier and city walls cast onto it. Then fix the shimmer: clamp water normal mip bias to +0.5 beyond 40m, roughness-regularise via roughness = max(roughness, saturate(length(ddx(N)+ddy(N))*0.6)), and resolve the full-frame cross-hatch weave with TAA (Halton(2,3) 8-tap jitter, variance-clipped history at 0.92) or alpha-to-coverage at 8x MSAA.

## critique 9 — 34/100
Rejected. Multiple automatic fails, several of them at the exact focus of this pass.

Side by side, unlabeled, against a real Civ VI in-game screenshot: the player calls Civ VI better, instantly, and it is not close. The single tell is the units cast no shadow. Brighten the flag-bearer at (1105-1215, 700-820) by 1.9x and the sand under him is perfectly uniform — no cast shadow, no contact darkening, no AO ring, nothing from the pole either. Same for the Warrior at (455-565, 690-810), whose feet dissolve into a blue smudge that is the water inlet behind him, not a shadow. Meanwhile the tree at (~200,110) casts a hard black streak down-right, so the sun and the shadow map plainly exist — units are simply not in the caster pass. That is the tell: in this frame the men are stickers pasted onto the ground while the scenery around them is lit. A Civ VI Warrior has a soft contact shadow welding him to the hex; that single relationship is what makes Firaxis's 32-pixel-tall units read as standing on a world instead of on top of a picture of one.

What is genuinely good, and I will say it once: the city name banners, the top resource bar, the notification cards and the end-turn/minimap cluster are competent, well-spaced UI with real typographic discipline. The coastline read and the overall color script hold up at thumbnail. Everything below the HUD layer does not.

At gameplay zoom the world is a prototype. Near-field terrain — the closest 150px of ground, (150-420, 560-720) and (900-1300, 820-900) — is a soft out-of-focus oatmeal blob with no detail layer, no grain, no normal relief; the blob cell size is identical near and far, so there is one world-space albedo and nothing else. Mid-field trees are faceted flat-shaded convex hulls in matte olive with zero texture (the one at 430-520, 240-300 shows its triangles). The village plinth at (~420,95) is a hex disc with a saturated UI-blue 2px stroke and a literal green checkerboard on its top face. Hex lines are double-drawn: a 1px grey world hairline runs alongside a separate gold territory ribbon at a different offset, and the grey line draws over grass cards and tree canopies with no depth respect. Nothing in the frame is antialiased — the "1" badge ring at (897,443), the flag's notch, the banner chip's right edge and the tree shadow's edge all stair-step at native 1600x900.

Solmere is the worst object in the build. Flat blue banner quads lie draped across and through the rooftops instead of standing on poles, a gold-ringed blue ellipse at (~1160,500) is untextured programmer art, and houses at random Y-rotations interpenetrate each other on a hard black-outlined hex footprint. And its city banner has an oversized unclipped blue chevron behind the plate running ~90px past it out over the ocean — a nine-slice or tail element at the wrong scale that Aurelia's banner does not have. Broken UI shipping in a review capture.

City growth does not read. Size 7 and size 5 have nearly the same footprint, the same building tiers, the same silhouette. The only growth signal in this frame is the digit in the banner chip, which means the 3D city is doing no work.

Grading is the one technical area that passes cleanly — I measured it: p0.1=1, p50=88, p99.9=252, 0.21% below 8, 0.36% above 247, mean saturation 0.385. Not crushed, not washed. Keep that curve.

Automatic fails present: mip-smeared near-field terrain; untextured/flat-matte surfaces (trees, flag, plinth); objects with no contact shadow (both units, pole, trees, pebbles); double-drawn hex lines; toy/programmer-art silhouettes; unresolved aliasing throughout; overlapping/unclipped UI on the Solmere banner. Any one of these is a reject on its own. Seven is a tech demo.

**Problems**
- Units cast no shadow at all. At 1.9x exposure the sand around the flag-bearer (1105-1215, 700-820) is uniform — no cast shadow, no contact AO, nothing from the pole. The Warrior (455-565, 690-810) is the same; the dark area at his feet is the water inlet behind him. Trees at (~200,110) DO cast, so the shadow map exists and units are excluded from the caster pass. This is the frame's defining failure.
- Near-field terrain is mip-smeared mush. The closest ground to camera — (150-420, 560-720) and the bottom strip (900-1300, 820-900) — is a soft low-frequency worley blob with no detail albedo, no normal, no grain. Blob cell size is identical at 8m and 80m, proving a single world-space layer with no distance-blended detail tiling. Sharpest thing on screen should be the nearest ground; it is the blurriest.
- Trees are untextured faceted blobs. The canopy at (430-520, 240-300) shows individual flat-shaded triangles under a single matte olive tone — no leaf cards, no alpha, no translucency, no trunk, no per-instance rotation or scale jitter, and no shadow on the grass beneath. Convex-hull silhouette. This is placeholder geometry, not stylization.
- Hex lines are double-drawn in two conflicting systems. A 1px grey world hairline (visible at 60-340, 80-260) runs alongside a separate gold territory ribbon at a different offset on the same map, plus a third saturated UI-blue stroke around city plinths. The grey hairline is 1px, broken/dotted along its run (guaranteed shimmer in motion) and draws OVER grass cards and tree canopies with no depth test.
- Solmere city (1030-1250, 400-560) is programmer art. Flat blue banner quads lie draped flat across and intersecting rooftops rather than standing on poles; an untextured gold-ringed blue ellipse sits at (~1160,500); houses at random Y-rotations interpenetrate each other and one is half-sunk through the ground plane; the whole thing sits on a hard black-outlined hex footprint that cuts the water with no dock or shore transition.
- Unclipped UI: the Solmere banner has an oversized translucent blue chevron behind the plate extending ~90px past its right edge out over the ocean, overhanging top and bottom, with a hard aliased outline. Aurelia's banner has no such element — this is a nine-slice/tail at the wrong scale. There is also a stray 1px grey divider running through the Aurelia plate to the right of the word 'Aurelia', and both plates are ~0.55 opacity so terrain noise bleeds through the text field.
- Zero antialiasing resolve. Stair-stepping is visible at 4x on the gold ring of the '1' badge (897,443), the flag's notched trailing edge, the blue chip edge on both city banners, the plinth rims, and the tree shadow's edge — the shadow itself is 1-tap hard with blocky pixel steps, no PCF. Nothing in this frame has been through TAA, SMAA, or even FXAA.
- City growth does not read in 3D. Aurelia (pop 7) and Solmere (pop 5) have nearly identical footprint radius, building tier, and silhouette mass; the third settlement at bottom-left is the same kit again. The only growth signal in the frame is the digit in the banner chip. Scale is also broken globally: grass cards stand roughly a third the height of a house, i.e. ~2m blades.

**Fixes**
- Put units and all props in the shadow caster pass. Cascades: 0-35m @2048², 35-90m @2048², 90-250m @1024². Depth bias 0.02, normal bias 0.4, slope-scale 1.5. Replace the current 1-tap lookup with 5x5 PCF, or PCSS with 8 blocker samples at light radius 0.02 rad — the tree shadow at (200,110) is hard-edged and stair-stepped, which will also fix that.
- Add screen-space contact shadows on top of the cascades: 12 steps, 0.35m max trace, 0.02m thickness. This is specifically what removes the sticker look at unit feet. Additionally give every unit a radial AO decal, 1.4m radius, 0.55 opacity, multiply blend, projected along the terrain normal, so the unit stays planted when the sun is behind it.
- Add distance-blended terrain detail. Keep the existing macro layer, then blend a detail albedo+normal at 0.5m tiling with saturate(1 - dist/25), and a second micro layer at 0.12m tiling for the nearest 8m. Set terrain aniso to 8x and mip bias -0.5. Convert the splat blend from linear alpha to height-blend (heightlerp contrast 0.6) so sand/grass boundaries stop being soft gradients.
- Rebuild the tree canopy: 3 crossed alpha-tested leaf cards from a 512² atlas with alpha-to-coverage, two-sided transmission 0.25 tinted (0.35, 0.55, 0.20), plus a trunk mesh. If the stylized blob must stay, at minimum add a canopy normal map, a 0.15 curvature rim, and 6-8% vertex noise displacement to break the convex hull. Per-instance: rotation 0-360°, uniform scale 0.8-1.25x, hue shift ±8%.
- Collapse to ONE hex grid. Delete the gold ribbon mesh and the blue plinth stroke; render the grid as a deferred decal at constant 1.5px screen width with fwidth antialiasing — smoothstep(0.0, fwidth(d)*1.5, d) — depth-tested against terrain only, alpha 0.35 on interior edges and 0.85 on territory borders. Terrain-only depth means it stops drawing over units, trees and canopies, which it currently does.
- Turn on TAA: 8-frame Halton jitter, 0.05 static blend weight, velocity-rejected history, then a 0.25 sharpen pass to recover the softening. SMAA T2x is the floor if TAA is off the table. Separately, capture review shots at 2560x1440 and downsample to 1600x900 — native 900p with no resolve is why every ring and notch in this frame stair-steps.
- Fix the Solmere banner: clip the tail/nine-slice to the plate rect (it currently overhangs ~90px right, plus top and bottom). Raise plate opacity from ~0.55 to 0.90 with a 2px inner dark stroke so terrain stops reading through the text field, add a 6px anchor stem from plate to city center plus a 3px-offset drop shadow at 0.35 opacity so it attaches instead of hovering, and remove the stray 1px grey divider inside the Aurelia plate.
- Generate cities from a street skeleton, not random scatter: 2-3 spines from the center at 3.5m width, snap building forward vectors to the nearest street normal, then place fill houses with a 0.4m no-overlap rejection test to kill the current roof/wall interpenetration. Replace the hex plinth mesh with a terrain-conforming ground decal (compacted dirt + cobble) height-blended into the surrounding grass over 1.2m so the disc silhouette disappears, and add a 0.6m AO skirt at every wall/ground junction.
- Make growth legible in 3D: footprint radius = 4 + 1.6*sqrt(pop); tier the kit (pop 1-3 thatch huts, 4-7 tiled houses, 8+ two-storey plus a wall ring); gate the central keep/tower on pop >= 6. Right now pop 7 and pop 5 are visually indistinguishable and the banner digit is doing all the work.
- Unit readability and materials: split roughness (cloth 0.85, leather 0.55, metal 0.35 with a 0.7 metallic mask) instead of the current uniform matte lambert; add a 0.05 sun-keyed fresnel rim and a 1.5px inverted-hull or depth-normal outline — that pairing, not texture detail, is why Civ VI units read at 32px. Then make the flag cloth: two bones plus a vertex sine, amplitude 0.06m, 3 Hz, phase = worldpos.x*4, falling to 0 at the pole edge, two-sided with 0.75 backface tint. Cut grass cards to 0.35m height, randomize rotation 0-360° and scale 0.7-1.3x, cull at 45m, and drive them with the same wind phase.

## critique 10 — 41/100
Rejected. Seven automatic fails present; this is a tech demo wearing a Civ costume.

Credit where due first, because it's short: the grading is the only department that would survive a Firaxis lighting review. Measured over the terrain (rows 60-860, cols 0-1270): median luma 100, p5=40, p95=214, only 0.17% above 245 and 0.54% below 20, per-channel clip 0.15%/0.007%/0.0%. That is a well-placed curve with no crush and no blowout, and the big soft cloud shadows raking across the dunes are genuinely the best idea in the image. Everything underneath that curve is broken.

The killers:

1) The frame carries a per-pixel noise/dither signal everywhere. Nyquist checker-energy ratio measures 0.73-0.87 in every region I sampled, including regions that are physically smooth. A flat patch of lit sand at (1080,700) reads 230 230 230 215 174 212 228 across one row - a plus/minus 35 LSB swing on a surface that should be a gradient. A post-TAA Civ VI frame sits around 0.2-0.35. In motion this entire image boils.

2) Units are rendered with screen-door transparency. Crop (1130,690)-(1220,800): the standard-bearer and his red banner are a literal ordered-dither checkerboard, adjacent pixels swinging 184/126/80/175/112. His "shadow" is the same stipple smeared into a rectangular band with no silhouette relationship to the model. Unresolved dithered alpha with no temporal resolve behind it.

3) Near-field terrain is blurrier than mid-field. Mean horizontal gradient: near sand (bottom-right, lit) 7.03, mid sand (right, lit) 10.26, far grass 9.77. Detail decreases as the surface approaches camera. That is the definition of mip smear with no detail layer. The bottom 200 rows are cream-colored Perlin fog with sprites stuck in it.

4) Hex lines are triple-drawn and illegible. On the village hex around (400,110) a blue claim outline, a grey neutral grid line and a tan territory border all run parallel along the same edges within 3px of each other. The grey line is a 1px hard raster that stair-steps in clean 2-3px runs at shallow angles, and it fades to invisible over sand above luma 200 while shouting over shaded grass. Three grid systems, none readable.

5) Objects have no contact shadow and no texture. The tree at (400,170)-(500,250) is three flat olive quads with visible facet edges, no trunk, no leaf silhouette, and casts literally nothing - the grass directly beneath it is the same brightness as grass 30px away. Grass billboards draw on top of the canopy, so the scatter pass sorts wrong too. Those same billboards stay bright yellow-green inside cloud shadow: vegetation isn't sampling the shadow term at all.

6) Programmer art in two places a player looks first. The Warrior portrait is a grey blob helmet on a flat blue torso with no face, no material, no lighting. The unit northeast of Aurelia at (760,380) is an unreadable mud-brown lump.

7) Contrast-boosted ocean (crop 950,60-1290,320 at +2.6) exposes a full hex lattice of seams across the water from per-tile normal/shading discontinuity, and the whitecaps are a uniformly tiled noise of identical 4px dashes running to the horizon with zero depth attenuation, zero sun glint, zero specular. The water is matte.

The literal answer you asked for: unlabeled, side by side with a real Civ VI gameplay screenshot, the player calls Civ VI better, immediately, without being able to articulate why for about two seconds. The single tell is the units - yours are see-through checkerboards. A player's eye goes to the figure holding the red banner, sees a fly screen where a soldier should be, and files the whole image as unfinished. No shipped 4X ships screen-door alpha on the hero unit at default zoom. The runner-up tell, once you patch that: the sand closest to the camera is the blurriest thing in the frame, and human vision flags inverted depth-of-detail as fake before it can name it.

**Problems**
- Screen-door dither on units and unit shadows - ordered-dither alpha with no temporal resolve; standard-bearer at (1175,760) swings 184/126/80/175 on adjacent pixels, model reads as a fly screen
- Global per-pixel noise: Nyquist checker-energy ratio 0.73-0.87 frame-wide, plus/minus 35 LSB swings on physically flat lit sand at (1080,700) - guaranteed shimmer in motion
- Inverted detail-vs-distance: near-field sand mean gradient 7.03 vs mid-field 10.26 vs far grass 9.77. Terrain gets blurrier as it approaches camera. Mip smear with no detail layer
- Triple-drawn hex edges at (400,110): blue claim outline + grey neutral grid + tan territory border parallel within 3px on the same edge; grey line is a 1px hard raster that stair-steps at shallow angles and drops below visibility over sand above luma 200
- Trees are untextured flat-shaded quads (crop 400,170-500,250) with visible facet seams, no trunk, no foliage cutout, and zero cast shadow on the grass directly beneath them
- Grass billboards ignore the shadow/cloud term entirely - stay bright inside cloud shadow - share a single yaw across every instance, and sort in front of tree canopies
- Ocean is per-hex plates: contrast-boosted crop shows a complete hex seam lattice from per-tile normal discontinuity, plus uniformly tiled 4px foam dashes with no depth attenuation and no specular anywhere
- Programmer-art silhouettes at the two highest-attention points: the Warrior UI portrait (featureless grey blob head, flat blue torso) and the unit at (760,380), an unreadable brown lump

**Fixes**
- Kill the global grain. Current Nyquist ratio 0.73-0.87, amplitude plus/minus 35 LSB. Apply grain pre-tonemap only, amplitude <=2 LSB (0.8%), 1/f-weighted rather than white, masked out above 0.70 luma so lit sand stays clean. Acceptance: measured checker ratio <0.35.
- Resolve the dithered alpha. Either make unit materials opaque with alpha-to-coverage under 4x MSAA, or add TAA: 8-sample Halton(2,3) jitter, 0.90 history blend, 5-tap YCoCg neighborhood clamp at variance gamma 1.0, history reject above 8 px/frame velocity.
- Fix the unit shadow, currently a stippled rectangular smear. Cascade 0 at 2048px covering 24m, 5x5 rotated-Poisson PCF (3x3 floor), normal-offset bias 0.35 texels, slope-scale depth bias 1.5. Add screen-space contact shadows at 8 steps, 0.35m max trace, to close the gap at the feet of every unit, hut and bridge pile.
- Add a terrain detail layer: detail-albedo plus detail-normal at 8-16 tiles/m UV density, overlay-blended at 0.35, fading in from 18m to 4m camera distance. Force 16x aniso on terrain samplers, clamp mip bias to -0.5 in the nearest cascade. Acceptance: near-field mean |dI/dx| >= 14, i.e. it must exceed the mid-field 10.26, not sit at 7.03.
- Collapse the grid to one pass. Rasterize neutral grid, city claim and territory border into a single SDF decal buffer with per-edge priority (border > claim > neutral); never composite two on one edge. Draw as a screen-space-width SDF: 1.6px core, 0.8px feather - resolution-independent and antialiased instead of a 1px hard raster.
- Make the grid legible across the full range: line color = lerp(RGB 0.92, RGB 0.10, smoothstep(0.45, 0.65, backgroundLuma)) at 0.30 alpha, plus a 1px 0.15-alpha counter-stroke. Must stay readable from background luma 25 through 245; it currently dies above 200.
- Rebuild vegetation: alpha-cutout foliage cards with a 3-cluster silhouette, normals lerped 0.5 toward world-up so canopies read spherical, every instance registered in cascade 0 so it casts, and a per-instance AO decal at 1.4x trunk radius, 0.45 opacity, 0.6m falloff. Randomize card yaw 0-360deg and uniform scale 0.8-1.3.
- Make grass receive light: sample the cloud-shadow and shadow-map term at the instance ROOT world XZ, not the card vertex, and multiply into vertex color. Move the scatter pass after opaque with depth-write on so cards stop drawing over tree canopies.
- Give water a surface: two scrolling normal maps at 0.9x and 0.31x world scale, velocities (0.030,0.017) and (-0.019,0.041) m/s; GGX spec at roughness 0.04 against a 3.5deg sun disc; depth-absorption ramp RGB(0.42,0.72,0.70) at 0m to RGB(0.03,0.11,0.19) at 14m; foam driven by scene-depth delta over a 0-1.2m band, not the uniform noise tile currently painted to the horizon. Weld the water mesh or average vertex normals across tile boundaries to kill the hex seam lattice.
- Kill the Aurelia roof moire (cap roof texel-to-pixel at 1.5 at default zoom, Kaiser-filtered mips alpha 4, 16x aniso, or halve albedo tile frequency and carry shingles in the normal map only). Then replace the Warrior portrait with a render of the real mesh under a 3-point rig (key 35deg elevation / 30deg azimuth, fill 0.25, rim 0.6 behind-left, 0.35 vignette), add a 24px 0-to-0.45 alpha falloff behind every panel so the UI stops butting against the world on a 1px black edge, and unify type - AEON, Masonry and TECH are three faces in one bar.

## critique 11 — 34/100
Side by side, unlabeled, at gameplay zoom, the player picks Civ VI in under a second — and it is not close. The single tell: **nothing on this terrain casts a contact shadow, so the whole surface reads as stickers on a blurred painted floor.** Zoom /tmp/H_pebbles.png (source 1000,640-1120,730 at 7x): every pebble is a solid mid-brown blob with zero occlusion darkening at its base, every grass blade is a 1-2px stroke that terminates against the ground with a hard edge and no darkening, and the sand between them is a two-tone cream/tan interpolation with no grain whatsoever at 7x nearest-neighbour. The eye reads "decal layer over gradient" instantly. Civ VI's ground is a hand-painted material with brushwork and readable strata that survives being zoomed; yours has all its detail in scattered props and none in the surface, so the instant a player looks at bare ground between the props there is literally nothing there. The numbers agree: near-field HF 19.1 vs far-field HF 31.9 — detail decreasing toward camera, the inverse of every shipped 4X. Secondary tell that seals it: the hex vertex at (830,400)-(960,490) has THREE mismatched line treatments meeting at one point — a thick tan ribbon, a thin flat white 2px segment, a gold ribbon — all stair-stepped with no AA. Firaxis would not ship a vertex where the grid disagrees with itself. What genuinely works: composition, atmospheric perspective, and the grading (no clipping at either end, healthy p1=26/p50=102/p99=235). That earns it "handsome tech demo," not "shipped game." Six automatic fails present. Rejected.

**Problems**
- AUTOFAIL — zero contact shadows on every scattered object. /tmp/H_pebbles.png (1000,640-1120,730 @7x) and /tmp/G_tree_base.png (400,380-520,470 @7x): pebbles, grass tufts and tree canopies all meet the ground with a hard silhouette edge and no AO ring, no cast shadow, no darkening. The tufts carry a small baked triangular smudge inside the sprite that is identical in shape and direction on every instance regardless of world position or sun angle — a texture, not a shadow. Objects float.
- AUTOFAIL — near-field terrain is mip-smeared / has no material above ~1/8 tile frequency. Measured HF=19.1 near-field vs 31.9 far-field (0.6x; should be 1.5-2.0x). At 7x nearest-neighbour the sand shows only smooth interpolated gradients — no texel structure, no grain, no ripple, no micro-normal. All apparent detail comes from scatter props; the surface contributes nothing.
- AUTOFAIL — hex lines are double-drawn and mutually inconsistent. /tmp/I_hexvertex.png: three treatments converge at one vertex (thick tan ribbon, thin white 2px line, gold ribbon), different widths, colours and AA. /tmp/F_hexlines.png (760,180-1160,440): pale-blue outline and gold territory ribbon run the same edge at a 1-2px offset. /tmp/G_tree_base.png upper-left shows a fourth faint grey line system in the grass. Every segment is raw stair-step with no smoothstep AA.
- AUTOFAIL — untextured flat-matte surfaces on hero props. Tree canopies (/tmp/E_veg_ground.png at 380,300-760,560) are single-value dark-green low-poly blobs with two flat facet highlights, no leaf texture, no trunk, no translucency. City roofs are flat unbroken red; the keep is a flat grey cylinder with dark rectangles for windows. Nothing has a roughness or normal response.
- AUTOFAIL — aliasing and shimmer throughout. Hex ribbons stair-step at 2-3px; grass alpha cards show a pale 1px halo fringe (alpha-test with no dilation or alpha-to-coverage); ocean at (1250,120)-(1560,400) has ~1px specular sparkle at uniform scale across all depths with no distance roughness ramp. The tree cast shadow in /tmp/G_tree_base.png has a hard aliased edge with zero penumbra — unfiltered shadow map.
- Biome transitions are hard hex-shaped cuts with no ecotone. /tmp/D_biome_trans.png (250,520-700,780): the sand/grass boundary is a straight line locked to the hex edge with ~2px of blend. No sand fingers into grass, no scrub intermediary, no dirt. Worse, scatter density is identical on both sides — the same grass tufts at the same density are strewn across pure white beach sand, which is biome-nonsense and destroys the read.
- Cliffs and rock do not exist. Across the entire frame, land meets ocean on a uniform flat beach ramp — no cliff faces, no strata, no boulders above the ~6px pebble decals, no slope-driven material change anywhere. A 4X terrain pass with no cliff system is not a terrain pass.
- Global directional streak artifact in all sand. /tmp/B_sand_nearfield.png and /tmp/F_hexlines.png: light/dark banding runs at the same ~30deg down-right angle across every tile regardless of slope, aspect or biome, plus perfectly straight parallel thin lines crossing whole tiles (UV/tangent seam or stretched detail tiling). Single-octave noise stretched along one fixed world axis — reads as brushed metal, not sediment.

**Fixes**
- Detail layer: base terrain stays at 2.0 m/texel; add a triplanar detail albedo+normal at 0.25 m/texel (2048^2, ~8 texels/cm for sand grain), full strength inside 12 m of camera, linear fade to zero by 40 m. Blend the detail normal with UDN (not overlay). Acceptance gate: near-field HF must reach >=1.6x far-field HF — currently 0.6x, so this is roughly a 2.7x jump, not a tweak.
- Contact shadows, two mechanisms, both required: (a) enable screen-space contact shadows, ray length 0.15 m, 8 steps, 0.02 m depth bias — this catches pebbles and tuft bases; (b) in the scatter shader write a radial ground-darkening decal at 1.4x each instance's footprint radius, albedo multiply 0.55 at centre falling to 1.0 at the rim, gamma 2.2. Delete the baked triangular smudge from the tuft sprite — it fights the real shadow.
- Kill the diagonal streak: replace the single stretched noise with 3-octave FBM, lacunarity 2.03, gain 0.5, and rotate octave 2 by 37deg and octave 3 by 71deg in world XZ so no global axis survives. Add a second detail tile at 6.0 world-unit period at 0.3 weight to break the 1.5-unit tiling.
- Tree canopies: 4-6 alpha leaf cards per canopy instead of one blob mesh, plus a visible trunk (canopies currently have none). Spherify the canopy normal toward its centroid at 0.7 blend. Add subsurface wrap 0.35 with warm transmission tint (approx #C8D66A) so backlit leaves aren't flat. Every canopy must write to the shadow map and receive its own contact decal.
- Unify the hex grid to ONE system: drop all ribbon meshes and draw the edge as a signed-distance field inside the terrain shader — width 0.06 hex-radii, smoothstep AA over 1.5 screen px, evaluated against the heightfield so it conforms and cannot z-fight. Territory ownership becomes a colour tint on that same SDF, not a second geometry pass. This kills the three-styles-at-one-vertex defect outright.
- Biome blend: per-vertex splat weights feathered over 0.35 hex-radii, with the mask warped by noise at 0.15 hex-radii amplitude so the boundary is irregular rather than hex-locked. Gate scatter by splat weight: grass tuft density must fall to <5% where sand weight >0.7 (currently identical on both sides).
- Shadow map: 3 cascades at 2048^2, first cascade covering 25 m, 3x3 PCF with 2-texel radius and normal-offset bias 0.4 texels. The current tree shadow edge is a hard aliased step with no penumbra.
- Shoreline: replace the amorphous white alpha splat with depth-driven foam — intensity = 1 - smoothstep(0.0, 0.8 m, water depth) — modulated by two scrolling foam-line textures at 0.15 and 0.35 m/s running parallel to the shore normal. Add a wet-sand band 1.2 m inland: albedo x0.70, perceptual roughness 0.25.
- Ocean shimmer: convert normal-map variance to roughness (Toksvig or LEAN) and clamp perceptual roughness to max(r, 0.02 + 0.0008 * screen_distance_px). Currently the crinkle normal is the same scale at all depths, which is what produces the 1px sparkle.
- Grass alpha fringe: switch to alpha-to-coverage with MSAA 4x, and dilate the albedo 4 px into the transparent region of the tuft atlas before packing. The pale halo on every blade in /tmp/H_pebbles.png is undilated premultiply bleed.

## critique 12 — 34/100
Unlabeled and side by side with any Civ VI coastal screenshot, the player picks Civ VI in under a second and does not have to think about it. The single tell: your water has no sun on it. Not "weak" specular — none. I measured it: 0.0025% of the frame reaches luminance 250, the 99th percentile is 225, and there is not one glint pixel anywhere on ocean, lake, or river. Real water at this sun angle throws a blown-out anisotropic glint sheet across the coast hexes; that highlight is the primary cue the brain uses to classify a surface as liquid. Without it, and with the surface desaturated to near-neutral (your shallow coast samples (188,199,183) — blue is LOWER than red; Civ VI's shallow coast is roughly (60,175,190), a +130 blue-red spread), the ocean reads as fog on frosted glass and the lake reads as poured concrete. The killer frame is the harbor at (660,470)-(860,600): a 200x130 region of literally flat-shaded polygon with a dock floating on it, no normal detail, no glint, no contact shadow, no waterline. Nothing in a shipped Civ has ever looked like that. Beyond the tell, this frame trips eight separate automatic fails, so the score is not close to the 88 gate.

**Problems**
- FLAT-MATTE WATER (auto-fail). Lake at (620,530)-(990,770) and harbor bay at (660,470)-(860,600) are mirror-smooth polygons with zero surface normal — no ripple, no wavelet, no perturbation of any kind. The only variation is a soft airbrushed cloud smear pasted on top. This is the single most disqualifying thing in the shot.
- NO SPECULAR ANYWHERE (measured). p99 luminance 225, p99.9 = 239, 0.0025% of pixels >= 250. Zero sun glint on any water body. Water without a highlight is not water.
- WATER ISN'T BLUE — washed-out grading (auto-fail). Deep ocean (85,102,107), mid ocean (165,193,179) — G > B, that is a green-grey. Shallow coast (188,199,183) is effectively neutral. A global cloud-reflection layer is being blended at roughly 55% uniform weight across every water surface, bleaching all saturation out. It also bleeds past the water mask onto the sand at the upper-left of the lake crop.
- NO DEPTH RAMP, AND IT'S INVERTED. Sampling x=1150 from far to near: lum 62 -> 108 -> 127 -> 162 -> 177 -> 159 -> 153. Brightness peaks mid-ocean and the far offshore is MILKIER than the near shore. The gradient is driven by the pasted cloud texture, not by depth. Enclosed lake center (135,149,167) is bluer than open ocean shallow — exactly backwards.
- DOUBLE-DRAWN HEX LINES (auto-fail). Clearly two independent grid systems compositing. At (820,430)-(1100,650) a pale cream hex outline, a separate grey hex line offset ~6-8px, and the blue border polyline all run in parallel; the same doubled grey pair recurs at the bottom-right of Solmere. The overlay also ignores terrain — it runs straight over tree canopies at a fixed screen offset.
- NO CONTACT SHADOWS (auto-fail). The dock and its four pilings sit on the water with no darkening ring, no shadow, no waterline foam, no displacement — the water is not even in the shadow-receiver mask. Same for the tree on sand at (890,560) and every grass billboard in the frame.
- HARD HEX-POLYGON SHORELINES. The lake terminates on a straight flat-shaded polygon edge that cuts across the beach with no depth fade, no alpha blend, no foam, no wet transition — and the sand visibly runs under the water plane at the bottom-right with a clean geometric slice.
- ALIASING + MIP-SMEAR IN THE SAME FRAME (two auto-fails). Near dirt at (200,600)-(300,700) shows textbook wormy moiré interference that will crawl violently in motion, while the grass hex beside it is an under-sampled mushy blob at roughly 1/4 the texel density. Distant hex lines and the blue border polyline are stair-stepped with no width clamp or distance fade. Reads as no TAA, or a TAA whose history is being rejected.

**Fixes**
- Depth-based absorption instead of the flat ramp. Beer-Lambert transmittance = exp(-depth * sigma), sigma_rgb = (0.45, 0.16, 0.11) per metre. Target sRGB: shallow 0-1.5m (61,184,189), mid 1.5-8m (15,92,128), deep >25m (3,15,36). Your shallow is currently (188,199,183) at B-R = -5; the target is B-R = +128.
- Gate the sky reflection on Fresnel instead of blending it flat. Schlick F = 0.02 + 0.98*(1-dot(N,V))^5; at this ~50-degree camera pitch dot(N,V) ~ 0.77, so the correct reflection weight is 0.02-0.04, not the ~0.55 you are using. Cap contribution at 0.25 even at grazing, and clamp reflection luminance to 1.5x the local water colour so clouds tint rather than bleach. Also fix the mask — it is leaking onto sand.
- Add GGX specular, roughness 0.035-0.06, sun intensity tuned so the glint core clips: target 0.4-0.9% of water pixels at >=250 (currently 0.0025% of the whole frame). Stretch it into a sheet, not a disc, by scaling the along-wind normal component 2.5x.
- Three scrolling normal layers, which alone fixes the lake and harbour: 8m and 31m world tiling (ratio 3.875, non-harmonic, kills beat patterns) scrolling 0.045 and 0.017 m/s at 40 degrees apart, strengths 0.35 and 0.18; plus a 2.2m detail layer at strength 0.12 faded out past 60m so it stays clear of the shimmer band.
- Depth-fade shoreline: waterDepth = linearEyeDepth(scene) - linearEyeDepth(water); alpha ramp over 0-0.35m; foam where waterDepth < 0.6m at intensity smoothstep(0.6, 0.05, waterDepth); jitter the band edge by the 8m normal map * 0.25m so it stops being a clean offset curve. Removes the hard hex silhouette.
- Rebuild foam as crest lines, not a milk blob. Your foam is opaque (245,236,226) at ~100% coverage over a 10-18px band. Use a triangle wave on the shore distance field, period 4.5m, giving bands at 0-0.8m / 2.2-2.9m / 4.5-5.0m at alpha 0.85 / 0.45 / 0.2, each modulated by a 6m-tiling scrolling foam texture. Retint to (240,246,250) — the current foam is warmer than the sky, which is precisely why it reads as snow.
- Wet sand is entirely absent. Add a 1.2m inland band: albedo * 0.62, roughness lerp 0.85 -> 0.28, specular +0.15, with the inland edge feathered by the same foam scroll so the last-wave line moves.
- Dock water contact: 0.35m-radius darkening ring per piling (albedo * 0.55 falling to 1.0 at 0.9m), a static ripple-ring normal perturbation, an SSAO contact term, and add water to the shadow-receiver mask so the deck casts.
- Collapse to one grid system. Render the hex as a distance-field line inside the terrain shader with an fwidth width clamp of max(1.15px, 0.02 world-units/px) and delete the second overlay pass. Fade grid alpha 0.55 at 20m to 0.0 at 140m to stop the distant crawl.
- River at (430,340)-(700,530) is pixel speckle with no banks. Cut the channel 0.4m into the terrain, widen to 3.5-5m, drive it with a flowmap at 0.9 m/s, drop the white-noise contrast from ~0.9 to 0.25, and emit whitewater only where the flowmap gradient exceeds 0.6. Also: there is no ship in this frame at all — wake, bow foam, and hull waterline cannot be judged, so re-shoot with a galley on a coast hex before calling this a water pass.

## critique 13 — 39/100
Unlabeled and side by side with a real Civ VI gameplay screenshot, the player picks Civ VI in under a second — not close. The single tell is the units. Zoom the warrior at (1090-1270, 690-850): it's a smooth capsule with sphere shoulders, brown sausage legs, a blank slot-visor head and a torso with literally zero albedo texture — a painted gradient, no cloth weave, no wear, no normal map, no roughness break-up. Civ VI's warrior at the same 110 px height has a patterned kilt, a strapped shield with a rim, a specular bronze edge, and a foot that darkens the dirt it stands on. Yours floats: the cast shadow is a detached 30 px-penumbra blob offset to the right with no contact AO at the boot line, and the second unit at (450-560, 740-820) has no visible legs at all below the belt and no ground contact whatsoever. That one figure sinks the frame; everything else is downstream of it. Second-order tell: near-field ground has measurably LESS detail than the mid-distance band (Laplacian edge energy 25.4 in the foreground crop vs 44.9 at the sand band 400 px further away) — the near field is inverted-sharpness mush, and the white beach at (600-900, 720-890) is a flat untextured cream blob with airbrushed edges. Automatic fails present, any one of which is disqualifying on its own: mip-smeared near-field terrain; double- and triple-drawn hex lines (see 300-620, 80-260 — a dark stroke, a pale stroke 2-3 px inboard, plus the fat blue border as a third parallel line for the same edge); untextured flat-matte surfaces (unit bodies, hut cones, Solmere's gold dome, city wall planes, sand); props with no contact shadow (rocks, grass, huts, city pads, both units); toy/programmer-art silhouettes (capsule soldiers, a literal green checkerboard quad as a farm at 470-600/130-200, and three "different" leaders sharing one identical bust with only a hue swap at 1090-1240/5-45); hard stair-step aliasing on every border diagonal; and interpenetrating geometry at Solmere where a blue flag quad passes straight through the dome tower. The UI chrome is the only genuinely shipping-grade thing here — the unit panel, banners and top bar would pass at a AAA studio at maybe 76 — and that makes the world look worse by contrast, because good UI over toy assets is the exact signature of a tech demo. It also leaks logic tells a Civ player catches instantly: a Warrior offering FOUND as an action, and two byte-identical "Aeonian engineers raise earthworks" dispatches stacked at T30.

**Problems**
- Units are untextured toys with no ground contact. Warrior at (1090-1270,690-850) is capsule limbs + sphere shoulders + blank visor, zero albedo/normal/roughness detail. Unit at (450-560,740-820) terminates at the belt — no legs, floating ~0.3 m, no contact AO. Cast shadows are detached 30 px-penumbra blobs; cascade 0 texel is roughly 0.5 m.
- Near-field terrain is mip-smeared. Measured Laplacian edge energy 25.4 in the (600-900,720-890) foreground crop vs 44.9 in the (700-1100,180-320) mid-distance band — sharpness is inverted with distance. Grazing-angle sand smear indicates 1x-2x anisotropic filtering. The beach is a flat cream blob with no grain, no wet line, no shell/pebble detail.
- Hex edges are drawn two and three times. At (300-620,80-260) each edge carries a dark navy stroke, a pale gray stroke offset 2-3 px inboard, and the blue territory ribbon as a third parallel line. Line weight is inconsistent and the grid vanishes entirely over the bright sand at (600-900,720-890) — illegible in exactly the place it must read.
- No contact shadows on any prop. Four identical rock blobs at (940-1290,560-760) sit as decals; grass billboards have no root darkening; the village pad at (470-700,80-260) is a hard-cut extruded cylinder with no AO skirt and no terrain blend; both city plinths float over their tiles.
- Solmere (1000-1240,380-560) is geometry soup: a blue flag quad passes through the gold dome tower, roofs interpenetrate, an untextured blue disc reads as a placeholder, and the whole cluster sits on a hard-edged brown octagon cutting into open water with no shoreline contact.
- Flat-matte materials throughout: unit tunics, the four hut cones, the Solmere dome, and the large city wall planes at (560-760,420-540) are single-value surfaces with painted-on lines, no normal response, no roughness variation, no specular break-up. Roof reds also read as two different materials at the same distance.
- Ambient term is a flat multiply, so shadow carries no albedo detail — edge energy 24 in the shadowed tile at (430-620,700-830) vs 45 in sunlight. Shadowed grass and dirt collapse to featureless mud, and the lower body of unit two disappears into it.
- Cities and banners give no growth read. Pop 7 Aurelia and pop 5 Solmere are both one arbitrary clump on one hex with zero worked improvements on any adjacent tile. Both banners are the same navy with no civ-colour tint, no anchor stem to the city, and no food/production arcs — the pop digit is the only signal.

**Fixes**
- Units: author 1024x1024 albedo + normal + ORM per unit (512 is the floor at this 110 px on-screen height). Three material zones with real roughness — cloth 0.85, leather 0.60, bronze 0.30 at metallic 1.0. Rebuild the silhouette to pass the 64 px black-cutout test: add a helmet crest, a shield with a 4 cm rim bevel, asymmetric weapon. Delete the sphere shoulders and capsule legs.
- Fix unit two's missing lower body: check the LOD0/LOD1 swap and the bounding box — the mesh is either culled below the belt or LOD1 ships legless. Then re-run ground snap: raycast to terrain height per frame and clamp foot IK to surface + 0.00 m, currently ~0.3 m of float.
- Grounding pass: per-instance contact decal at 1.3x footprint radius, 55% opacity, plus SSAO at 0.35 m radius / 0.8 intensity, plus screen-space contact shadows at 0.08 m length / 8 steps. Apply to units, rocks, grass clumps, huts, and both city plinths.
- Shadows: CSM cascade 0 to 15 m at 2048 (roughly 7 mm texel, down from the ~0.5 m the current 30 px penumbra implies), 4 cascades at 15/40/100/250 m, 0.5 px normal-offset bias. The flag pole should throw a readable hard stripe; right now it throws nothing.
- Near-field terrain: triplanar detail albedo+normal at 0.5 m tiling, blended 0-25 m, 0.6 albedo strength / 1.0 normal strength; anisotropic filtering to 16x; mip bias -0.5; force mip 0 inside 30 m. Target foreground edge energy at or above 45 to match the mid band. Add a wet-sand darkening band 1.5 m inland of the waterline with a 0.25 roughness delta.
- Hex grid: one system, one stroke. Render the grid as a distance field in the terrain material at constant 1.5 px screen width, alpha 0.22, drawn once — delete the second pale stroke. Territory border as a separate 3.5 px pass co-located on the same edge (offset 0, not parallel) with a 6 px inner gradient fade instead of a hard companion line. MSAA 4x on that pass or TAA; the 14-degree diagonals are currently raw stair-steps.
- Ambient/GI: replace the flat shadow multiply with a sky-colour ambient at 4500 K, luminance floor 0.06-0.09, plus one indirect bounce so shadowed albedo survives. Then a filmic curve with the shoulder starting at 0.75 so the beach stops flattening to paper (frame p95 is 193 with no roll-off).
- Solmere layout: snap building props to a 0.5 m grid with a 0.4 m clearance test against neighbours and a no-intersect check against banner and flag quads. Replace both hard-cut city plinths with a 1.5 m terrain-height blend plus a rubble/foliage skirt and a 2 m AO gradient into the surrounding tile.
- Growth read: bind the city mesh set to pop. 1-3 = 3-5 huts; 4-6 = add a stone wall segment and two-storey buildings; 7-9 = keep, closed wall ring, and two farm improvements pushed onto adjacent worked tiles. Currently pop 7 and pop 5 are indistinguishable clutter and no tile around either city is improved.
- Banners: tint the body with the owner's primary civ colour and the pop badge with the secondary, add a 12 px stem anchoring the banner to the city centre, and add the 6 px food and production arcs. Also filter the action bar by unit type — a Warrior must not offer FOUND — and dedupe the notification queue; two identical earthworks dispatches are stacked at T30. Give the three leaders three distinct busts; they are currently one mesh with a hue swap.

## critique 14 — 46/100
Reject. Unlabeled, side by side with a real Civ VI in-game shot at gameplay zoom, a player picks Civ VI in under a second — and the single tell is the hex grid. Yours is a constant-screen-width embossed pipe: a light blue-gray core with a dark outline on BOTH sides (visible everywhere in the NW field), stair-stepping down every diagonal, flaring into a bright knot at every three-way vertex, and doubling up wherever a cyan territory ribbon runs along an edge that already carries the neutral seam. Civ VI's grid is a soft, distance-fading darkening that lives IN the ground; it never gets brighter than the dirt and it disappears as hexes shrink. Yours gets thicker relative to the hex the further out you look, so the far field turns into a wireframe overlay. The diagnostic underneath it is worse: I measured near-field edge energy at 22.6 (grass) / 32.3 (sand) against 45.5 in the MID field — your near field is softer than your middle distance, which is backwards — while the grid ribbon resolves in a single pixel (598: 55,82,113 → 599: 115,161,209 → 601: back to terrain). The only razor-sharp thing in the frame is the overlay, and the actual ground is mush. That is the signature of a decal/grid pass composited after the TAA resolve, or drawn unjittered: TAA is eating your terrain detail and giving the grid nothing. A player says "the grid looks drawn on top of a blurry photo." They're right. Six of the listed automatic fails are present — mip-smeared near field, flat-matte surfaces (city plinth wall, warrior torso, lake plane), objects with no contact shadow (city towers, plinth, dock, and the warrior's 40px flagpole casts nothing), double-drawn hex lines, thin-line aliasing, and a dead grading ceiling (map-region luma p99.9 = 239, 0.0006% of pixels above 252 — nothing in this frame sparkles). The UI card art and typography are the only genuinely shippable elements here, and even those have a ghost text string ("Trib…") burned at low alpha into the top band of the Solmere plaque. This reads as a competent tech demo with a good UI artist attached, not a shipping 4X.

**Problems**
- DOUBLE-DRAWN HEX LINES (auto-fail). Every neutral edge is a 3-band dark/light/dark embossed ribbon ~5-6px wide at constant screen width, so it grows relative to the hex with distance and turns the NW quadrant into a wireframe. Where a cyan territory ribbon runs on the same edge you get both ribbons offset by 1-2px — a literal doubled line. Three-way vertices flare bright from additive quad overlap.
- NEAR-FIELD IS BLURRIER THAN MID-FIELD (auto-fail). Measured mean |edge| : near-grass 22.6, near-sand 32.3, mid-grass 45.5. The sand hex under the warrior is a single low-frequency fBm blotch — no grain, no pebble decals, no detail normal, no anisotropic resolve. At this zoom Civ VI shows sand ripple normals and scattered rock/grass cards.
- NOTHING CASTS A READABLE SHADOW (auto-fail). Autocontrast on the warrior shows only a formless dark smudge with no head, no arms, and critically no flagpole shadow — a 40px vertical casting nothing means cascade 0 texel size is far too coarse or the mesh is out of the caster set. The city casts nothing onto its own roofs, its plinth, the grass, or the water. No AO at the plinth/ground contact line at all.
- FLAT-MATTE UNTEXTURED SURFACES (auto-fail). The city plinth is a hard-edged extrusion with a uniform gray-blue wall — cardboard. The warrior torso is one flat pale-green value with a single mauve band, zero normal or roughness variation. The lake is a flat plane with no normal, no fresnel, no reflection, no depth absorption.
- THIN-LINE ALIASING (auto-fail). Grid ribbon steps 1px per column ({599,600,601} → {598,599,600} with no intermediate coverage). The Solmere/Aurelia banner chevrons stair-step down their diagonals with a brown fringe. Flagpoles are 1-2px hard sticks. TAA is blurring surfaces but resolving none of the high-contrast thin geometry.
- DEAD GRADING CEILING + COLD CRUSHED TOE. Map-region luma: p1=17, p50=89, p95=193, p99.9=239, only 0.0006% above 252. The top 6% of the range is unused — no sun glint on 400,000 pixels of ocean, no specular on a single roof. Meanwhile shadow ambient is a heavy navy (~0.28,0.40,0.50), so the frame reads as night-in-shadow / day-in-light with no mid-tone. Washed-out top, crushed cold bottom, both at once.
- WATER IS ONE TEXTURE AT ONE SCALE. Ocean and lake are the same scrolling teal caustic with no depth gradient (shallow reads identical to deep), no directional wave normals, no fresnel, no SSR, and shore foam that is a static white blob rather than depth-driven. Zero reflections anywhere in the frame is a 2026 tell on its own.
- TOY SILHOUETTES + UI GHOST DRAW. The warrior is a Playmobil blob — capsule arms, mitten hands, a 2px stick for a weapon, no rim light separating it from the ground. And there is leftover ghost text at low alpha in the top band of the Solmere plaque (img ~1226-1242, 327-331) — a stale draw that shipped into the frame.

**Fixes**
- Fix the grid AA at the source, not with more TAA: make the grid an analytic SDF in the terrain shader — distance to the nearest of the 3 hex edge planes — and feather with screen-space derivatives: w = fwidth(d); a = 1.0 - smoothstep(hw - w, hw + w, d). This removes the staircase and, because it is one sample per pixel, removes the vertex knots without any mesh work. Composite it BEFORE the TAA resolve, in the jittered pass.
- Kill the double line: make the neutral grid darkening-only, no light core. albedo *= lerp(1.0, 0.72, a), half-width 0.8px (1.6px total) at the near plane, and fade alpha to 0 as the hex's screen size drops below 48px so the far field stops reading as wireframe. Suppress the neutral ribbon entirely on any edge that also carries a territory border — one edge, one line.
- Territory borders: separate pass, 5-6px, soft-shouldered (core alpha 0.85, 2.5px feather), drawn only on the outer perimeter of a territory. Raise contrast against sand — current peak is (125,172,217) at 68% luma, which vanishes over (216,170,103) beach. Darken the border core to ~(40,90,150) with a 1px light inner rim so it reads on both grass and sand.
- Blend the ribbon quads with max() on alpha, not additive, or rebuild the grid as a single triangulated ring mesh with shared corner vertices — either kills the bright three-way vertex knots.
- Restore near-field detail: add a detail-albedo + detail-normal layer tiled at ~0.35m, strength = 1 - saturate(depth/25m). Set terrain sampler mip bias to -0.5 and force 16x anisotropic. Expect near-field |edge| to land above mid-field (target >55 near vs 45 mid), inverting the current measurement.
- If TAA is the blur source (near-field motion is largest, which matches the data), tighten the history clamp: neighborhood clamp in YCoCg against a 1.25-sigma box instead of a min/max box, and drop blend feedback from ~0.95 to 0.88. Verify by re-measuring near-grass edge energy — it should jump 40%+.
- Shadows: set CSM cascade 0 to cover 18-22m at 2048 (~1cm/texel) so a 40px flagpole projects a visible shadow, and switch to normal-offset bias at 1.5 texels instead of a constant depth bias. Add a screen-space contact-shadow trace, 16 steps at 0.25m max ray length — that single pass gives every unit, tree, rock, and the city plinth the base darkening the frame currently has nowhere.
- City plinth: triplanar stone albedo + detail normal on the wall, add the city meshes to the shadow-caster set (the towers currently cast nothing onto their own roofs), and run GTAO at half-res with a 1.2m world radius, power 1.6, applied to indirect only — the plinth/ground contact line needs it most.
- Grading: retune the tonemap so highlights survive. Give water a GGX spec at roughness 0.04-0.08 and terrain a weak spec at roughness 0.75 so sun glint reaches 250-255 across ~0.5% of the frame; ACES shoulder start at 0.85 rather than crushing at 0.93. Lift the toe so p1 lands 26-30. Cut the shadow ambient blue by 25% and add a ground-bounce term around (0.35,0.30,0.18) so shadows read as warm-shaded earth rather than dusk.
- Water: two normal maps at 0.8x and 3.5x UV scrolling at +0.03 and -0.017 units/s, Beer-Lambert depth absorption with extinction ~(0.35,0.08,0.05) per meter so shallow separates from deep, fresnel-weighted SSR (fall back to a cubemap), and depth-driven foam: foam = 1 - saturate(depth/0.6m) modulated by a scrolling noise, replacing the static white shoreline blob. Also remove the ghost text baked into the Solmere plaque top band and add a 1px SDF feather to the banner chevron diagonals.

## critique 15 — 38/100
Reject. Six automatic fails present, any one of which kills it: mip-smeared near-field, props with no contact shadow, an untextured flat-matte plinth, double-drawn hex lines, a literal green placeholder checkerboard, and aliased border polylines.

Side by side, unlabeled, against a real Civ VI in-game shot: the player picks Civ VI instantly, and not on a close call. The single tell is contact shadow on ground clutter. In Civ VI every pebble, tuft, bush and tree is bedded into the ground with a painted AO footprint, so the props and the terrain are one surface. Here I sampled a vertical through the pebbles at (1033,703) and (1116,708) — sand luminance 180, pebble body 96, and then sand comes straight back to 125-139 within 1-3 px. There is no darkened band on the sun-opposite side at all. Same for roughly 35 grass tufts and every rock on the plateau. Combine that with a ground texture that is one octave of gaussian cloud noise (measured high-pass mean 8.3 near-field, actually LOWER than the 11.5 at midground — the closest ground in frame is the blurriest ground in frame) and the whole map reads as stickers laid on a soft painting. A player will not say "the AO decals are missing"; they will say "that one looks like a real place and this one looks like assets scattered on a plane." That is the tell.

Second-order tells that seal it: every hex edge is drawn twice — a pale gray-blue stroke and a dark navy stroke offset about 2 px, crossing into a bright X blob at each vertex (see (166,153) and the edge run around (150,175)-(230,270)) — and the grid decal draws OVER the props, bisecting the tuft at (90,139) and running across the top face of the village plinth. And shadow color is broken at the physical level: lit sand measures hue 34 deg / sat 0.47, shadowed sand hue 130 deg / sat 0.17, and the tuft-base shadow at (238,518) is rgb(45,55,84) — pure navy on tan sand. That is a raw sky-cubemap ambient with zero albedo weighting and zero ground bounce. Shadows rotating 90-100 degrees off the lit hue is something no shipped Firaxis frame does.

Grading is not clipped (p1 18 / p99 222, 0.00% blown, 0.23% crushed) — that is the one thing this frame gets right, and it does not save it. The aerial perspective is far too aggressive too close: the settlement at y≈130 is already sunk to L 40-90 blue-gray about eight tiles out. And a green programmer-art checkerboard is sitting on the plaza quad at (488,139) in a shot someone chose to submit.

This is a tech demo with good composition and a competent HUD, not a shipping 4X terrain pass.

**Problems**
- Near-field terrain is mip-smeared and has less detail than midground: clean-patch high-pass mean 8.3 at y790-840 vs 11.5 at y480-530. The closest ground in frame is the blurriest. Reads as trilinear-only filtering plus a positive LOD bias, and the base albedo carries only one noise octave — at 10x on (230,470)-(330,540) there is literally zero pixel-scale grain, only 4-8 px gaussian blobs.
- Zero contact shadow on all ground scatter. Vertical scans through pebbles at (1033,703) and (1116,708) show sand 180 -> pebble 96 -> sand 125-139 with no darkened band on either side. Same for ~35 tufts and every boulder (e.g. (44,113), (1000,690)). Structures cast (the dock does), scatter does not — a per-instance cast flag is off.
- Hex grid is double-drawn: two strokes per edge, pale gray-blue plus dark navy, offset ~2 px, converging into a bright X blob at every vertex. Clearest at (166,153) and along (150,175)-(230,270). It also z-orders after props, bisecting the tuft at (90,139) and crossing the raised village plinth's top face.
- Ambient is a raw sky sample with no albedo weighting and no ground bounce. Lit sand hue 34deg/sat 0.47; shadowed sand hue 130deg/sat 0.17; tuft-base shadow at (238,518) is rgb(45,55,84), navy on tan. Shadow hue rotates 90-100deg off the lit hue across the frame.
- Untextured flat-matte surface plus programmer art: the settlement plinth at (380,60)-(600,210) is a solid gray-violet disc with a hard vector rim and no material at all, the buildings are flat-shaded untextured cones, and the plaza quad at (488,139) is a green CHECKERBOARD placeholder.
- One tuft sprite reused ~35x and one pebble mesh ~20x per screen at a single scale, single hue, single yaw — visible instancing with no variation. Tufts are all ~9 px regardless of depth, so the scatter has no distance hierarchy.
- Biome transitions are straight alpha lerps snapped to hex edges with no transitional flora, plus at least one hard-edged rectangular grass splat pasted onto the sand at (1362,517). Scatter density stops dead at the mask boundary instead of ramping.
- Two incompatible shore treatments in one frame: the coast at (1150,420)-(1420,600) has a soft white foam bloom smeared 25-30 px up onto the land with no wave structure, while the lake edge at (720,595) is a hard 1 px polygon cut against the bank with no foam, no wet-sand darkening, and no depth ramp. Territory border lines are hard-aliased staircases drawn brighter than the terrain — rgb(145,185,224) at (1152,650), above local terrain luminance.

**Fixes**
- Fix filtering first: force 16x anisotropic on the terrain sampler and set mip LOD bias to -0.5. Then add a triplanar detail albedo+normal layer tiled at a 0.35 m world period, blended by camera distance with saturate(1 - (dist-18)/37) so it is full strength inside 18 m and gone by 55 m. Target: near-field clean-patch high-pass mean >= 25 with midground at 14-16, i.e. near must measure highest, not lowest.
- Re-author the plains/sand albedo with three frequency bands instead of one: macro at 4 m, meso at 0.8 m, micro at 0.12 m, on a 2048sq tiling set. Drive a detail normal at the 0.12 m band with 0.6 strength. Break the repeat with a 32 m macro-variation map multiplying albedo in [0.85, 1.18].
- Turn the per-instance shadow-cast flag back on for all scatter (pebbles, tufts, bushes, boulders) and add screen-space contact shadows at 0.25 m max ray length, 8 steps — the 2048 cascade cannot resolve props this small on its own.
- Add a per-instance AO decal under every scatter prop: radial gradient at 1.6x the prop footprint radius, multiply blend, 0.55 opacity at center falling to 0 at the rim, center biased 15% of the radius toward the light-opposite direction.
- Fix the ambient: ambient = skyIrradiance * 0.35 + albedo * groundAlbedo * skyLuminance * 0.45. Validation target: shadowed sand lands at hue 28-38 deg, sat 0.30-0.38, value 0.28-0.36 — shadow hue must stay within 10 deg of the lit hue everywhere in frame.
- Kill the second hex stroke. If the two passes are grid and territory border, inset the border 4 px inside the hex, drop grid opacity to 25% wherever a border overlaps, and cap grid width at 1.0 px screen with a 0.5 px AA falloff. Render the grid decal BEFORE scatter with depth-test enabled so it stops drawing over rocks, tufts and the village plinth.
- Stop rasterizing the border as a flat polyline. Multiply the border color into the terrain lighting rather than drawing over it, cap it at 0.85 of local terrain luminance, and antialias with a 1.5 px smoothstep on the signed distance to the edge.
- Give scatter real variation: per-instance uniform scale in [0.6, 1.7], yaw in [0, 360), albedo hue jitter +/-8 deg, value jitter +/-18%, and a minimum of 3 mesh/sprite variants each for tufts and pebbles. Ramp tuft world scale with camera distance so near tufts render ~2.2x the far ones.
- Replace the biome alpha lerp with a height-blend off the detail heightmap: heightBlend(h1+a1, h2+a2, 0.15) at a 0.35 m blend width, and drive scatter density off the same mask so tuft density ramps 0 to full across a 1.5-2 tile band. Find and delete the hard-edged grass splat quad at (1362,517).
- Unify the shoreline: depth-based wet-sand darkening to 0.55x albedo over the first 0.6 m of depth, foam mask driven by 1 - saturate(depth/0.45) with two animated noise bands, and a 0.12 m intersection foam line — applied to lakes and coast alike. Separately, push fog start out to ~3.5x its current distance and cap max fog density at 0.35 so the mid-map at y≈130 stops crushing to blue-gray L 40-90. Also texture or delete the settlement plinth, remove the green checkerboard at (488,139), and dedupe the two identical T30 'Aeonian engineers raise earthworks' dispatches into one row with a count badge.

## critique 16 — 31/100
Side by side, unlabeled, with any Civ VI coastal gameplay screenshot: the player picks Civ VI in under a second, and it isn't close. The single tell is the specular. Your "sun glitter" is one stamped lens-shaped white dash, roughly 14x3px, tilted ~20 degrees, sprayed on a near-regular lattice across every drop of water in the frame — and it is the exact same pixel size in the sheltered harbor at Aurelia (crop 680-900 x 460-600) as it is at the far top-right edge of the map 700px deeper into the scene (crop 1230-1350 x 120-220). Real specular shrinks and densifies with distance and clusters on wave crests along the sun axis. Yours ignores perspective, ignores the water normal underneath it, lands indifferently on crests, troughs and foam, and even sits on top of the translucent water plates where they overhang land. A player doesn't have the vocabulary for "non-perspective-corrected glitter decal" — they say "why is there rice floating on the ocean." That's the tell. The runner-up tell, one glance later: the ocean is visibly built from hexagons. At 620-1010 x 400-700 three adjacent water hexes carry three different materials — flat matte blue with zero texture in the harbor, turquoise caustic worms in the tile below it, and a bright high-frequency white sparkle field in the tile north-east — each terminating in a razor-straight seam at the tile edge. Civ VI renders one continuous water surface whose color is driven by depth; you can pan across its whole map and never see a straight-line tint change. This is a tech demo of a hex-plate water shader, not a shipped 4X ocean.

**Problems**
- AUTOMATIC FAIL - the ocean is not a surface, it is a stack of translucent hexagonal plates. At 560-900 x 560-880 the water hex is unmistakably a semi-transparent quad floating above the heightmap: the green grass tufts and ground texture of the neighbouring land tile are clearly visible THROUGH the blue tint where the plate overhangs, and the plate edge slices the coast foam off with a hard straight diagonal. Adjacent water hexes have discontinuous tint and entirely different surface textures with hard seams (620-1010 x 400-700). Even in open deep ocean behind the notification stack there is a visible straight lighter seam through the blue.
- AUTOMATIC FAIL - untextured, flat-matte water. The harbor at 680-900 x 460-600 is a solid unlit blue fill. No normal map, no caustics, no ripple, no reflection, no gradient. It is a flat colour with a few white dash decals on it. Elsewhere the water is one low-res cyan squiggle layer magnified past its resolution over a flat base - blocky and pixel-steppy at 8x with soft mush between (690-810 x 600-700).
- AUTOMATIC FAIL - no contact shadows on anything touching water. The dock at ~740,530 casts nothing on the water beneath it. Its four pilings pierce the surface with no ripple ring, no foam collar, no waterline darkening - they intersect a flat plane like level-editor placeholders. The trees at 1150-1580 x 300-640 and 560-900 x 560-880 cast no shadow on sand or water. Grass tufts have no contact AO. The masts and banners at Solmere cast nothing.
- AUTOMATIC FAIL - illegible, double-drawn hex lines. At 8x (690-810 x 600-700) a pale-blue 3px territory ribbon runs parallel to a second darker blue-grey 2px tile outline offset by 2-3px, and at the lower-left a dark grey-green thin line runs parallel to a lighter one. Three line systems coexist across the frame (grey tile outline, blue territory ribbon, cream selection outline). The grey outline is drawn opaque ON TOP of the coast foam at 1150-1580 x 300-640, cutting the foam band with a straight line.
- AUTOMATIC FAIL - blurry, mip-smeared near-field terrain. The sand and dirt bank across the entire bottom third (y>600) is a low-frequency tan/brown blur with no grain, no pebble detail, no detail normal. The stone apron left of the dock is grey mush. The rubble at ~830,530 is an unresolvable smear. This is the closest terrain to camera and it carries less detail than the distant ocean.
- AUTOMATIC FAIL - toy silhouettes and aliasing. Trees are flat low-poly green blobs with hard-edged alpha tops and stick trunks (1150-1580 x 300-640). Solmere's buildings are flat-colour blocks - red roof planes whose tile pattern is mip-blurred to nothing, untextured grey walls, banners that are solid blue rectangles with an illegible crest. The territory border ribbon is drawn with zero AA - hard stairsteps on every diagonal against soft terrain behind it.
- No sky reflection and inverted Fresnel. There is no cloud, no sky tint, no horizon ramp anywhere on the water. The far half of the ocean (top of frame) is DARKER and more saturated than the near half, which is backwards for a ~55-degree camera pitch - grazing angles should be the pale sky-reflecting ones. The harbor reflects no part of Aurelia: not the red roofs, not the walls, not the dock. Nothing.
- Coast physics are absent. Foam is an airbrushed white cotton smear with no breaker line, no lace edge, no swash/backwash, and it sits ON the dry sand above the waterline in several places (900-1300 x 60-340). Wet sand does not exist anywhere - the beach goes from foam straight to dry tan with no darkened saturated band and no roughness drop. Caustics are drawn on the surface plane at full strength in deep open ocean rather than projected onto the seabed and masked by depth. Separately, the notification stack ships a duplicate: 'Aeonian engineers raise earthworks.' twice, identical, both T30.

**Fixes**
- Delete the per-hex water plates outright. Render one continuous water mesh across the entire map at a single sea level Y=0.0. Depth comes from sampling the terrain heightfield, not from tile ownership. This one change removes every straight-line tint seam, every plate-over-grass overhang, and the flat-matte harbor tile in a single stroke. Nothing else on this list matters until this is done.
- Depth-driven colour on that single surface. Per-channel Beer-Lambert absorption with coefficients R=0.45, G=0.11, B=0.06 per world unit, so red dies first and you get the shallow-to-deep ramp for free. Anchor shallow at #2E8C9E and deep at #10314F, transmittance exp(-d*coeff) over d = 0 to 6 units. Blend the seabed albedo through it below d=3.5.
- Bin the stamped dash sprite sheet. Replace with GGX specular on the real water normal: roughness 0.03-0.08, sun as a disc light at 0.55 degrees angular radius. Drive the normal with two scrolling normal maps at 1.7x and 0.31x world tiling panning at 0.055 and 0.021 uv/s on divergent headings, plus a third detail map at 0.07x for close range. Glint size and density then fall out of the NDF and perspective automatically. If art insists on an authored sparkle layer, scale each sprite by 1/depth and clamp instances per screen-pixel to 1.
- Add Fresnel and a real sky reflection. Schlick with F0=0.02 so grazing angles at the top of the frame reach 0.6-0.9 reflectance. Feed it the sky cubemap plus half-res SSR of the coastline and city, 12 steps, 0.6 roughness cone, 3-tap vertical blur. The far ocean must go paler and sky-tinted; right now it is darker than the near ocean, which is the wrong sign.
- Mask caustics by depth and project them on the seabed, not the surface. smoothstep(3.5, 0.4, d) so they vanish past 3.5 units, sample at the seabed depth so they parallax correctly, 3-frame animated set at 0.5 Hz, tint toward seabed albedo, peak intensity 0.35. Deep ocean should have zero caustics and currently has them at full strength.
- Rebuild coast foam as a shoreline signed-distance band with three layers driven by a 0.18 Hz swash sawtooth: (a) breaker line at d = 0.9-1.3m, alpha 0.9, advancing and retreating 0.35m over the cycle; (b) dissipation lace at d = 0.0-0.9m using an fbm erosion threshold that rises through the cycle; (c) sub-surface whitewater tint at d = 1.3-2.5m, alpha 0.25, rendered in the water body not on the sand. Foam albedo 0.86 not 1.0, and it must receive the same sun shadow as terrain.
- Add wet sand - this is six lines and it will do more for the coast than anything else here. wetness w = smoothstep(1.6, 0.0, height above waterline) modulated by the same swash cycle as the foam. Wet: albedo * 0.55, roughness 0.18 against 0.85 dry, F0 lifted to 0.035. Right now there is a hard tonal cut from white foam to dry tan with nothing between.
- Every object intersecting the water needs four things it currently has none of: a cast shadow onto the water surface; a foam collar decal at 0.35m radius for pilings and a 1.2m elongated ellipse for hulls at alpha 0.5; waterline albedo darkening to 0.4 for 0.15m above the intersection; and two outward-animating ripple rings at 0.6m and 1.1m radius, alpha 0.12, 0.4 m/s. Start with the dock at 740,530 - it is the most visible offender.
- Collapse the hex line systems from three to one. Delete the per-tile grey outline entirely. Render territory borders only, as a signed-distance ribbon at 1.6px core plus 1.2px feather, drawn into the AA-resolved buffer so the diagonals stop stairstepping. Over water, drop border opacity to 0.45 and composite as overlay so it stops slicing through foam as an opaque line. Keep the cream selection outline but only on the one selected tile.
- Fix the near field: negative mip bias of -0.75 with 16x anisotropic on all terrain layers, plus a detail albedo and detail normal at 0.25m tiling blended in over the 0-8m camera range. Then enable a 12m, 2048px shadow cascade 0 so props actually plant - every tree, rock, grass tuft and building in the bottom half of the frame is currently floating.

## critique 17 — 34/100
Rejected, and not narrowly. Side by side, unlabeled, against a real Civ VI gameplay shot, every player picks Civ VI in under a second. The single tell: nothing in this frame casts a shadow. Not one unit, city, tree, rock, flagpole, or pier plank has a directional shadow or a contact darkening — the warrior at (630,520) has boots that simply stop and the grass continues underneath, the Aurelia castle sits on the hex with no shadow leaning off it, the eight identical pebbles on Solmere's sand at (1050-1200, 700-800) float like stickers. Civ VI's read at gameplay zoom is built on a hard sun with cast shadows plus AO in every roof-wall junction; strip that and the whole map reads as decals on a painted plane, which is exactly what this reads as. Runner-up tell, equally instant once you look for it: the world-space city banners ("Aurelia 6", the hammer glyph at 560,455) are blurry and color-fringed while the top HUD text is razor sharp — two text sharpnesses in one frame means the banners are being composited into an upscaled 3D buffer. Shipping games never do that. Beyond the tells, the frame has seven of your listed automatic fails present simultaneously. Measured, not vibes: near-field sand at the bottom of the frame has Laplacian variance 140 while mid-field grass has 506 — detail goes DOWN as the camera gets closer, which is the textbook mip-smear signature; the ocean sits at 2728, i.e. pixel-scale albedo noise that will shimmer violently in motion; the luma histogram's 99.9th percentile is 228 with 0.001% of pixels above 247, so there is not a single specular highlight anywhere in a midday coastal scene; and quadrant means run TL 82 / TR 115 / BL 79 / BR 100, a 30% left-right imbalance from two unmotivated dark wedges with straight edges (one across the ocean behind the notification stack, one over the whole upper-left). Add the green checkerboard sitting in the middle of Calyx that reads as a literal missing-texture UV grid, the two flat grey untextured triangles poking out of the surf near (1063,413), the unit standing inside Solmere's wall with its shield clipping a roof, and the tree canopy passing through a banner — this is a tech demo, and a soft one. It is closer to a Humankind-inspired mobile 4X mockup than to anything Firaxis would greenlight for a Steam page.

**Problems**
- Zero shadows in the entire frame. No cast shadows, no contact AO, no blob fallback. Warrior at (630,520): boots end, terrain continues, no darkening at the contact point. Aurelia castle, Calyx, Solmere, all trees, all rocks, both flagpoles, the pier at (600,660) — all unshadowed. Automatic fail on its own.
- Near-field terrain is mip-smeared. Measured Laplacian variance: bottom-of-frame sand 140, mid-field grass 506, far terrain 86. Detail decreases as the camera approaches — no detail-normal/detail-albedo layer, and filtering is falling off a cliff on magnification. The whole 3D buffer also looks upscaled: world-space banner text is soft and fringed while HUD text at the same pixel size is crisp.
- Double- and triple-drawn hex lines. Along Calyx's south-west edge two parallel blue territory strokes run ~6px apart (each adjacent tile drawing its own copy) with a separate grey grid line under them; at the warrior's tile a blue border, a light grid line, and the gold selection hex all cross within 8px. All are hard-edged and stair-step down every diagonal — no AA on the grid at all.
- Untextured flat-matte surfaces everywhere. Calyx and Aurelia both sit on hard-edged flat grey elliptical pedestal discs with no blend into the terrain. Solmere's tower cone is pure vertex-colored pale yellow with a visibly faceted silhouette. Castle walls are flat grey with no albedo, normal, or roughness variation. Two flat grey-green untextured triangles protrude from the water at ~(1063,413) and ~(1030,455) — coastal skirt polys with a missing material.
- Programmer-art / toy silhouettes. A saturated green-on-green checkerboard square sits inside Calyx at ~(320,215) and reads exactly like a UV test grid. The warrior is four brown cylinders, a featureless dome helmet with a visor slot, and a flat blue disc shield with no boss — roughly 26px tall where Civ VI reads a unit at 55-70px with a legible weapon head and crest. Flags are 2-tone hard-cornered quads with no cloth shading.
- Grading is dull and unlit, not stylized. 99.9th percentile luma is 228; only 0.001% of pixels exceed 247. Median 88. There is no sun glint on water, no hot roof edge, no bright sand — the tonemapper's shoulder is doing nothing. Layered on top: two large unmotivated dark wedges with straight edges (one diagonal across the ocean top-right, one over the entire upper-left) drop the left half to mean 82 vs 115 on the right, and they kill territory-border legibility — the border at (95-135, 200) is (57,69,84) against grass (64,88,71), effectively invisible.
- Geometry interpenetration and placement failures at Solmere. A unit stands halfway inside the city wall with its shield overlapping a red roof. A tree canopy passes through both a blue banner and a roof. A soft white smoke card lies flat and clipped into the sand at ~(1090,635). The scattered pebbles are one mesh at one rotation and one scale, repeated eight times; the grass tufts are one billboard at one rotation, repeated across the whole map.
- Aliasing and shimmer. Ocean albedo noise measures at 2728 Laplacian variance — pixel-scale high frequency in a moving surface with no roughness clamping, which will crawl. Hex lines and the gold movement chevron stair-step with no edge AA. The warrior's flag has a magenta/lavender fringe along its top contour at (615,485), the signature of an over-strength post-tonemap sharpen on an aliased edge. Water also shows hard per-hex color quads with no depth blend, so the shallow/deep transition breaks along straight tile edges.

**Fixes**
- Sun and shadows: one directional light, azimuth 135 deg, elevation 42 deg. 3-cascade CSM at 25/60/150 m, 2048x2048 per cascade, 2x2 PCF, normal-offset bias 0.6 texels, depth bias 0.0015. Add GTAO at 0.35 m radius, 2 slices x 3 steps, intensity 1.0. Under every unit and building also drop a blob-shadow decal at 1.2x footprint, 55% opacity, 0.15 m falloff, so contact reads even when the CSM texel lands wrong.
- Near-field terrain: add a detail-albedo + detail-normal pair tiling at 0.5 m (8x macro UV), blended by a triplanar mask over 0-18 m from camera and faded out by 26 m. Set anisotropic filtering to 16x and mip bias -0.5. Target: Laplacian variance in the bottom 80 rows >= 800 (currently 140) and >= mid-field's 506.
- Stop upscaling the 3D buffer, or at minimum move all world-space labels (city banners, the movement-cost '1' pip) into the native-resolution UI pass, projecting only the anchor point. Give each banner a drop shadow at offset (0,2), 6px blur, 55% black, so it separates from terrain instead of sitting flat on it.
- Hex grid: dedupe the edge list so each shared edge is drawn once, then render the grid as a single screen-space SDF decal at 1.6px core with 1.0px smoothstep AA. Territory borders become a separate pass: 5px band inset 3px inside the tile, alpha 0.85, drawn after the grid with 0.02 m polygon offset. Selection ring gets its own additive pass at 3px pulsing at 0.8 Hz. One line per edge, three distinct visual weights, no crossings.
- Ocean: replace the animated albedo noise with two scrolling normal maps (tiling 12 m at 0.030 UV/s, tiling 31 m at -0.017 UV/s), GGX roughness 0.06, Fresnel F0 0.02, cubemap reflection with SSR on top. Drive shallow/deep color from scene depth (ramp 0.9 m to 14 m), not from a per-hex tint, to kill the straight tile-edge color breaks. Clamp specular via Toksvig / roughness-from-normal-variance to stop the shimmer that the 2728 variance guarantees.
- Grading: +0.6 EV exposure, ACES tonemap with the shoulder retuned so sun-facing sand and water specular land at 250-255 for 0.5-1.5% of pixels. Target histogram: mid-frame mean luma 118-125, 95th pct ~205, 99.9th pct ~252 (currently 88 / 168 / 228). Drop the cloud-shadow layer opacity from ~0.45 to 0.18, scroll it, and give it a noise-broken edge — never a straight line. Rebalance so left-right quadrant means sit within 8% of each other, not 30%.
- Fix the missing materials: assign the cliff material to the coastal skirt polys at ~(1063,413) and ~(1030,455), or push the skirt 0.5 m below the water plane so it never surfaces. Replace Calyx's green checkerboard at (320,215) with actual farm-plot geometry (row meshes + furrow normal). Give castle roofs/walls real albedo+normal+roughness: stone at roughness 0.55-0.75, tile at 0.40, with edge wear masks.
- Kill the pedestal discs. Height-blend the city ground material into the terrain over a 1.5 m transition with a noise mask (blend sharpness ~0.35) so the footprint dissolves. No hard ellipse, no flat untextured grey.
- Units: scale up ~1.35x so a warrior reads at 55-70px at this zoom. Add a rim light at 0.6 intensity from the sun's complement to separate silhouettes from terrain. Model an actual spearhead and a shield boss. Snap units to per-tile unit sockets rather than tile center, and run a capsule-vs-building depenetration pass — that fixes the Solmere unit standing inside the wall. Run the same bounds check on props: reject any tree whose bounds overlap a city footprint by more than 0.1 m (fixes the canopy through the banner and roof).
- Post and HUD: enable TAA with 8-tap Halton jitter and a 0.05 neighborhood blend clamp (or SMAA T2x minimum) to clear the hex-line and flag stair-stepping. Move the sharpen pass before tonemap and cut strength from ~0.8 to 0.25 to remove the magenta fringe at (615,485). In the top bar, set value baseline at y=30 and delta baseline at y=44 within the 55px bar with 8px bottom padding (the +17 currently clips the bar edge), normalize all resource icons to a 24x24 box at 20px optical cap height, and delete the empty separator cell plus the orphan gold wing ornament at x 870-1060.

